"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/ImageManipulator.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ImageManipulator = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const ImageTypes_1 = require("../image/ImageTypes");
const ImageRef_1 = require("../image/ImageRef");
const ImageTypes_2 = require("../image/ImageTypes");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for image processing. If applicable on the platform, all the methods return images with the same liveness flag as the input image.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await ImageManipulator.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class ImageManipulator {
    /**
    Initialize the image processor.
    @returns Instance of ImageManipulator.
    */
    static async create() {
        ImageManipulator.performStaticInitialization();
        const nativeHandle = await ImageManipulator.nativeImageManipulatorInit().catch(Errors_1.remapError);
        return new ImageManipulator(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeCropRect = addonNative.nativeImageManipulatorCropRect.bind(this);
        this.nativeRotate = addonNative.nativeImageManipulatorRotate.bind(this);
        this.nativeFlip = addonNative.nativeImageManipulatorFlip.bind(this);
        this.nativeResize = addonNative.nativeImageManipulatorResize.bind(this);
        ImageManipulator.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            ImageManipulator.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = ImageManipulator.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return ImageManipulator.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Crops out the given rectangle from the image.
    @param image - The image to crop.
    @param rect - The rectangle in normalized coordinates to crop.
    @returns Cropped image.
    */
    async cropRect(image, rect) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        if (!TypeVerification.verifyRectangleDouble(rect)) {
            throw TypeError('rect must be of type Rectangle');
        }
        const partial = await this.nativeCropRect(image, rect).catch(Errors_1.remapError);
        return ImageRef_1.ImageRef.fromPartial(partial);
    }
    /**
    Rotates the given image by the given angle.
    @param image - The image to rotate
    @param rotation - Rotation to be applied
    @returns Rotated image.
    */
    async rotate(image, rotation) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        if (!TypeVerification.enumVerifier(ImageTypes_2.ImageRotationValues)(rotation)) {
            throw TypeError('rotation must be of type ImageRotation');
        }
        const partial = await this.nativeRotate(image, rotation).catch(Errors_1.remapError);
        return ImageRef_1.ImageRef.fromPartial(partial);
    }
    /**
    Flips the given image horizontally or vertically.
    @param image - The image to flip
    @param origin - Location of the image origin in the image coordinate system.
    @returns The result of the filter application.
    */
    async flip(image, origin) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        if (!TypeVerification.enumVerifier(ImageTypes_1.ImageOriginValues)(origin)) {
            throw TypeError('origin must be of type ImageOrigin');
        }
        const partial = await this.nativeFlip(image, origin).catch(Errors_1.remapError);
        return ImageRef_1.ImageRef.fromPartial(partial);
    }
    /**
    Resizes the given image to the given size.
    @param image - The image to resize
    @param size - Size of the longer side of the image.
    @returns Resized image.
    */
    async resize(image, size) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        if (!TypeVerification.verifyInt(size)) {
            throw TypeError('size must be of type number');
        }
        const partial = await this.nativeResize(image, size).catch(Errors_1.remapError);
        return ImageRef_1.ImageRef.fromPartial(partial);
    }
    static performStaticInitialization() {
        if (ImageManipulator.staticInitializationPerformed) {
            return;
        }
        ImageManipulator.staticInitializationPerformed = true;
        ImageManipulator.nativeImageManipulatorInit = addonNative.nativeImageManipulatorInit.bind(ImageManipulator);
        ImageManipulator.nativeDispose = addonNative.nativeImageManipulatorDispose;
    }
}
exports.ImageManipulator = ImageManipulator;
ImageManipulator.registry = new FinalizationRegistry((nativeHandle) => {
    ImageManipulator.nativeDispose(nativeHandle);
});
ImageManipulator.staticInitializationPerformed = false;
