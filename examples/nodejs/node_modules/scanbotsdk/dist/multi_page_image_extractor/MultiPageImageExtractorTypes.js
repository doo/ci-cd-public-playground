"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/MultiPageImageExtractorTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PageExtractionResult = exports.ExtractedPage = exports.ExtractedImage = exports.ExtractedImageMetadata = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const ImageRef_1 = require("../image/ImageRef");
/**
The metadata is taken from the PDF stream's dictionary, not from the image header inside the stream.
In a well-formed PDF file, the image metadata encoded in the stream dictionary should be the same as the one encoded in the image header.
This, however, is neither required nor checked and may not be true for an ill-formed PDF.
*/
class ExtractedImageMetadata {
    /** @param source {@displayType `DeepPartial<ExtractedImageMetadata>`} */
    constructor(source = {}) {
        if (source.height !== undefined) {
            this.height = source.height;
            if (!TypeVerification.verifyInt(this.height)) {
                throw TypeError('height must be of type number');
            }
        }
        else {
            throw new Error("height must be present in constructor argument");
        }
        if (source.width !== undefined) {
            this.width = source.width;
            if (!TypeVerification.verifyInt(this.width)) {
                throw TypeError('width must be of type number');
            }
        }
        else {
            throw new Error("width must be present in constructor argument");
        }
    }
}
exports.ExtractedImageMetadata = ExtractedImageMetadata;
/**
An image extracted from a multipage image.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getExtractedImageFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class ExtractedImage {
    /** @param source {@displayType `DeepPartial<ExtractedImage>`} */
    constructor(source = {}) {
        this._releasePromise = null;
        if (source.image !== undefined) {
            this.image = ImageRef_1.ImageRef.fromPartial(source.image);
        }
        else {
            throw new Error("image must be present in constructor argument");
        }
        if (source.metadata !== undefined) {
            this.metadata = source.metadata != null ? new ExtractedImageMetadata(source.metadata) : null;
        }
        else {
            throw new Error("metadata must be present in constructor argument");
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            promises.push(this.image.release());
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.ExtractedImage = ExtractedImage;
/**
A single page extracted from a multipage image.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getExtractedPageFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class ExtractedPage {
    /** @param source {@displayType `DeepPartial<ExtractedPage>`} */
    constructor(source = {}) {
        this._releasePromise = null;
        if (source.images !== undefined) {
            this.images = source.images.map((it) => { return new ExtractedImage(it); });
        }
        else {
            throw new Error("images must be present in constructor argument");
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            this.images.forEach((it) => { promises.push(it.release()); });
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.ExtractedPage = ExtractedPage;
/**
Result of the extraction of pages from a multipage image.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getPageExtractionResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class PageExtractionResult {
    /** @param source {@displayType `DeepPartial<PageExtractionResult>`} */
    constructor(source = {}) {
        this._releasePromise = null;
        if (source.pages !== undefined) {
            this.pages = source.pages.map((it) => { return new ExtractedPage(it); });
        }
        else {
            throw new Error("pages must be present in constructor argument");
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            this.pages.forEach((it) => { promises.push(it.release()); });
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.PageExtractionResult = PageExtractionResult;
