"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/MultiPageImageExtractor.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MultiPageImageExtractor = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const MultiPageImageExtractorTypes_1 = require("../multi_page_image_extractor/MultiPageImageExtractorTypes");
const RandomAccessSource_1 = require("../io/RandomAccessSource");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for extracting images from multi-page images (PDF, TIFF) and single-page images (JPEG, PNG).
 *
 * Required licence feature(s): ImageProcessing.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await MultiPageImageExtractor.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class MultiPageImageExtractor {
    /**
    Initialize the extractor.
    @returns Instance of MultiPageImageExtractor.
    */
    static async create() {
        MultiPageImageExtractor.performStaticInitialization();
        const nativeHandle = await MultiPageImageExtractor.nativeMultiPageImageExtractorInit().catch(Errors_1.remapError);
        return new MultiPageImageExtractor(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeRun = addonNative.nativeMultiPageImageExtractorRun.bind(this);
        MultiPageImageExtractor.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            MultiPageImageExtractor.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = MultiPageImageExtractor.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return MultiPageImageExtractor.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Extracts images from the given source. If possible, images that are originally stored in a compressed way, are not decompressed automatically but are initialized in a lazy way.
    * This means that the image data is only decompressed when it is accessed for the first time.
    * That's why when the images are used after extraction, it is recommended to close (dispose) (if platform uses Garbage Collector) images as soon as they are not needed anymore to prevent storing a lot of images in decompressed state in memory.
    * After the method is executed, the source doesn't have to exist anymore.
    *
    @param source - The source to extract images from. Supported multi-page image formats are TIFF and PDF. Supported single page image formats are JPEG and PNG.
    @returns The result of the extraction.
    */
    async run(source) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(RandomAccessSource_1.RandomAccessSource)(source)) {
            throw TypeError('source must be of type RandomAccessSource');
        }
        const partial = await this.nativeRun(source).catch(Errors_1.remapError);
        return new MultiPageImageExtractorTypes_1.PageExtractionResult(partial);
    }
    static performStaticInitialization() {
        if (MultiPageImageExtractor.staticInitializationPerformed) {
            return;
        }
        MultiPageImageExtractor.staticInitializationPerformed = true;
        MultiPageImageExtractor.nativeMultiPageImageExtractorInit = addonNative.nativeMultiPageImageExtractorInit.bind(MultiPageImageExtractor);
        MultiPageImageExtractor.nativeDispose = addonNative.nativeMultiPageImageExtractorDispose;
    }
}
exports.MultiPageImageExtractor = MultiPageImageExtractor;
MultiPageImageExtractor.registry = new FinalizationRegistry((nativeHandle) => {
    MultiPageImageExtractor.nativeDispose(nativeHandle);
});
MultiPageImageExtractor.staticInitializationPerformed = false;
