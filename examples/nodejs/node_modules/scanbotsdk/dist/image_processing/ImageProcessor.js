"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/ImageProcessor.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ImageProcessor = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const ParametricFilters_1 = require("../image_processing/ParametricFilters");
const ParametricFilters_2 = require("../image_processing/ParametricFilters");
const ParametricFilters_3 = require("../image_processing/ParametricFilters");
const ParametricFilters_4 = require("../image_processing/ParametricFilters");
const ParametricFilters_5 = require("../image_processing/ParametricFilters");
const ImageRef_1 = require("../image/ImageRef");
const ImageTypes_1 = require("../image/ImageTypes");
const ParametricFilters_6 = require("../image_processing/ParametricFilters");
const ParametricFilters_7 = require("../image_processing/ParametricFilters");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for image processing.
 *
 * Required licence feature(s): ImageProcessing.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await ImageProcessor.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class ImageProcessor {
    /**
    Initialize the image processor.
    @returns Instance of ImageProcessor.
    */
    static async create() {
        ImageProcessor.performStaticInitialization();
        const nativeHandle = await ImageProcessor.nativeImageProcessorInit().catch(Errors_1.remapError);
        return new ImageProcessor(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeCrop = addonNative.nativeImageProcessorCrop.bind(this);
        this.nativeResize = addonNative.nativeImageProcessorResize.bind(this);
        this.nativeRotate = addonNative.nativeImageProcessorRotate.bind(this);
        this.nativeApplyFilter = addonNative.nativeImageProcessorApplyFilter.bind(this);
        ImageProcessor.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            ImageProcessor.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = ImageProcessor.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return ImageProcessor.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Crops the given image to the given quadrilateral.
    @param image - The image to crop.
    @param quad - The quadrilateral of normalized points to crop.
    @returns Cropped image.
    */
    async crop(image, quad) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        if (!TypeVerification.listVerifier(TypeVerification.verifyPointDouble)(quad)) {
            throw TypeError('quad must be of type Point[]');
        }
        const partial = await this.nativeCrop(image, quad).catch(Errors_1.remapError);
        return ImageRef_1.ImageRef.fromPartial(partial);
    }
    /**
    Resizes the given image to the given size.
    @param image - The image to resize
    @param size - Size of the longer side of the image.
    @returns Resized image.
    */
    async resize(image, size) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        if (!TypeVerification.verifyInt(size)) {
            throw TypeError('size must be of type number');
        }
        const partial = await this.nativeResize(image, size).catch(Errors_1.remapError);
        return ImageRef_1.ImageRef.fromPartial(partial);
    }
    /**
    Rotates the given image by the given angle.
    @param image - The image to rotate
    @param rotation - Rotation to be applied
    @returns Rotated image.
    */
    async rotate(image, rotation) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        if (!TypeVerification.enumVerifier(ImageTypes_1.ImageRotationValues)(rotation)) {
            throw TypeError('rotation must be of type ImageRotation');
        }
        const partial = await this.nativeRotate(image, rotation).catch(Errors_1.remapError);
        return ImageRef_1.ImageRef.fromPartial(partial);
    }
    /**
    Applies given filter to the given image.
    @param image - The image to apply filter on
    @param filter - Filter to be applied
    @returns The result of the filter application.
    */
    async applyFilter(image, filter) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        if (!TypeVerification.polymorphicTypeVerifier([ParametricFilters_6.ScanbotBinarizationFilter, ParametricFilters_4.CustomBinarizationFilter, ParametricFilters_2.ColorDocumentFilter, ParametricFilters_1.BrightnessFilter, ParametricFilters_3.ContrastFilter, ParametricFilters_5.GrayscaleFilter, ParametricFilters_7.WhiteBlackPointFilter])(filter)) {
            throw TypeError('filter must be of type ParametricFilter');
        }
        const partial = await this.nativeApplyFilter(image, filter).catch(Errors_1.remapError);
        return ImageRef_1.ImageRef.fromPartial(partial);
    }
    static performStaticInitialization() {
        if (ImageProcessor.staticInitializationPerformed) {
            return;
        }
        ImageProcessor.staticInitializationPerformed = true;
        ImageProcessor.nativeImageProcessorInit = addonNative.nativeImageProcessorInit.bind(ImageProcessor);
        ImageProcessor.nativeDispose = addonNative.nativeImageProcessorDispose;
    }
}
exports.ImageProcessor = ImageProcessor;
ImageProcessor.registry = new FinalizationRegistry((nativeHandle) => {
    ImageProcessor.nativeDispose(nativeHandle);
});
ImageProcessor.staticInitializationPerformed = false;
