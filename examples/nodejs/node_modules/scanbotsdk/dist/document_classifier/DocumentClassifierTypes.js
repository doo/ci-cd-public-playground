"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/DocumentClassifierTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DocumentClassifierResult = exports.DocumentTypeValues = exports.DocumentClassifierStatusValues = exports.DocumentClassifierConfiguration = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const DocumentScannerTypes_1 = require("../document_scanner/DocumentScannerTypes");
/**
Configuration for the document classifier.
*/
class DocumentClassifierConfiguration {
    /** @param source {@displayType `DeepPartial<DocumentClassifierConfiguration>`} */
    constructor(source = {}) {
        /**
        If true, image will be scanned for a valid document first.
        
        Default is true
        */
        this.crop = true;
        if (source.crop !== undefined) {
            this.crop = source.crop;
            if (!TypeVerification.verifyBoolean(this.crop)) {
                throw TypeError('crop must be of type boolean');
            }
        }
    }
}
exports.DocumentClassifierConfiguration = DocumentClassifierConfiguration;
/** @hidden */
exports.DocumentClassifierStatusValues = [
    "SUCCESS",
    "FAILURE_SCANNING"
];
/** @hidden */
exports.DocumentTypeValues = [
    "UNKNOWN",
    "BILL_OF_LADING",
    "PAY_SHEET",
    "SCALE_TICKET",
    "TOLL_RECEIPT",
    "WASH_RECEIPT"
];
/**
Contains the result of running the document classifier.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getDocumentClassifierResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class DocumentClassifierResult {
    /** @param source {@displayType `DeepPartial<DocumentClassifierResult>`} */
    constructor(source = {}) {
        this._releasePromise = null;
        if (source.documentType !== undefined) {
            this.documentType = source.documentType;
        }
        else {
            throw new Error("documentType must be present in constructor argument");
        }
        if (source.confidence !== undefined) {
            this.confidence = source.confidence;
        }
        else {
            throw new Error("confidence must be present in constructor argument");
        }
        if (source.status !== undefined) {
            this.status = source.status;
        }
        else {
            throw new Error("status must be present in constructor argument");
        }
        if (source.documentScanningResult !== undefined) {
            this.documentScanningResult = new DocumentScannerTypes_1.DocumentScanningResult(source.documentScanningResult);
        }
        else {
            throw new Error("documentScanningResult must be present in constructor argument");
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            promises.push(this.documentScanningResult.release());
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.DocumentClassifierResult = DocumentClassifierResult;
