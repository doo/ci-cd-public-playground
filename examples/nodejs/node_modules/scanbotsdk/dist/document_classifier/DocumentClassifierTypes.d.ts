import "../support/Disposable";
import type { DeepPartial } from "../support/DeepPartial";
import { DocumentScanningResult } from "../document_scanner/DocumentScannerTypes";
/**
Configuration for the document classifier.
*/
export declare class DocumentClassifierConfiguration {
    /**
    If true, image will be scanned for a valid document first.
    
    Default is true
    */
    readonly crop: boolean;
    /** @param source {@displayType `DeepPartial<DocumentClassifierConfiguration>`} */
    constructor(source?: DeepPartial<DocumentClassifierConfiguration>);
}
/**
The status of the classification result.

- `SUCCESS`:
   The document was classified successfully.
- `FAILURE_SCANNING`:
   No document was detected.
*/
export type DocumentClassifierStatus = 
/**
The document was classified successfully.
*/
"SUCCESS" | 
/**
No document was detected.
*/
"FAILURE_SCANNING";
/** @hidden */
export declare const DocumentClassifierStatusValues: ReadonlyArray<DocumentClassifierStatus>;
/**
The type of the document.

- `UNKNOWN`:
   The document type is unknown.
- `BILL_OF_LADING`:
   Bill of lading.
- `PAY_SHEET`:
   Pay sheet.
- `SCALE_TICKET`:
   Scale ticket.
- `TOLL_RECEIPT`:
   Toll receipt.
- `WASH_RECEIPT`:
   Wash receipt.
*/
export type DocumentType = 
/**
The document type is unknown.
*/
"UNKNOWN" | 
/**
Bill of lading.
*/
"BILL_OF_LADING" | 
/**
Pay sheet.
*/
"PAY_SHEET" | 
/**
Scale ticket.
*/
"SCALE_TICKET" | 
/**
Toll receipt.
*/
"TOLL_RECEIPT" | 
/**
Wash receipt.
*/
"WASH_RECEIPT";
/** @hidden */
export declare const DocumentTypeValues: ReadonlyArray<DocumentType>;
/**
Contains the result of running the document classifier.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getDocumentClassifierResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
export declare class DocumentClassifierResult implements AsyncDisposable {
    /**
    The classification result.
    */
    readonly documentType: DocumentType;
    /**
    The confidence of the classification result.
    */
    readonly confidence: number;
    /**
    The status of the classification result.
    */
    readonly status: DocumentClassifierStatus;
    /**
    The result of the document scanning.
    */
    readonly documentScanningResult: DocumentScanningResult;
    /** @param source {@displayType `DeepPartial<DocumentClassifierResult>`} */
    constructor(source?: DeepPartial<DocumentClassifierResult>);
    private _releasePromise;
    release(): Promise<void>;
    [Symbol.asyncDispose](): Promise<void>;
}
