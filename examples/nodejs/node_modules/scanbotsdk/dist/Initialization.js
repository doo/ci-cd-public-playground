"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DeviceSession = exports.deregisterDevice = exports.getLicenseInfo = exports.initialize = exports.setLogging = void 0;
const LicensingTypes_1 = require("./licensing/LicensingTypes");
const TypeVerification = __importStar(require("./support/TypeVerification"));
require("./support/Disposable");
const Errors_1 = require("./support/Errors");
const ImageRef_1 = require("./image/ImageRef");
const addon = require('../native/scanbotsdk_nodejs.node');
/**
 Enables or disables logging. This function can be called even before calling {@link initialize}.
 @param enable - If true, logging is enabled. If false, logging is disabled.
 */
function setLogging(enable) {
    TypeVerification.assertType({ enable }, TypeVerification.verifyBoolean, 'boolean');
    addon.nativeSetLogging(enable);
}
exports.setLogging = setLogging;
/**
 Initialize the Scanbot SDK with the given license key.
 Must be called before using any other functions in the SDK.
 @param options - The initialization options.
 @returns A promise that resolves when the SDK is initialized.
 */
async function initialize({ licenseKey, writablePath = '.', autoreleaseRequired = true, numThreads = 1, onlineLicenseCheckCompletionTimeoutMs = 15000 }) {
    TypeVerification.assertType({ licenseKey }, TypeVerification.verifyString, 'string');
    TypeVerification.assertType({ writablePath }, TypeVerification.verifyString, 'string');
    TypeVerification.assertType({ numThreads }, TypeVerification.verifyInt, 'int');
    TypeVerification.assertType({ onlineLicenseCheckCompletionTimeoutMs }, TypeVerification.verifyInt, 'int');
    await addon.nativeInitialize(licenseKey, writablePath, numThreads, onlineLicenseCheckCompletionTimeoutMs).catch(Errors_1.remapError);
    (0, ImageRef_1.setAutoreleaseRequired)(autoreleaseRequired);
}
exports.initialize = initialize;
/**
 Current license information.
 @returns The current license info.
 */
async function getLicenseInfo() {
    let licenseInfo = await addon.nativeGetLicenseInfo().catch(Errors_1.remapError);
    return new LicensingTypes_1.LicenseInfo(licenseInfo);
}
exports.getLicenseInfo = getLicenseInfo;
/**
 Effectively notifies the server that the license is no longer used by this device.
 After calling the method SDK behaves like no license is available.
 You should call this method if you are using floating license to notify the server that the
 device is no longer using the license and so another device can use it.
 If you are using a floating license, but you do not call this method, the license will be
 released automatically after a period specified in the license.
 If during work with the SDK you initialize the SDK with multiple licenses, there is no need to
 call the function after finishing work with each of the licenses, only with the last one. For
 others, the function is implicitly called automatically.
 The function returns immediately, but the deregistration process may take some time.
 You can use {@link DeviceSession} helper class to use this method with 'await using' typescript syntax.
 */
function deregisterDevice(timeoutMs = 15000) {
    TypeVerification.assertType({ timeoutMs }, TypeVerification.verifyInt, 'int');
    return addon.nativeDeregisterDevice(timeoutMs).catch(Errors_1.remapError);
}
exports.deregisterDevice = deregisterDevice;
/**
 Convenience class to manage device usage for a floating license.
 */
class DeviceSession {
    /**
     Constructor to create a DeviceSession with a given close timeout.
     @param deviceDeregisterTimeoutMs - The timeout in milliseconds for device deregistration.
     */
    constructor(deviceDeregisterTimeoutMs = 15000) {
        TypeVerification.assertType({ deviceDeregisterTimeoutMs }, TypeVerification.verifyInt, 'int');
        this.deviceDeregisterTimeoutMs = deviceDeregisterTimeoutMs;
    }
    [Symbol.asyncDispose]() {
        return deregisterDevice(this.deviceDeregisterTimeoutMs);
    }
}
exports.DeviceSession = DeviceSession;
