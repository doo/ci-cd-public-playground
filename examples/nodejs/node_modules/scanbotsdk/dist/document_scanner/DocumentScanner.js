"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/DocumentScanner.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DocumentScanner = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const DocumentScannerTypes_1 = require("../document_scanner/DocumentScannerTypes");
const DocumentScannerTypes_2 = require("../document_scanner/DocumentScannerTypes");
const DocumentScannerTypes_3 = require("../document_scanner/DocumentScannerTypes");
const ImageRef_1 = require("../image/ImageRef");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for document detection and scanning.
 *
 * Required licence feature(s): DocumentScanner.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await DocumentScanner.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class DocumentScanner {
    /**
    Initialize the document detector.
    @param configuration - The configuration for the document detector.
    @returns Instance of DocumentScanner.
    */
    static async create(configuration = new DocumentScannerTypes_2.DocumentScannerConfiguration({})) {
        DocumentScanner.performStaticInitialization();
        if (!TypeVerification.typeVerifier(DocumentScannerTypes_2.DocumentScannerConfiguration)(configuration)) {
            throw TypeError('configuration must be of type DocumentScannerConfiguration');
        }
        const nativeHandle = await DocumentScanner.nativeDocumentScannerInit(configuration).catch(Errors_1.remapError);
        return new DocumentScanner(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeRun = addonNative.nativeDocumentScannerRun.bind(this);
        this.nativeScan = addonNative.nativeDocumentScannerScan.bind(this);
        this.nativeClearResult = addonNative.nativeDocumentScannerClearResult.bind(this);
        DocumentScanner.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            DocumentScanner.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = DocumentScanner.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return DocumentScanner.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Detect the document contour in the given image.
    @param image - The image to detect the document contour in.
    @returns The detection result.
    */
    async run(image) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        const partial = await this.nativeRun(image).catch(Errors_1.remapError);
        return new DocumentScannerTypes_1.DocumentDetectionResult(partial);
    }
    /**
    Detect the document contour in the given image and crop it.
    @param image - The image to detect the document contour in.
    @returns The scanning result.
    */
    async scan(image) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        const partial = await this.nativeScan(image).catch(Errors_1.remapError);
        return new DocumentScannerTypes_3.DocumentScanningResult(partial);
    }
    /**
    Clears the accumulated results. Should be called after a document has been detected to the customers
    * satisfaction to prevent a poisoned cache.
    *
    */
    async clearResult() {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        const partial = await this.nativeClearResult().catch(Errors_1.remapError);
        return partial;
    }
    static performStaticInitialization() {
        if (DocumentScanner.staticInitializationPerformed) {
            return;
        }
        DocumentScanner.staticInitializationPerformed = true;
        DocumentScanner.nativeDocumentScannerInit = addonNative.nativeDocumentScannerInit.bind(DocumentScanner);
        DocumentScanner.nativeDispose = addonNative.nativeDocumentScannerDispose;
    }
}
exports.DocumentScanner = DocumentScanner;
DocumentScanner.registry = new FinalizationRegistry((nativeHandle) => {
    DocumentScanner.nativeDispose(nativeHandle);
});
DocumentScanner.staticInitializationPerformed = false;
