"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/DocumentQualityAnalyzerTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DocumentQualityAnalyzerResult = exports.DocumentQualityAnalyzerConfiguration = exports.DocumentQualityThreshold = exports.DocumentQualityValues = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const ImageRef_1 = require("../image/ImageRef");
/** @hidden */
exports.DocumentQualityValues = [
    "VERY_POOR",
    "POOR",
    "REASONABLE",
    "GOOD",
    "EXCELLENT"
];
/**
Point in the quality-number of symbols space to separate quality levels.
*/
class DocumentQualityThreshold {
    /** @param source {@displayType `DeepPartial<DocumentQualityThreshold>`} */
    constructor(source = {}) {
        if (source.symbolQuality !== undefined) {
            this.symbolQuality = source.symbolQuality;
            if (!TypeVerification.verifyDouble(this.symbolQuality)) {
                throw TypeError('symbolQuality must be of type number');
            }
        }
        else {
            throw new Error("symbolQuality must be present in constructor argument");
        }
        if (source.symbolRatio !== undefined) {
            this.symbolRatio = source.symbolRatio;
            if (!TypeVerification.verifyDouble(this.symbolRatio)) {
                throw TypeError('symbolRatio must be of type number');
            }
        }
        else {
            throw new Error("symbolRatio must be present in constructor argument");
        }
    }
}
exports.DocumentQualityThreshold = DocumentQualityThreshold;
/**
Document Quality Analyzer configuration.
*/
class DocumentQualityAnalyzerConfiguration {
    /** @param source {@displayType `DeepPartial<DocumentQualityAnalyzerConfiguration>`} */
    constructor(source = {}) {
        /**
        Quality thresholds to separate quality levels.
        */
        this.qualityThresholds = [new DocumentQualityThreshold({ "symbolQuality": 0.5, "symbolRatio": 0.5 }), new DocumentQualityThreshold({ "symbolQuality": 0.7, "symbolRatio": 0.3 }), new DocumentQualityThreshold({ "symbolQuality": 0.85, "symbolRatio": 0.3 }), new DocumentQualityThreshold({ "symbolQuality": 0.9, "symbolRatio": 0.1 })];
        /**
        quality levels.
        */
        this.qualityIndices = ["VERY_POOR", "POOR", "REASONABLE", "GOOD", "EXCELLENT"];
        /**
        Enable orientation detection. Document orientation will be returned in `DocumentQualityAnalyzerResult.orientation`.
        
        Default is false
        */
        this.detectOrientation = false;
        /**
        Inspect small text (typically less than 12 pixels height) when analyzing the document quality.
        If true, processing will be slower, but when the quality issue with a document is the low resolution of the text,
        then the issue will be caught.
        
        Default is false
        */
        this.inspectSmallText = false;
        /**
        Maximum image size in pixels, if image is bigger, it will be resized.
        
        Default is 2000
        */
        this.maxImageSize = 2000;
        /**
        If estimated number of symbols is less than this value, return that document is not found.
        
        Default is 20
        */
        this.minEstimatedNumberOfSymbolsForDocument = 20;
        /**
        At least this fraction of the image will be processed, range is from 0 to 1.
        Decreasing this value below 1.0 can lead to faster processing but may result in
        less accurate prediction, as only a subset of the image tiles will be analyzed.
        
        Default is 1
        */
        this.minProcessedFraction = 1.0;
        /**
        At most this fraction of the image will be processed, range is from 0 to 1.
        Decreasing this value below 1.0 can lead to faster processing but may result in
        less accurate prediction, as only a subset of the image tiles will be analyzed.
        
        Default is 1
        */
        this.maxProcessedFraction = 1.0;
        /**
        If this number of symbols is found and minProcessedFraction of the image is processed, the processing stops.
        
        Default is 100
        */
        this.minNumberOfRequiredSymbols = 100;
        /**
        If detectOrientation is True, minNumberOfRequiredSymbols is found, and orientationConfidence is bigger than this threshold, the processing stops.
        If the maxProcessedFraction part of the image is processed and orientationConfidence is lower than the threshold, the returned orientation will be 0.
        
        Default is 0
        */
        this.minRequiredOrientationConfidence = 0.0;
        /**
        Image will be processed in tiles of this size; will be ignored if image is small.
        
        Default is 300
        */
        this.tileSize = 300;
        /**
        If true, will return the quality heatmap as a part of the result.
        In this case, the entire image will be processed and the minProcessedFraction will be ignored.
        Useful for debugging situations where the returned quality doesn't appear to match the perceived quality.
        
        Default is false
        */
        this.returnQualityHeatmap = false;
        if (source.qualityThresholds !== undefined) {
            this.qualityThresholds = source.qualityThresholds.map((it) => { return new DocumentQualityThreshold(it); });
            if (!TypeVerification.listVerifier(TypeVerification.typeVerifier(DocumentQualityThreshold))(this.qualityThresholds)) {
                throw TypeError('qualityThresholds must be of type DocumentQualityThreshold[]');
            }
        }
        if (source.qualityIndices !== undefined) {
            this.qualityIndices = source.qualityIndices.map((it) => { return it; });
            if (!TypeVerification.listVerifier(TypeVerification.enumVerifier(exports.DocumentQualityValues))(this.qualityIndices)) {
                throw TypeError('qualityIndices must be of type DocumentQuality[]');
            }
        }
        if (source.detectOrientation !== undefined) {
            this.detectOrientation = source.detectOrientation;
            if (!TypeVerification.verifyBoolean(this.detectOrientation)) {
                throw TypeError('detectOrientation must be of type boolean');
            }
        }
        if (source.inspectSmallText !== undefined) {
            this.inspectSmallText = source.inspectSmallText;
            if (!TypeVerification.verifyBoolean(this.inspectSmallText)) {
                throw TypeError('inspectSmallText must be of type boolean');
            }
        }
        if (source.maxImageSize !== undefined) {
            this.maxImageSize = source.maxImageSize;
            if (!TypeVerification.verifyInt(this.maxImageSize)) {
                throw TypeError('maxImageSize must be of type number');
            }
        }
        if (source.minEstimatedNumberOfSymbolsForDocument !== undefined) {
            this.minEstimatedNumberOfSymbolsForDocument = source.minEstimatedNumberOfSymbolsForDocument;
            if (!TypeVerification.verifyInt(this.minEstimatedNumberOfSymbolsForDocument)) {
                throw TypeError('minEstimatedNumberOfSymbolsForDocument must be of type number');
            }
        }
        if (source.minProcessedFraction !== undefined) {
            this.minProcessedFraction = source.minProcessedFraction;
            if (!TypeVerification.verifyDouble(this.minProcessedFraction)) {
                throw TypeError('minProcessedFraction must be of type number');
            }
        }
        if (source.maxProcessedFraction !== undefined) {
            this.maxProcessedFraction = source.maxProcessedFraction;
            if (!TypeVerification.verifyDouble(this.maxProcessedFraction)) {
                throw TypeError('maxProcessedFraction must be of type number');
            }
        }
        if (source.minNumberOfRequiredSymbols !== undefined) {
            this.minNumberOfRequiredSymbols = source.minNumberOfRequiredSymbols;
            if (!TypeVerification.verifyInt(this.minNumberOfRequiredSymbols)) {
                throw TypeError('minNumberOfRequiredSymbols must be of type number');
            }
        }
        if (source.minRequiredOrientationConfidence !== undefined) {
            this.minRequiredOrientationConfidence = source.minRequiredOrientationConfidence;
            if (!TypeVerification.verifyDouble(this.minRequiredOrientationConfidence)) {
                throw TypeError('minRequiredOrientationConfidence must be of type number');
            }
        }
        if (source.tileSize !== undefined) {
            this.tileSize = source.tileSize;
            if (!TypeVerification.verifyInt(this.tileSize)) {
                throw TypeError('tileSize must be of type number');
            }
        }
        if (source.returnQualityHeatmap !== undefined) {
            this.returnQualityHeatmap = source.returnQualityHeatmap;
            if (!TypeVerification.verifyBoolean(this.returnQualityHeatmap)) {
                throw TypeError('returnQualityHeatmap must be of type boolean');
            }
        }
    }
}
exports.DocumentQualityAnalyzerConfiguration = DocumentQualityAnalyzerConfiguration;
/**
Result of document quality analysis. It can be used to determine, e.g., if a document is good enough to be used for OCR processing.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getDocumentQualityAnalyzerResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class DocumentQualityAnalyzerResult {
    /** @param source {@displayType `DeepPartial<DocumentQualityAnalyzerResult>`} */
    constructor(source = {}) {
        /**
        Quality heatmap.
        Black marks areas of no detected text. Colors represent the quality of detected text,
        with red meaning the lowest quality and yellow and green meaning progressively higher quality.
        */
        this.qualityHeatmap = null;
        this._releasePromise = null;
        if (source.documentFound !== undefined) {
            this.documentFound = source.documentFound;
        }
        else {
            throw new Error("documentFound must be present in constructor argument");
        }
        if (source.quality !== undefined) {
            this.quality = source.quality != null ? (exports.DocumentQualityValues.includes(source.quality) ? source.quality : null) : null;
        }
        else {
            throw new Error("quality must be present in constructor argument");
        }
        if (source.orientation !== undefined) {
            this.orientation = source.orientation != null ? source.orientation : null;
        }
        else {
            throw new Error("orientation must be present in constructor argument");
        }
        if (source.orientationConfidence !== undefined) {
            this.orientationConfidence = source.orientationConfidence != null ? source.orientationConfidence : null;
        }
        else {
            throw new Error("orientationConfidence must be present in constructor argument");
        }
        if (source.cumulativeQualityHistogram !== undefined) {
            this.cumulativeQualityHistogram = source.cumulativeQualityHistogram.map((it) => { return it; });
        }
        else {
            throw new Error("cumulativeQualityHistogram must be present in constructor argument");
        }
        if (source.cumulativeContrastHistogram !== undefined) {
            this.cumulativeContrastHistogram = source.cumulativeContrastHistogram.map((it) => { return it; });
        }
        else {
            throw new Error("cumulativeContrastHistogram must be present in constructor argument");
        }
        if (source.cumulativeTextSizeHistogram !== undefined) {
            this.cumulativeTextSizeHistogram = source.cumulativeTextSizeHistogram.map((it) => { return it; });
        }
        else {
            throw new Error("cumulativeTextSizeHistogram must be present in constructor argument");
        }
        if (source.qualityHeatmap !== undefined) {
            this.qualityHeatmap = source.qualityHeatmap != null ? ImageRef_1.ImageRef.fromPartial(source.qualityHeatmap) : null;
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            {
                if (this.qualityHeatmap != null) {
                    promises.push(this.qualityHeatmap.release());
                }
            }
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.DocumentQualityAnalyzerResult = DocumentQualityAnalyzerResult;
