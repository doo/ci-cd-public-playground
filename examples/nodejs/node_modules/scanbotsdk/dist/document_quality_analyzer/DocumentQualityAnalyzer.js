"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/DocumentQualityAnalyzer.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DocumentQualityAnalyzer = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const DocumentQualityAnalyzerTypes_1 = require("../document_quality_analyzer/DocumentQualityAnalyzerTypes");
const DocumentQualityAnalyzerTypes_2 = require("../document_quality_analyzer/DocumentQualityAnalyzerTypes");
const ImageRef_1 = require("../image/ImageRef");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for analyzing document quality.
 *
 * Required licence feature(s): ImageProcessing.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await DocumentQualityAnalyzer.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class DocumentQualityAnalyzer {
    /**
    Initialize the document quality analyzer.
    @param configuration - The configuration to use for the document quality analyzer.
    @returns Instance of DocumentQualityAnalyzer.
    */
    static async create(configuration = new DocumentQualityAnalyzerTypes_1.DocumentQualityAnalyzerConfiguration({})) {
        DocumentQualityAnalyzer.performStaticInitialization();
        if (!TypeVerification.typeVerifier(DocumentQualityAnalyzerTypes_1.DocumentQualityAnalyzerConfiguration)(configuration)) {
            throw TypeError('configuration must be of type DocumentQualityAnalyzerConfiguration');
        }
        const nativeHandle = await DocumentQualityAnalyzer.nativeDocumentQualityAnalyzerInit(configuration).catch(Errors_1.remapError);
        return new DocumentQualityAnalyzer(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeRun = addonNative.nativeDocumentQualityAnalyzerRun.bind(this);
        DocumentQualityAnalyzer.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            DocumentQualityAnalyzer.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = DocumentQualityAnalyzer.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return DocumentQualityAnalyzer.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    analyze document quality of the given image.
    @param image - The image to analyze.
    @returns The analyzed document quality.
    */
    async run(image) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        const partial = await this.nativeRun(image).catch(Errors_1.remapError);
        return new DocumentQualityAnalyzerTypes_2.DocumentQualityAnalyzerResult(partial);
    }
    static performStaticInitialization() {
        if (DocumentQualityAnalyzer.staticInitializationPerformed) {
            return;
        }
        DocumentQualityAnalyzer.staticInitializationPerformed = true;
        DocumentQualityAnalyzer.nativeDocumentQualityAnalyzerInit = addonNative.nativeDocumentQualityAnalyzerInit.bind(DocumentQualityAnalyzer);
        DocumentQualityAnalyzer.nativeDispose = addonNative.nativeDocumentQualityAnalyzerDispose;
    }
}
exports.DocumentQualityAnalyzer = DocumentQualityAnalyzer;
DocumentQualityAnalyzer.registry = new FinalizationRegistry((nativeHandle) => {
    DocumentQualityAnalyzer.nativeDispose(nativeHandle);
});
DocumentQualityAnalyzer.staticInitializationPerformed = false;
