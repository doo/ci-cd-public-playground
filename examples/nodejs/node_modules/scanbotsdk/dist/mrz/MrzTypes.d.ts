import "../support/Disposable";
import type { DeepPartial } from "../support/DeepPartial";
import { AccumulatedResultsVerifierConfiguration } from "../frame_accumulation/FrameAccumulationTypes";
import { GenericDocument } from "../generic_document/GenericDocument";
/**
Type of document containing the MRZ.

- `UNKNOWN`:
   Undefined.
- `CREW_MEMBER_CERTIFICATE`:
   Crew member certificate.
- `ID_CARD`:
   ID card.
- `PASSPORT`:
   Passport.
- `VISA`:
   Visa card.
- `CH_DRIVING_LICENSE`:
   Swiss driver license.
*/
export type MrzDocumentType = 
/**
Undefined.
*/
"UNKNOWN" | 
/**
Crew member certificate.
*/
"CREW_MEMBER_CERTIFICATE" | 
/**
ID card.
*/
"ID_CARD" | 
/**
Passport.
*/
"PASSPORT" | 
/**
Visa card.
*/
"VISA" | 
/**
Swiss driver license.
*/
"CH_DRIVING_LICENSE";
/** @hidden */
export declare const MrzDocumentTypeValues: ReadonlyArray<MrzDocumentType>;
/**
Container for result of MRZ scanning attempt.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getMrzScannerResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
export declare class MrzScannerResult implements AsyncDisposable {
    /**
    Scanning successful.
    
    Default is false
    */
    readonly success: boolean;
    /**
    Raw string value of MRZ.
    */
    readonly rawMRZ: string;
    /**
    Generic document containing MRZ data.
    */
    readonly document: GenericDocument | null;
    /** @param source {@displayType `DeepPartial<MrzScannerResult>`} */
    constructor(source?: DeepPartial<MrzScannerResult>);
    private _releasePromise;
    release(): Promise<void>;
    [Symbol.asyncDispose](): Promise<void>;
}
/**
Defines how to handle incomplete MRZ results (e.g. caused by failed validation).

- `ACCEPT`:
   Accept incomplete results. Fields failing validation will have a validation status of INVALID. Typically used for single-shot scanning.
- `REJECT`:
   Reject incomplete results. If any fields are missing or fail validation, the result document will be empty. Typically used for live scanning.
*/
export type MrzIncompleteResultHandling = 
/**
Accept incomplete results. Fields failing validation will have a validation status of INVALID. Typically used for single-shot scanning.
*/
"ACCEPT" | 
/**
Reject incomplete results. If any fields are missing or fail validation, the result document will be empty. Typically used for live scanning.
*/
"REJECT";
/** @hidden */
export declare const MrzIncompleteResultHandlingValues: ReadonlyArray<MrzIncompleteResultHandling>;
/**
Configuration for MRZ scanner.
*/
export declare class MrzScannerConfiguration {
    /**
    Configure the frame accumulation process.
    */
    frameAccumulationConfiguration: AccumulatedResultsVerifierConfiguration;
    /**
    Enable MRZ detection. If disabled, the scanner skips the detection step and assumes that the input image is a crop of the MRZ area.
    
    Default is true
    */
    enableDetection: boolean;
    /**
    Defines how to handle incomplete MRZ results (e.g. caused by failed validation).
    
    Default is REJECT
    */
    incompleteResultHandling: MrzIncompleteResultHandling;
    /**
    If true, crops of the detected MRZ and its fields will be returned in the result.
    
    Default is false
    */
    returnCrops: boolean;
    /** @param source {@displayType `DeepPartial<MrzScannerConfiguration>`} */
    constructor(source?: DeepPartial<MrzScannerConfiguration>);
}
/**
Configuration for MRZ parser.
*/
export declare class MrzParserConfiguration {
    /**
    Defines how to handle incomplete MRZ results (e.g. caused by failed validation).
    
    Default is ACCEPT
    */
    incompleteResultHandling: MrzIncompleteResultHandling;
    /** @param source {@displayType `DeepPartial<MrzParserConfiguration>`} */
    constructor(source?: DeepPartial<MrzParserConfiguration>);
}
