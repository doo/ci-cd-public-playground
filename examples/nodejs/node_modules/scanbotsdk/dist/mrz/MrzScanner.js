"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/MRZScanner.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MrzParser = exports.MrzScanner = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const ImageRef_1 = require("../image/ImageRef");
const MrzTypes_1 = require("../mrz/MrzTypes");
const MrzTypes_2 = require("../mrz/MrzTypes");
const MrzTypes_3 = require("../mrz/MrzTypes");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for machine readable zone scanning.
 *
 * Required licence feature(s): MrzScanner.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await MrzScanner.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class MrzScanner {
    /**
    Initialize the MRZ scanner.
    @param configuration - The configuration to use for the scanner.
    @returns Instance of MrzScanner.
    */
    static async create(configuration = new MrzTypes_2.MrzScannerConfiguration({})) {
        MrzScanner.performStaticInitialization();
        if (!TypeVerification.typeVerifier(MrzTypes_2.MrzScannerConfiguration)(configuration)) {
            throw TypeError('configuration must be of type MrzScannerConfiguration');
        }
        const nativeHandle = await MrzScanner.nativeMrzScannerInit(configuration).catch(Errors_1.remapError);
        return new MrzScanner(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeRun = addonNative.nativeMrzScannerRun.bind(this);
        this.nativeClearResult = addonNative.nativeMrzScannerClearResult.bind(this);
        MrzScanner.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            MrzScanner.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = MrzScanner.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return MrzScanner.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Recognize the MRZ in the given image.
    @param image - The image to recognize.
    @returns The recognition result.
    */
    async run(image) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        const partial = await this.nativeRun(image).catch(Errors_1.remapError);
        return new MrzTypes_3.MrzScannerResult(partial);
    }
    /**
    Clears the accumulated results. Should be called after a document has been recognized to the customers
    * satisfaction to prevent a poisoned cache.
    *
    */
    async clearResult() {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        const partial = await this.nativeClearResult().catch(Errors_1.remapError);
        return partial;
    }
    static performStaticInitialization() {
        if (MrzScanner.staticInitializationPerformed) {
            return;
        }
        MrzScanner.staticInitializationPerformed = true;
        MrzScanner.nativeMrzScannerInit = addonNative.nativeMrzScannerInit.bind(MrzScanner);
        MrzScanner.nativeDispose = addonNative.nativeMrzScannerDispose;
    }
}
exports.MrzScanner = MrzScanner;
MrzScanner.registry = new FinalizationRegistry((nativeHandle) => {
    MrzScanner.nativeDispose(nativeHandle);
});
MrzScanner.staticInitializationPerformed = false;
/**
 API for machine readable zone parsing.
 *
 * Required licence feature(s): MrzScanner.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await MrzParser.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class MrzParser {
    /**
    Initialize the MRZ parser.
    @param configuration - The configuration to use for the parser.
    @returns Instance of MrzParser.
    */
    static async create(configuration = new MrzTypes_1.MrzParserConfiguration({})) {
        MrzParser.performStaticInitialization();
        if (!TypeVerification.typeVerifier(MrzTypes_1.MrzParserConfiguration)(configuration)) {
            throw TypeError('configuration must be of type MrzParserConfiguration');
        }
        const nativeHandle = await MrzParser.nativeMrzParserInit(configuration).catch(Errors_1.remapError);
        return new MrzParser(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeParse = addonNative.nativeMrzParserParse.bind(this);
        MrzParser.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            MrzParser.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = MrzParser.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return MrzParser.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Parse the MRZ string.
    @param text - The MRZ string to parse. Should be formatted with newlines.
    @returns The recognition result.
    */
    async parse(text) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.verifyString(text)) {
            throw TypeError('text must be of type string');
        }
        const partial = await this.nativeParse(text).catch(Errors_1.remapError);
        return new MrzTypes_3.MrzScannerResult(partial);
    }
    static performStaticInitialization() {
        if (MrzParser.staticInitializationPerformed) {
            return;
        }
        MrzParser.staticInitializationPerformed = true;
        MrzParser.nativeMrzParserInit = addonNative.nativeMrzParserInit.bind(MrzParser);
        MrzParser.nativeDispose = addonNative.nativeMrzParserDispose;
    }
}
exports.MrzParser = MrzParser;
MrzParser.registry = new FinalizationRegistry((nativeHandle) => {
    MrzParser.nativeDispose(nativeHandle);
});
MrzParser.staticInitializationPerformed = false;
