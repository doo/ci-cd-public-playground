"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/CheckScannerTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CheckScannerConfiguration = exports.CheckStandardValues = exports.CheckScanningResult = exports.CheckMagneticInkStripScanningStatusValues = exports.CheckDocumentDetectionModeValues = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const DocumentScannerTypes_1 = require("../document_scanner/DocumentScannerTypes");
const GenericDocument_1 = require("../generic_document/GenericDocument");
const ImageRef_1 = require("../image/ImageRef");
const CommonTypes_1 = require("../common/CommonTypes");
/** @hidden */
exports.CheckDocumentDetectionModeValues = [
    "DISABLED",
    "DETECT_DOCUMENT",
    "DETECT_AND_CROP_DOCUMENT"
];
/** @hidden */
exports.CheckMagneticInkStripScanningStatusValues = [
    "SUCCESS",
    "INCOMPLETE_VALIDATION",
    "ERROR_NOTHING_FOUND"
];
/**
The result of check scanning.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getCheckScanningResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class CheckScanningResult {
    /** @param source {@displayType `DeepPartial<CheckScanningResult>`} */
    constructor(source = {}) {
        /**
        Magnetic ink strip scanning status.
        
        Default is ERROR_NOTHING_FOUND
        */
        this.status = "ERROR_NOTHING_FOUND";
        /**
        Crop of the check if documentDetectionMode is set to DETECT_AND_CROP_DOCUMENT. Will be non-empty, only if check recognition succeeded.
        */
        this.croppedImage = null;
        this._releasePromise = null;
        if (source.status !== undefined) {
            this.status = source.status;
        }
        if (source.check !== undefined) {
            this.check = source.check != null ? new GenericDocument_1.GenericDocument(source.check) : null;
        }
        else {
            throw new Error("check must be present in constructor argument");
        }
        if (source.documentDetectionResult !== undefined) {
            this.documentDetectionResult = source.documentDetectionResult != null ? new DocumentScannerTypes_1.DocumentDetectionResult(source.documentDetectionResult) : null;
        }
        else {
            throw new Error("documentDetectionResult must be present in constructor argument");
        }
        if (source.croppedImage !== undefined) {
            this.croppedImage = source.croppedImage != null ? ImageRef_1.ImageRef.fromPartial(source.croppedImage) : null;
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            {
                if (this.check != null) {
                    promises.push(this.check.release());
                }
            }
            {
                if (this.croppedImage != null) {
                    promises.push(this.croppedImage.release());
                }
            }
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.CheckScanningResult = CheckScanningResult;
/** @hidden */
exports.CheckStandardValues = [
    "USA",
    "FRA",
    "KWT",
    "AUS",
    "IND",
    "ISR",
    "UAE",
    "CAN"
];
/**
Configuration of the check scanner.
*/
class CheckScannerConfiguration {
    /** @param source {@displayType `DeepPartial<CheckScannerConfiguration>`} */
    constructor(source = {}) {
        /**
        Document detection to be performed in addition to scanning the machine-readable data in the check.
        
        By default only the machine-readable data is extracted during check scanning. Optionally, the coordinates and a crop of the entire check document can be returned, in addition to the check data.
        A check scan result may still be successful even if the whole document is not visible in the input image and the complete document could not be located.
        
        If cropping is enabled, check recognition will be performed on the cropped image of the check, which may improve recognition results.
        
        Default is DISABLED
        */
        this.documentDetectionMode = "DISABLED";
        /**
        Accepted check standards. Optional, by default - all checks are accepted.
        With an empty list no filter is applied and all checks are accepted.
        */
        this.acceptedCheckStandards = [];
        /**
        Scanners typically can produce better results from a single image if they are allowed to spend a longer time analyzing it.
        
        On the other hand, scanners can produce even better results if they can analyze multiple images of the same subject and cross-check and combine the results.
        The processing mode tells the scanner whether to optimize for single or multiple images of a subject.
        
        Default is AUTO
        */
        this.processingMode = "AUTO";
        if (source.documentDetectionMode !== undefined) {
            this.documentDetectionMode = source.documentDetectionMode;
            if (!TypeVerification.enumVerifier(exports.CheckDocumentDetectionModeValues)(this.documentDetectionMode)) {
                throw TypeError('documentDetectionMode must be of type CheckDocumentDetectionMode');
            }
        }
        if (source.acceptedCheckStandards !== undefined) {
            this.acceptedCheckStandards = source.acceptedCheckStandards.map((it) => { return it; });
            if (!TypeVerification.listVerifier(TypeVerification.enumVerifier(exports.CheckStandardValues))(this.acceptedCheckStandards)) {
                throw TypeError('acceptedCheckStandards must be of type CheckStandard[]');
            }
        }
        if (source.processingMode !== undefined) {
            this.processingMode = source.processingMode;
            if (!TypeVerification.enumVerifier(CommonTypes_1.ProcessingModeValues)(this.processingMode)) {
                throw TypeError('processingMode must be of type ProcessingMode');
            }
        }
    }
}
exports.CheckScannerConfiguration = CheckScannerConfiguration;
