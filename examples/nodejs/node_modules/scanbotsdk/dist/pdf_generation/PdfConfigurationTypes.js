"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/PDFConfigurationTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PdfConfiguration = exports.ResamplingMethodValues = exports.PageFitValues = exports.PageDirectionValues = exports.PageSizeValues = exports.PdfAttributes = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const ParametricFilters_1 = require("../image_processing/ParametricFilters");
const ParametricFilters_2 = require("../image_processing/ParametricFilters");
const ParametricFilters_3 = require("../image_processing/ParametricFilters");
const ParametricFilters_4 = require("../image_processing/ParametricFilters");
const ParametricFilters_5 = require("../image_processing/ParametricFilters");
const ParametricFilters_6 = require("../image_processing/ParametricFilters");
const ParametricFilters_7 = require("../image_processing/ParametricFilters");
const ParametricFilters_8 = require("../image_processing/ParametricFilters");
/**
PDF attributes.
*/
class PdfAttributes {
    /** @param source {@displayType `DeepPartial<PdfAttributes>`} */
    constructor(source = {}) {
        /**
        Author.
        
        Default is "Created with Scanbot SDK"
        */
        this.author = "Created with Scanbot SDK";
        /**
        Creator.
        
        Default is ""
        */
        this.creator = "";
        /**
        Title.
        
        Default is ""
        */
        this.title = "";
        /**
        Subject.
        
        Default is ""
        */
        this.subject = "";
        /**
        Keywords.
        
        Default is ""
        */
        this.keywords = "";
        if (source.author !== undefined) {
            this.author = source.author;
            if (!TypeVerification.verifyString(this.author)) {
                throw TypeError('author must be of type string');
            }
        }
        if (source.creator !== undefined) {
            this.creator = source.creator;
            if (!TypeVerification.verifyString(this.creator)) {
                throw TypeError('creator must be of type string');
            }
        }
        if (source.title !== undefined) {
            this.title = source.title;
            if (!TypeVerification.verifyString(this.title)) {
                throw TypeError('title must be of type string');
            }
        }
        if (source.subject !== undefined) {
            this.subject = source.subject;
            if (!TypeVerification.verifyString(this.subject)) {
                throw TypeError('subject must be of type string');
            }
        }
        if (source.keywords !== undefined) {
            this.keywords = source.keywords;
            if (!TypeVerification.verifyString(this.keywords)) {
                throw TypeError('keywords must be of type string');
            }
        }
    }
}
exports.PdfAttributes = PdfAttributes;
/** @hidden */
exports.PageSizeValues = [
    "LETTER",
    "LEGAL",
    "A3",
    "A4",
    "A5",
    "B4",
    "B5",
    "EXECUTIVE",
    "US4x6",
    "US4x8",
    "US5x7",
    "COMM10",
    "CUSTOM"
];
/** @hidden */
exports.PageDirectionValues = [
    "PORTRAIT",
    "LANDSCAPE",
    "AUTO"
];
/** @hidden */
exports.PageFitValues = [
    "FIT_IN",
    "FILL_IN",
    "STRETCH",
    "NONE"
];
/** @hidden */
exports.ResamplingMethodValues = [
    "NONE",
    "NEAREST",
    "LINEAR",
    "CUBIC",
    "LANCZOS4",
    "AREA"
];
/**
The parameters `pageSize`, `pageFit`, `dpi` and `resamplingMethod` interact in a complex way
when adding bitmap images (JPEG, PNG, or raw) to the PDF.
There are three cases to consider:
1. `pageSize` is `CUSTOM`:
   in this case `dpi` is used as the conversion ratio to calculate both the physical page and
   image sizes in inches from the bitmap dimensions. `resamplingMethod` is ignored.
2. `pageSize` is not `CUSTOM` AND `pageFit` is `NONE`:
   in this case `dpi` is used as the conversion ratio to calculate only the physical image
   size in inches from the bitmap dimensions. The image is centered onto the page.
   `resamplingMethod` is ignored.
3. `pageSize` is not `CUSTOM` AND `pageFit` is not `NONE`:
   in this case the page has a pre-determined physical size and we want to stretch the image to
   fit the page in one of the ways described by `pageFit`. The image DPI is automatically
   calculated for each image from the given physical page size, page fit setting and image
   dimensions. There are two sub-cases to consider:
   a. `resamplingMethod` is NONE OR the calculated DPI is less than or equal to `dpi`:
      in this case the image is added to the PDF without resampling, which means that the image
      rectangle is simply rescaled to fit into the page as specified by `pageFit` and
      `pageSize`.
   b. `resamplingMethod` is not NONE AND the calculated DPI is greater than `dpi`:
      in this case the bitmap of the image is resampled to match `dpi` using the selected method
      before adding it to the PDF. This will result in a bitmap that is smaller than the
      original image and hence, the resulting PDF will be smaller in size.
*/
class PdfConfiguration {
    /** @param source {@displayType `DeepPartial<PdfConfiguration>`} */
    constructor(source = {}) {
        /**
        Attributes.
        */
        this.attributes = new PdfAttributes({});
        /**
        Physical size of the page. If CUSTOM, the page size will be set as the image size at given dpi.
        
        Default is A4
        */
        this.pageSize = "A4";
        /**
        Page direction.
        
        Default is PORTRAIT
        */
        this.pageDirection = "PORTRAIT";
        /**
        How to fit the image into the page. Only used if `pageSize` is not `CUSTOM`.
        The image can either be stretched to fill the page,
        in one of three ways, or centered on the page without stretching.
        
        Default is FIT_IN
        */
        this.pageFit = "FIT_IN";
        /**
        The `dpi` parameter has two different meanings depending on the value of `pageSize` and
        `pageFit`. If pageSize is CUSTOM or pageFit is NONE, then `dpi` is the conversion ratio used
        to convert from units of pixels to physical inches when adding bitmap images (JPEG, PNG, or
        raw) to the PDF. Otherwise, if `resamplingMethod` is not NONE, then the image is downscaled
        if necessary (if the image after being fit to the page has a higher calculated DPI than
        `dpi`) to match `dpi` before adding it to the PDF. Otherwise, the setting is ignored and the
        calculated image DPI is used instead.
        
        Default is 72
        */
        this.dpi = 72;
        /**
        JPEG quality for images.
        Applies if an image is added as a cv::Mat and therefore needs to be encoded.
        Also applies if `resamplingMethod` not NONE and the image being added needs to be
        downscaled. Otherwise, when adding JPEG files to the PDF, the files are copied directly into
        the PDF and not re-encoded, which is many times faster and also preserves the quality of the
        original.
        
        Default is 80
        */
        this.jpegQuality = 80;
        /**
        Resampling method.
        
        Default is NONE
        */
        this.resamplingMethod = "NONE";
        /**
        Filter to apply to the input image when adding pages with binarization.
        */
        this.binarizationFilter = null;
        if (source.attributes !== undefined) {
            this.attributes = new PdfAttributes(source.attributes);
            if (!TypeVerification.typeVerifier(PdfAttributes)(this.attributes)) {
                throw TypeError('attributes must be of type PdfAttributes');
            }
        }
        if (source.pageSize !== undefined) {
            this.pageSize = source.pageSize;
            if (!TypeVerification.enumVerifier(exports.PageSizeValues)(this.pageSize)) {
                throw TypeError('pageSize must be of type PageSize');
            }
        }
        if (source.pageDirection !== undefined) {
            this.pageDirection = source.pageDirection;
            if (!TypeVerification.enumVerifier(exports.PageDirectionValues)(this.pageDirection)) {
                throw TypeError('pageDirection must be of type PageDirection');
            }
        }
        if (source.pageFit !== undefined) {
            this.pageFit = source.pageFit;
            if (!TypeVerification.enumVerifier(exports.PageFitValues)(this.pageFit)) {
                throw TypeError('pageFit must be of type PageFit');
            }
        }
        if (source.dpi !== undefined) {
            this.dpi = source.dpi;
            if (!TypeVerification.verifyInt(this.dpi)) {
                throw TypeError('dpi must be of type number');
            }
        }
        if (source.jpegQuality !== undefined) {
            this.jpegQuality = source.jpegQuality;
            if (!TypeVerification.verifyInt(this.jpegQuality)) {
                throw TypeError('jpegQuality must be of type number');
            }
        }
        if (source.resamplingMethod !== undefined) {
            this.resamplingMethod = source.resamplingMethod;
            if (!TypeVerification.enumVerifier(exports.ResamplingMethodValues)(this.resamplingMethod)) {
                throw TypeError('resamplingMethod must be of type ResamplingMethod');
            }
        }
        if (source.binarizationFilter !== undefined) {
            this.binarizationFilter = source.binarizationFilter != null ? ParametricFilters_6.ParametricFilter.from(source.binarizationFilter) : null;
            if (!TypeVerification.optionalVerifier(TypeVerification.polymorphicTypeVerifier([ParametricFilters_7.ScanbotBinarizationFilter, ParametricFilters_4.CustomBinarizationFilter, ParametricFilters_2.ColorDocumentFilter, ParametricFilters_1.BrightnessFilter, ParametricFilters_3.ContrastFilter, ParametricFilters_5.GrayscaleFilter, ParametricFilters_8.WhiteBlackPointFilter]))(this.binarizationFilter)) {
                throw TypeError('binarizationFilter must be of type ParametricFilter | null');
            }
        }
    }
}
exports.PdfConfiguration = PdfConfiguration;
