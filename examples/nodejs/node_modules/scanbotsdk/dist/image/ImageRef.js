"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ImageRef = exports.autorelease = exports.setAutoreleaseRequired = exports.AutoReleasable = void 0;
const addon = require('../../native/scanbotsdk_nodejs.node');
const ImageRefTypes_1 = require("./ImageRefTypes");
const TypeVerification = __importStar(require("../support/TypeVerification"));
require("../support/Disposable");
const Errors_1 = require("../support/Errors");
/**
 Base class for all objects whose lifetime is managed by autorelease pools created with the {@link autorelease} function.
 */
class AutoReleasable {
    constructor() {
        this.retained = false;
    }
    registerForAutoReleasing(uniqueId) {
        if (uniqueId != null) {
            if (AutoReleasePool.globalPool !== null) {
                AutoReleasePool.globalPool.addObject(this);
                return;
            }
            if (AutoReleasePool.autoreleaseRequired) {
                const errorMessage = `An object of an AutoReleasable type ${this.constructor.name} was created outside of an autorelease pool. ` +
                    'Unless autorelease pools are disabled, operations that create AutoReleasable objects ' +
                    'must be wrapped in an autorelease pool. For example:' +
                    'autorelease(async () => {\n' +
                    '   const result = barcodeScanner.run(image);\n' +
                    '});';
                throw new Error(errorMessage);
            }
        }
    }
    /**
     * Checks if this object is retained.
     */
    isRetained() {
        return this.retained;
    }
    /**
     Marks this object as retained, meaning that it will not be automatically released when exit from {@link autorelease} function occurs.
     */
    retain() {
        this.retained = true;
    }
}
exports.AutoReleasable = AutoReleasable;
class AutoReleasePool {
    constructor() {
        this.poolObjects = [];
    }
    async releaseAll() {
        const poolObjects = this.poolObjects;
        this.poolObjects = [];
        const releasePromises = [];
        for (const weakObj of poolObjects) {
            const obj = weakObj.deref();
            if (obj && !obj.isRetained()) {
                releasePromises.push(obj.release());
            }
        }
        await Promise.all(releasePromises);
    }
    addObject(obj) {
        this.poolObjects.push(new WeakRef(obj));
    }
}
AutoReleasePool.autoreleaseRequired = true;
AutoReleasePool.globalPool = null;
AutoReleasePool.globalPoolReferences = 0;
/**
 Sets whether all objects that implement {@link AutoReleasable} interface (e.g. instances of {@link ImageRef}) are required to be created inside an {@link autorelease} pool.
 By default, this is true which means that unless the object is created inside {@link autorelease} function, it will throw an error when trying to create it.
 We recommend setting this to false only if you are going to use 'await using' syntax when working with objects that contain {@link ImageRef} instances.
 Otherwise, if you don't manually call release, such object native resources will not be released until the object is garbage collected.
 * @param required - If true, all objects that implement {@link AutoReleasable} interface must be created inside {@link autorelease} function.
 */
function setAutoreleaseRequired(required) {
    TypeVerification.assertType({ required }, TypeVerification.verifyBoolean, 'boolean');
    AutoReleasePool.autoreleaseRequired = required;
}
exports.setAutoreleaseRequired = setAutoreleaseRequired;
/**
 * Create an autorelease pool, execute the given computation in it and release the pool.
 * All objects that were created during the computation that derive from {@link AutoReleasable}
 * (e.g. instances  {@link ImageRef}) and were not retained with a call to `.retain()` are released
 * when the autorelease pool is released at the end of the computation. If autorelease pools
 * are nested, then the objects are released when the outermost autorelease pool is released.
 *
 * AutoReleasable objects require an autorelease pool when created unless the
 * requirement has been disabled with a call to {@link setAutoreleaseRequired}.
 * @param computation - The computation to execute.
 * @returns A promise that resolves to the result of the computation.
 */
async function autorelease(computation) {
    if (AutoReleasePool.globalPool === null) {
        AutoReleasePool.globalPool = new AutoReleasePool();
    }
    AutoReleasePool.globalPoolReferences++;
    const releasePoolRef = async () => {
        AutoReleasePool.globalPoolReferences--;
        if (AutoReleasePool.globalPoolReferences === 0) {
            await AutoReleasePool.globalPool.releaseAll();
            AutoReleasePool.globalPool = null;
        }
    };
    try {
        return await computation();
    }
    finally {
        await releasePoolRef();
    }
}
exports.autorelease = autorelease;
/**
 Represent an image that is backed by native resources and can be created from various sources.
 @example
 Since the instance of the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
 ```
 await using instance = await ImageRef.fromPath('path/to/image.jpg');
 ```
 Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class ImageRef extends AutoReleasable {
    constructor(uniqueId) {
        super();
        this.releasePromise = null;
        this.nativeSaveImage = addon.nativeImageRefSaveImage.bind(this);
        this.nativeEncodeImage = addon.nativeImageRefEncodeImage.bind(this);
        this.nativeImageInfo = addon.nativeImageRefImageInfo.bind(this);
        ImageRef.initializeStaticMethods();
        this.uniqueId = uniqueId;
        if (this.uniqueId !== null) {
            this.registerForAutoReleasing(uniqueId);
            ImageRef.registry.register(this, this.uniqueId, this);
        }
    }
    /** @hidden */
    static fromPartial(source) {
        if (source instanceof ImageRef) {
            return source;
        }
        if ((source.uniqueId === undefined) || !TypeVerification.optionalVerifier(TypeVerification.verifyString)(source.uniqueId)) {
            throw new Error("When creating ImageRef from partial, uniqueId must be a string");
        }
        return new ImageRef(source.uniqueId);
    }
    /**
     * Creates an image from the given path with given options.
     * @param path - Path to the image file.
     * @param options - Options used during image loading
     * @returns Loaded image reference.
     */
    static async fromPath(path, options = new ImageRefTypes_1.PathImageLoadOptions()) {
        ImageRef.initializeStaticMethods();
        TypeVerification.assertType({ path }, TypeVerification.verifyString, 'string');
        TypeVerification.assertType({ options }, TypeVerification.typeVerifier(ImageRefTypes_1.PathImageLoadOptions), 'PathImageLoadOptions');
        return ImageRef.fromPartial(await ImageRef.nativeFromPath(path, options).catch(Errors_1.remapError));
    }
    /** Creates an image from the given encoded buffer e.g. from jpeg with given options.
     * @param buffer - Encoded image data as a Uint8Array.
     * @param options - Options used during image loading.
     * @returns Loaded image reference.
     */
    static async fromEncodedBuffer(buffer, options = new ImageRefTypes_1.BufferImageLoadOptions()) {
        ImageRef.initializeStaticMethods();
        TypeVerification.assertType({ buffer }, TypeVerification.verifyByteArray, 'Uint8Array');
        TypeVerification.assertType({ options }, TypeVerification.typeVerifier(ImageRefTypes_1.BufferImageLoadOptions), 'BufferImageLoadOptions');
        return ImageRef.fromPartial(await ImageRef.nativeFromEncodedBuffer(buffer, options).catch(Errors_1.remapError));
    }
    /**
     * Releases all alive images despite any existing references.
     */
    static releaseAllImages() {
        ImageRef.initializeStaticMethods();
        return ImageRef.nativeImageRefReleaseAllImages();
    }
    /**
     * Saves the image to the given path with given options.
     * @param path - Path to save the image to.
     * @param options - Options used during image saving.
     */
    saveImage(path, options = new ImageRefTypes_1.SaveImageOptions()) {
        TypeVerification.assertType({ path }, TypeVerification.verifyString, 'string');
        TypeVerification.assertType({ options }, TypeVerification.typeVerifier(ImageRefTypes_1.SaveImageOptions), 'SaveImageOptions');
        return this.nativeSaveImage(path, options).catch(Errors_1.remapError);
    }
    /**
     * Encodes the image with given options.
     * @param options - Options used during image encoding.
     * @returns Encoded image data as a Uint8Array.
     */
    encodeImage(options = new ImageRefTypes_1.EncodeImageOptions()) {
        TypeVerification.assertType({ options }, TypeVerification.typeVerifier(ImageRefTypes_1.EncodeImageOptions), 'EncodeImageOptions');
        return this.nativeEncodeImage(options).catch(Errors_1.remapError);
    }
    /**
     * Returns the image information.
     * @returns image information
     */
    imageInfo() {
        return this.nativeImageInfo().catch(Errors_1.remapError);
    }
    /**
     * Releases the native resources associated with this image.
     */
    release() {
        if (this.releasePromise === null) {
            const uniqueId = this.uniqueId;
            this.uniqueId = null;
            ImageRef.registry.unregister(this);
            this.releasePromise = ImageRef.nativeImageRefRelease(uniqueId).catch(Errors_1.remapError);
        }
        return this.releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
    static initializeStaticMethods() {
        if (ImageRef.staticMethodsInitialized) {
            return;
        }
        ImageRef.nativeFromPath = addon.nativeImageRefFromPath.bind(ImageRef);
        ImageRef.nativeFromEncodedBuffer = addon.nativeImageRefFromEncodedBuffer.bind(ImageRef);
        ImageRef.nativeImageRefRelease = addon.nativeImageRefRelease.bind(ImageRef);
        ImageRef.nativeImageRefReleaseAllImages = addon.nativeImageRefReleaseAllImages.bind(ImageRef);
    }
}
exports.ImageRef = ImageRef;
ImageRef.registry = new FinalizationRegistry((uniqueId) => {
    addon.nativeImageRefRelease(uniqueId);
});
ImageRef.staticMethodsInitialized = false;
