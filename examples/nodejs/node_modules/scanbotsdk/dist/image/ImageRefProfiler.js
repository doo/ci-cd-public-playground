"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/ImageRefProfiler.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ImageRefProfiler = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const ImageRefTypes_1 = require("../image/ImageRefTypes");
const ImageRefTypes_2 = require("../image/ImageRefTypes");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 ImageRef memory profiler.
 *
 * ImageRefs are reference-counted. If references aren't correctly released for whatever reason, the image memory will leak. Leaking memory over time may cause out-of-memory crashes and must be avoided. The profiler can return basic and detailed information about all currently alive ImageRefs, that can be useful to track leaking ImageRefs or to confirm that there are no leaks.
 */
class ImageRefProfiler {
    /**
    Returns a snapshot of all alive ImageRefs. The snapshot contains a list of ImageRefs with information such as in-memory size. If detailed profiling is enabled (by calling enableDetailedProfiling beforehand), the profile will contain additional information such as the source from which each ImageRef has been created.
    @returns The snapshot of all alive ImageRefs.
    */
    static async makeSnapshot() {
        ImageRefProfiler.performStaticInitialization();
        const partial = await this.nativeMakeSnapshot().catch(Errors_1.remapError);
        return new ImageRefTypes_2.ImageRefPoolSnapshot(partial);
    }
    /**
    After the function is called, ImageRefs will store additional information, such as source from which they were created. This information may be important when trying to understand a profile. This function must be called BEFORE the first ImageRef is created, otherwise details may be lost.
    */
    static async enableDetailedProfiling() {
        ImageRefProfiler.performStaticInitialization();
        const partial = await this.nativeEnableDetailedProfiling().catch(Errors_1.remapError);
        return partial;
    }
    /**
    Computes difference between two snapshots. To confirm that a particular operation doesn't leak, take a snapshot before and after the operation, and check that the diff doesn't contain any unexpected new ImageRefs.
    @param firstSnapshot - Snapshot that was taken earlier
    @param secondSnapshot - Snapshot that was taken later
    @returns Difference between two snapshots
    */
    static async diff(firstSnapshot, secondSnapshot) {
        ImageRefProfiler.performStaticInitialization();
        if (!TypeVerification.typeVerifier(ImageRefTypes_2.ImageRefPoolSnapshot)(firstSnapshot)) {
            throw TypeError('firstSnapshot must be of type ImageRefPoolSnapshot');
        }
        if (!TypeVerification.typeVerifier(ImageRefTypes_2.ImageRefPoolSnapshot)(secondSnapshot)) {
            throw TypeError('secondSnapshot must be of type ImageRefPoolSnapshot');
        }
        const partial = await this.nativeDiff(firstSnapshot, secondSnapshot).catch(Errors_1.remapError);
        return new ImageRefTypes_1.ImageRefPoolSnapshotsDiff(partial);
    }
    static performStaticInitialization() {
        if (ImageRefProfiler.staticInitializationPerformed) {
            return;
        }
        ImageRefProfiler.staticInitializationPerformed = true;
        ImageRefProfiler.nativeMakeSnapshot = addonNative.nativeImageRefProfilerMakeSnapshot.bind(ImageRefProfiler);
        ImageRefProfiler.nativeEnableDetailedProfiling = addonNative.nativeImageRefProfilerEnableDetailedProfiling.bind(ImageRefProfiler);
        ImageRefProfiler.nativeDiff = addonNative.nativeImageRefProfilerDiff.bind(ImageRefProfiler);
    }
}
exports.ImageRefProfiler = ImageRefProfiler;
ImageRefProfiler.staticInitializationPerformed = false;
