"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/TextPatternScannerTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TextPatternScannerConfiguration = exports.PatternContentValidator = exports.DefaultContentValidator = exports.ContentValidator = exports.TextPatternScannerResult = exports.SymbolBox = exports.WordBox = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
/**
Structure containing recognized word text and bounds.
*/
class WordBox {
    /** @param source {@displayType `DeepPartial<WordBox>`} */
    constructor(source = {}) {
        /**
        Confidence of the recognition.
        
        Default is 0.0
        */
        this.recognitionConfidence = 0.0;
        if (source.text !== undefined) {
            this.text = source.text;
        }
        else {
            throw new Error("text must be present in constructor argument");
        }
        if (source.boundingRect !== undefined) {
            this.boundingRect = { "x": source.boundingRect.x, "y": source.boundingRect.y, "width": source.boundingRect.width, "height": source.boundingRect.height };
        }
        else {
            throw new Error("boundingRect must be present in constructor argument");
        }
        if (source.recognitionConfidence !== undefined) {
            this.recognitionConfidence = source.recognitionConfidence;
        }
    }
}
exports.WordBox = WordBox;
/**
Structure containing recognized symbol text and bounds.
*/
class SymbolBox {
    /** @param source {@displayType `DeepPartial<SymbolBox>`} */
    constructor(source = {}) {
        if (source.symbol !== undefined) {
            this.symbol = source.symbol;
        }
        else {
            throw new Error("symbol must be present in constructor argument");
        }
        if (source.boundingRect !== undefined) {
            this.boundingRect = { "x": source.boundingRect.x, "y": source.boundingRect.y, "width": source.boundingRect.width, "height": source.boundingRect.height };
        }
        else {
            throw new Error("boundingRect must be present in constructor argument");
        }
        if (source.recognitionConfidence !== undefined) {
            this.recognitionConfidence = source.recognitionConfidence;
        }
        else {
            throw new Error("recognitionConfidence must be present in constructor argument");
        }
    }
}
exports.SymbolBox = SymbolBox;
/**
The result of the text line recognition.
*/
class TextPatternScannerResult {
    /** @param source {@displayType `DeepPartial<TextPatternScannerResult>`} */
    constructor(source = {}) {
        /**
        Confidence of the recognition.
        
        Default is 0.0
        */
        this.confidence = 0.0;
        /**
        Whether the validation was successful.
        
        Default is false
        */
        this.validationSuccessful = false;
        if (source.rawText !== undefined) {
            this.rawText = source.rawText;
        }
        else {
            throw new Error("rawText must be present in constructor argument");
        }
        if (source.wordBoxes !== undefined) {
            this.wordBoxes = source.wordBoxes.map((it) => { return new WordBox(it); });
        }
        else {
            throw new Error("wordBoxes must be present in constructor argument");
        }
        if (source.symbolBoxes !== undefined) {
            this.symbolBoxes = source.symbolBoxes.map((it) => { return new SymbolBox(it); });
        }
        else {
            throw new Error("symbolBoxes must be present in constructor argument");
        }
        if (source.confidence !== undefined) {
            this.confidence = source.confidence;
        }
        if (source.validationSuccessful !== undefined) {
            this.validationSuccessful = source.validationSuccessful;
        }
    }
}
exports.TextPatternScannerResult = TextPatternScannerResult;
/** @internal */
var ContentValidator;
(function (ContentValidator) {
    /** @internal */
    function from(source) {
        const _type = source["_type"];
        switch (_type) {
            case "DefaultContentValidator": return new DefaultContentValidator(source);
            case "PatternContentValidator": return new PatternContentValidator(source);
            default: throw new Error(`Unknown child class name: ${_type}`);
        }
    }
    ContentValidator.from = from;
})(ContentValidator || (exports.ContentValidator = ContentValidator = {}));
/**
Default content validator. Accepts only non-empty strings.
*/
class DefaultContentValidator {
    /** @param source {@displayType `DeepPartial<DefaultContentValidator>`} */
    constructor(source = {}) {
        this._type = "DefaultContentValidator";
        /**
        OCR whitelist. Empty string means no restriction.
        
        Default is ""
        */
        this.allowedCharacters = "";
        if (source.allowedCharacters !== undefined) {
            this.allowedCharacters = source.allowedCharacters;
            if (!TypeVerification.verifyString(this.allowedCharacters)) {
                throw TypeError('allowedCharacters must be of type string');
            }
        }
    }
}
exports.DefaultContentValidator = DefaultContentValidator;
/**
Pattern content validator.
*/
class PatternContentValidator {
    /** @param source {@displayType `DeepPartial<PatternContentValidator>`} */
    constructor(source = {}) {
        this._type = "PatternContentValidator";
        /**
        OCR whitelist. Empty string means no restriction.
        
        Default is ""
        */
        this.allowedCharacters = "";
        /**
        Whether the pattern should match the whole string or just a substring.
        
        Default is false
        */
        this.matchSubstring = false;
        if (source.allowedCharacters !== undefined) {
            this.allowedCharacters = source.allowedCharacters;
            if (!TypeVerification.verifyString(this.allowedCharacters)) {
                throw TypeError('allowedCharacters must be of type string');
            }
        }
        if (source.pattern !== undefined) {
            this.pattern = source.pattern;
            if (!TypeVerification.verifyString(this.pattern)) {
                throw TypeError('pattern must be of type string');
            }
        }
        else {
            throw new Error("pattern must be present in constructor argument");
        }
        if (source.matchSubstring !== undefined) {
            this.matchSubstring = source.matchSubstring;
            if (!TypeVerification.verifyBoolean(this.matchSubstring)) {
                throw TypeError('matchSubstring must be of type boolean');
            }
        }
    }
}
exports.PatternContentValidator = PatternContentValidator;
/**
Configuration for the text pattern scanner.
*/
class TextPatternScannerConfiguration {
    /** @param source {@displayType `DeepPartial<TextPatternScannerConfiguration>`} */
    constructor(source = {}) {
        /**
        Maximum image size (height or width) for OCR process. 0 - do not rescale.
        
        Default is 0
        */
        this.ocrResolutionLimit = 0;
        /**
        Maximum number of accumulated frames to inspect before actual result is returned.
        
        Default is 3
        */
        this.maximumNumberOfAccumulatedFrames = 3;
        /**
        Minimum number of accumulated frames that have equal result.
        
        Default is 2
        */
        this.minimumNumberOfRequiredFramesWithEqualScanningResult = 2;
        /**
        Content validator.
        */
        this.validator = new DefaultContentValidator({});
        if (source.ocrResolutionLimit !== undefined) {
            this.ocrResolutionLimit = source.ocrResolutionLimit;
            if (!TypeVerification.verifyInt(this.ocrResolutionLimit)) {
                throw TypeError('ocrResolutionLimit must be of type number');
            }
        }
        if (source.maximumNumberOfAccumulatedFrames !== undefined) {
            this.maximumNumberOfAccumulatedFrames = source.maximumNumberOfAccumulatedFrames;
            if (!TypeVerification.verifyInt(this.maximumNumberOfAccumulatedFrames)) {
                throw TypeError('maximumNumberOfAccumulatedFrames must be of type number');
            }
        }
        if (source.minimumNumberOfRequiredFramesWithEqualScanningResult !== undefined) {
            this.minimumNumberOfRequiredFramesWithEqualScanningResult = source.minimumNumberOfRequiredFramesWithEqualScanningResult;
            if (!TypeVerification.verifyInt(this.minimumNumberOfRequiredFramesWithEqualScanningResult)) {
                throw TypeError('minimumNumberOfRequiredFramesWithEqualScanningResult must be of type number');
            }
        }
        if (source.validator !== undefined) {
            this.validator = ContentValidator.from(source.validator);
            if (!TypeVerification.polymorphicTypeVerifier([DefaultContentValidator, PatternContentValidator])(this.validator)) {
                throw TypeError('validator must be of type ContentValidator');
            }
        }
    }
}
exports.TextPatternScannerConfiguration = TextPatternScannerConfiguration;
