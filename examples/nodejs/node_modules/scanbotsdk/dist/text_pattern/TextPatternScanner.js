"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/TextPatternScanner.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TextPatternScanner = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const ImageRef_1 = require("../image/ImageRef");
const TextPatternScannerTypes_1 = require("../text_pattern/TextPatternScannerTypes");
const TextPatternScannerTypes_2 = require("../text_pattern/TextPatternScannerTypes");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for text pattern scanning.
 *
 * Required licence feature(s): TextPatternScanner.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await TextPatternScanner.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class TextPatternScanner {
    /**
    Initialize the generic text line scanner.
    @param configuration - The configuration to use for the generic text line scanner.
    @returns Instance of TextPatternScanner.
    */
    static async create(configuration = new TextPatternScannerTypes_1.TextPatternScannerConfiguration({})) {
        TextPatternScanner.performStaticInitialization();
        if (!TypeVerification.typeVerifier(TextPatternScannerTypes_1.TextPatternScannerConfiguration)(configuration)) {
            throw TypeError('configuration must be of type TextPatternScannerConfiguration');
        }
        const nativeHandle = await TextPatternScanner.nativeTextPatternScannerInit(configuration).catch(Errors_1.remapError);
        return new TextPatternScanner(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeRun = addonNative.nativeTextPatternScannerRun.bind(this);
        this.nativeCleanRecognitionQueue = addonNative.nativeTextPatternScannerCleanRecognitionQueue.bind(this);
        TextPatternScanner.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            TextPatternScanner.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = TextPatternScanner.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return TextPatternScanner.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Recognize text line in the given image.
    @param image - The image to recognize text line in.
    @returns The recognized text line.
    */
    async run(image) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        const partial = await this.nativeRun(image).catch(Errors_1.remapError);
        return new TextPatternScannerTypes_2.TextPatternScannerResult(partial);
    }
    /**
    Clean the queue of recognize text lines.
    */
    async cleanRecognitionQueue() {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        const partial = await this.nativeCleanRecognitionQueue().catch(Errors_1.remapError);
        return partial;
    }
    static performStaticInitialization() {
        if (TextPatternScanner.staticInitializationPerformed) {
            return;
        }
        TextPatternScanner.staticInitializationPerformed = true;
        TextPatternScanner.nativeTextPatternScannerInit = addonNative.nativeTextPatternScannerInit.bind(TextPatternScanner);
        TextPatternScanner.nativeDispose = addonNative.nativeTextPatternScannerDispose;
    }
}
exports.TextPatternScanner = TextPatternScanner;
TextPatternScanner.registry = new FinalizationRegistry((nativeHandle) => {
    TextPatternScanner.nativeDispose(nativeHandle);
});
TextPatternScanner.staticInitializationPerformed = false;
