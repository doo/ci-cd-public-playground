"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/MedicalCertificateScanner.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MedicalCertificateScanner = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const ImageRef_1 = require("../image/ImageRef");
const MedicalCertificateTypes_1 = require("../medical_certificate/MedicalCertificateTypes");
const MedicalCertificateTypes_2 = require("../medical_certificate/MedicalCertificateTypes");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for medical certificate scanning.
 *
 * Required licence feature(s): MedicalCertificateScanner.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await MedicalCertificateScanner.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class MedicalCertificateScanner {
    /**
    Initialize the generic document recognizer.
    @returns Instance of MedicalCertificateScanner.
    */
    static async create() {
        MedicalCertificateScanner.performStaticInitialization();
        const nativeHandle = await MedicalCertificateScanner.nativeMedicalCertificateScannerInit().catch(Errors_1.remapError);
        return new MedicalCertificateScanner(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeRun = addonNative.nativeMedicalCertificateScannerRun.bind(this);
        MedicalCertificateScanner.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            MedicalCertificateScanner.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = MedicalCertificateScanner.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return MedicalCertificateScanner.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Recognize the document in the given image.
    @param image - The image to recognize.
    @param parameters - The recognition parameters.
    @returns The recognition result.
    */
    async run(image, parameters) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        if (!TypeVerification.typeVerifier(MedicalCertificateTypes_1.MedicalCertificateScanningParameters)(parameters)) {
            throw TypeError('parameters must be of type MedicalCertificateScanningParameters');
        }
        const partial = await this.nativeRun(image, parameters).catch(Errors_1.remapError);
        return new MedicalCertificateTypes_2.MedicalCertificateScanningResult(partial);
    }
    static performStaticInitialization() {
        if (MedicalCertificateScanner.staticInitializationPerformed) {
            return;
        }
        MedicalCertificateScanner.staticInitializationPerformed = true;
        MedicalCertificateScanner.nativeMedicalCertificateScannerInit = addonNative.nativeMedicalCertificateScannerInit.bind(MedicalCertificateScanner);
        MedicalCertificateScanner.nativeDispose = addonNative.nativeMedicalCertificateScannerDispose;
    }
}
exports.MedicalCertificateScanner = MedicalCertificateScanner;
MedicalCertificateScanner.registry = new FinalizationRegistry((nativeHandle) => {
    MedicalCertificateScanner.nativeDispose(nativeHandle);
});
MedicalCertificateScanner.staticInitializationPerformed = false;
