"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/MedicalCertificateTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MedicalCertificateScanningResult = exports.MedicalCertificateFormTypeValues = exports.MedicalCertificatePatientInfoBox = exports.MedicalCertificatePatientInfoField = exports.MedicalCertificatePatientInfoFieldTypeValues = exports.MedicalCertificateDateRecord = exports.MedicalCertificateDateRecordTypeValues = exports.MedicalCertificateCheckBox = exports.MedicalCertificateCheckBoxTypeValues = exports.MedicalCertificateScanningParameters = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const DocumentScannerTypes_1 = require("../document_scanner/DocumentScannerTypes");
const ImageRef_1 = require("../image/ImageRef");
/**
Parameters for the medical certificate scanning. The scanner supports
Form 1 (Arbeitsunfähigkeitsbescheinigung) and
Form 21a (Ärztliche Bescheinigung für den Bezug von Krankengeld bei Erkrankung eines Kindes)
from the KBV (Kassenärztliche Bundesvereinigung) list of forms.
*/
class MedicalCertificateScanningParameters {
    /** @param source {@displayType `DeepPartial<MedicalCertificateScanningParameters>`} */
    constructor(source = {}) {
        /**
        Document will be detected and cropped before recognition.
        If false, a cropped image of a document is assumed.
        
        Default is true
        */
        this.shouldCropDocument = true;
        /**
        All data in the patient info box will be extracted.
        If false, the patient info box is ignored.
        
        Default is true
        */
        this.recognizePatientInfoBox = true;
        /**
        Some forms are printed with an extra barcode that encodes the same information as the document.
        Reading the barcode is more reliable than OCR and is recommended when possible.
        If false, the barcode will not be scanned.
        
        Default is true
        */
        this.recognizeBarcode = true;
        /**
        If true, cropped document image will be extracted and returned.
        
        Default is false
        */
        this.extractCroppedImage = false;
        /**
        If true, the image is sharpened before processing.
        
        Default is false
        */
        this.preprocessInput = false;
        if (source.shouldCropDocument !== undefined) {
            this.shouldCropDocument = source.shouldCropDocument;
            if (!TypeVerification.verifyBoolean(this.shouldCropDocument)) {
                throw TypeError('shouldCropDocument must be of type boolean');
            }
        }
        if (source.recognizePatientInfoBox !== undefined) {
            this.recognizePatientInfoBox = source.recognizePatientInfoBox;
            if (!TypeVerification.verifyBoolean(this.recognizePatientInfoBox)) {
                throw TypeError('recognizePatientInfoBox must be of type boolean');
            }
        }
        if (source.recognizeBarcode !== undefined) {
            this.recognizeBarcode = source.recognizeBarcode;
            if (!TypeVerification.verifyBoolean(this.recognizeBarcode)) {
                throw TypeError('recognizeBarcode must be of type boolean');
            }
        }
        if (source.extractCroppedImage !== undefined) {
            this.extractCroppedImage = source.extractCroppedImage;
            if (!TypeVerification.verifyBoolean(this.extractCroppedImage)) {
                throw TypeError('extractCroppedImage must be of type boolean');
            }
        }
        if (source.preprocessInput !== undefined) {
            this.preprocessInput = source.preprocessInput;
            if (!TypeVerification.verifyBoolean(this.preprocessInput)) {
                throw TypeError('preprocessInput must be of type boolean');
            }
        }
    }
}
exports.MedicalCertificateScanningParameters = MedicalCertificateScanningParameters;
/** @hidden */
exports.MedicalCertificateCheckBoxTypeValues = [
    "UNKNOWN",
    "WORK_ACCIDENT",
    "ASSIGNED_TO_ACCIDENT_INSURANCE_DOCTOR",
    "INITIAL_CERTIFICATE",
    "RENEWED_CERTIFICATE",
    "INSURED_PAY_CASE",
    "FINAL_CERTIFICATE",
    "REQUIRES_CARE_YES",
    "REQUIRES_CARE_NO",
    "ACCIDENT_YES",
    "ACCIDENT_NO",
    "OTHER_ACCIDENT",
    "ENTITLEMENT_TO_CONTINUED_PAYMENT_YES",
    "ENTITLEMENT_TO_CONTINUED_PAYMENT_NO",
    "SICK_PAY_WAS_CLAIMED_NO",
    "SICK_PAY_WAS_CLAIMED_YES",
    "SINGLE_PARENT_NO",
    "SINGLE_PARENT_YES"
];
/**
Structure to contain full information about found box.
*/
class MedicalCertificateCheckBox {
    /** @param source {@displayType `DeepPartial<MedicalCertificateCheckBox>`} */
    constructor(source = {}) {
        /**
        Box type.
        
        Default is UNKNOWN
        */
        this.type = "UNKNOWN";
        /**
        True if the box is checked.
        
        Default is false
        */
        this.checked = false;
        /**
        Confidence of the checked/unchecked prediction.
        
        Default is 0.0
        */
        this.checkedConfidence = 0.0;
        if (source.type !== undefined) {
            this.type = source.type;
        }
        if (source.checked !== undefined) {
            this.checked = source.checked;
        }
        if (source.checkedConfidence !== undefined) {
            this.checkedConfidence = source.checkedConfidence;
        }
        if (source.quad !== undefined) {
            this.quad = source.quad.map((it) => { return { "x": it.x, "y": it.y }; });
        }
        else {
            throw new Error("quad must be present in constructor argument");
        }
    }
}
exports.MedicalCertificateCheckBox = MedicalCertificateCheckBox;
/** @hidden */
exports.MedicalCertificateDateRecordTypeValues = [
    "INCAPABLE_OF_WORK_SINCE",
    "INCAPABLE_OF_WORK_UNTIL",
    "DIAGNOSED_ON",
    "DOCUMENT_DATE",
    "BIRTH_DATE",
    "CHILD_NEEDS_CARE_FROM",
    "CHILD_NEEDS_CARE_UNTIL",
    "UNDEFINED"
];
/**
Structure to contain date record information.
*/
class MedicalCertificateDateRecord {
    /** @param source {@displayType `DeepPartial<MedicalCertificateDateRecord>`} */
    constructor(source = {}) {
        /**
        Validated date string.
        
        Default is ""
        */
        this.value = "";
        /**
        Raw date string.
        
        Default is ""
        */
        this.rawString = "";
        /**
        Date type.
        
        Default is UNDEFINED
        */
        this.type = "UNDEFINED";
        /**
        Confidence in the recognized value.
        
        Default is 0.0
        */
        this.recognitionConfidence = 0.0;
        if (source.quad !== undefined) {
            this.quad = source.quad.map((it) => { return { "x": it.x, "y": it.y }; });
        }
        else {
            throw new Error("quad must be present in constructor argument");
        }
        if (source.value !== undefined) {
            this.value = source.value;
        }
        if (source.rawString !== undefined) {
            this.rawString = source.rawString;
        }
        if (source.type !== undefined) {
            this.type = source.type;
        }
        if (source.recognitionConfidence !== undefined) {
            this.recognitionConfidence = source.recognitionConfidence;
        }
    }
}
exports.MedicalCertificateDateRecord = MedicalCertificateDateRecord;
/** @hidden */
exports.MedicalCertificatePatientInfoFieldTypeValues = [
    "INSURANCE_PROVIDER",
    "FIRST_NAME",
    "LAST_NAME",
    "ADDRESS_STRING1",
    "ADDRESS_STRING2",
    "DIAGNOSE",
    "HEALTH_INSURANCE_NUMBER",
    "INSURED_PERSON_NUMBER",
    "STATUS",
    "PLACE_OF_OPERATION_NUMBER",
    "DOCTOR_NUMBER",
    "UNDEFINED"
];
/**
Patient information field.
*/
class MedicalCertificatePatientInfoField {
    /** @param source {@displayType `DeepPartial<MedicalCertificatePatientInfoField>`} */
    constructor(source = {}) {
        if (source.type !== undefined) {
            this.type = source.type;
        }
        else {
            throw new Error("type must be present in constructor argument");
        }
        if (source.value !== undefined) {
            this.value = source.value;
        }
        else {
            throw new Error("value must be present in constructor argument");
        }
        if (source.recognitionConfidence !== undefined) {
            this.recognitionConfidence = source.recognitionConfidence;
        }
        else {
            throw new Error("recognitionConfidence must be present in constructor argument");
        }
    }
}
exports.MedicalCertificatePatientInfoField = MedicalCertificatePatientInfoField;
/**
Patient information box.
*/
class MedicalCertificatePatientInfoBox {
    /** @param source {@displayType `DeepPartial<MedicalCertificatePatientInfoBox>`} */
    constructor(source = {}) {
        /**
        Whether the patient info box has contents.
        
        Default is false
        */
        this.hasContents = false;
        if (source.quad !== undefined) {
            this.quad = source.quad.map((it) => { return { "x": it.x, "y": it.y }; });
        }
        else {
            throw new Error("quad must be present in constructor argument");
        }
        if (source.fields !== undefined) {
            this.fields = source.fields.map((it) => { return new MedicalCertificatePatientInfoField(it); });
        }
        else {
            throw new Error("fields must be present in constructor argument");
        }
        if (source.hasContents !== undefined) {
            this.hasContents = source.hasContents;
        }
    }
}
exports.MedicalCertificatePatientInfoBox = MedicalCertificatePatientInfoBox;
/** @hidden */
exports.MedicalCertificateFormTypeValues = [
    "UNKNOWN",
    "FORM_1A",
    "FORM_1B",
    "FORM_1C",
    "FORM_1D",
    "FORM_21A",
    "FORM_21A_BACK",
    "FORM_1B_CUSTOM"
];
/**
The result of the medical certificate scanning.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getMedicalCertificateScanningResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class MedicalCertificateScanningResult {
    /** @param source {@displayType `DeepPartial<MedicalCertificateScanningResult>`} */
    constructor(source = {}) {
        /**
        True if scanning was successful.
        
        Default is false
        */
        this.scanningSuccessful = false;
        /**
        Form type.
        
        Default is UNKNOWN
        */
        this.formType = "UNKNOWN";
        /**
        The number of 90-degree clockwise rotations that were applied to the original image.
        The same number of counter-clockwise rotations are necessary to make the image upright again.
        
        Default is 0
        */
        this.clockwiseRotations = 0;
        /**
        The cropped image used for recognition.
        */
        this.croppedImage = null;
        /**
        The scale factor used to scale the image to the recognition size.
        
        Default is 1.0
        */
        this.scaleX = 1.0;
        /**
        The scale factor used to scale the image to the recognition size.
        
        Default is 1.0
        */
        this.scaleY = 1.0;
        this._releasePromise = null;
        if (source.scanningSuccessful !== undefined) {
            this.scanningSuccessful = source.scanningSuccessful;
        }
        if (source.patientInfoBox !== undefined) {
            this.patientInfoBox = new MedicalCertificatePatientInfoBox(source.patientInfoBox);
        }
        else {
            throw new Error("patientInfoBox must be present in constructor argument");
        }
        if (source.checkBoxes !== undefined) {
            this.checkBoxes = source.checkBoxes.map((it) => { return new MedicalCertificateCheckBox(it); });
        }
        else {
            throw new Error("checkBoxes must be present in constructor argument");
        }
        if (source.dates !== undefined) {
            this.dates = source.dates.map((it) => { return new MedicalCertificateDateRecord(it); });
        }
        else {
            throw new Error("dates must be present in constructor argument");
        }
        if (source.formType !== undefined) {
            this.formType = source.formType;
        }
        if (source.clockwiseRotations !== undefined) {
            this.clockwiseRotations = source.clockwiseRotations;
        }
        if (source.croppedImage !== undefined) {
            this.croppedImage = source.croppedImage != null ? ImageRef_1.ImageRef.fromPartial(source.croppedImage) : null;
        }
        if (source.scaleX !== undefined) {
            this.scaleX = source.scaleX;
        }
        if (source.scaleY !== undefined) {
            this.scaleY = source.scaleY;
        }
        if (source.documentDetectionResult !== undefined) {
            this.documentDetectionResult = source.documentDetectionResult != null ? new DocumentScannerTypes_1.DocumentDetectionResult(source.documentDetectionResult) : null;
        }
        else {
            throw new Error("documentDetectionResult must be present in constructor argument");
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            {
                if (this.croppedImage != null) {
                    promises.push(this.croppedImage.release());
                }
            }
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.MedicalCertificateScanningResult = MedicalCertificateScanningResult;
