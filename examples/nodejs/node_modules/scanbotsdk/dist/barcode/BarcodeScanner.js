"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/BarcodeScanner.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.BarcodeScanner = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const BarcodeScannerTypes_1 = require("../barcode/BarcodeScannerTypes");
const BarcodeScannerTypes_2 = require("../barcode/BarcodeScannerTypes");
const ImageRef_1 = require("../image/ImageRef");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for barcode scanning.
 *
 * Required licence feature(s): BarcodeScanner.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await BarcodeScanner.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class BarcodeScanner {
    /**
    Initialize the barcode scanner.
    @param configuration - The configuration to use for the barcode scanner.
    @returns Instance of BarcodeScanner.
    */
    static async create(configuration = new BarcodeScannerTypes_1.BarcodeScannerConfiguration({})) {
        BarcodeScanner.performStaticInitialization();
        if (!TypeVerification.typeVerifier(BarcodeScannerTypes_1.BarcodeScannerConfiguration)(configuration)) {
            throw TypeError('configuration must be of type BarcodeScannerConfiguration');
        }
        const nativeHandle = await BarcodeScanner.nativeBarcodeScannerInit(configuration).catch(Errors_1.remapError);
        return new BarcodeScanner(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeRun = addonNative.nativeBarcodeScannerRun.bind(this);
        this.nativeClearFrameAccumulationCache = addonNative.nativeBarcodeScannerClearFrameAccumulationCache.bind(this);
        BarcodeScanner.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            BarcodeScanner.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = BarcodeScanner.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return BarcodeScanner.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Recognize barcodes in the given image.
    @param image - The image to recognize barcodes in.
    @returns The recognized barcodes.
    */
    async run(image) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        const partial = await this.nativeRun(image).catch(Errors_1.remapError);
        return new BarcodeScannerTypes_2.BarcodeScannerResult(partial);
    }
    /**
    Clear the cache of the barcode frame accumulator.
    */
    async clearFrameAccumulationCache() {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        const partial = await this.nativeClearFrameAccumulationCache().catch(Errors_1.remapError);
        return partial;
    }
    static performStaticInitialization() {
        if (BarcodeScanner.staticInitializationPerformed) {
            return;
        }
        BarcodeScanner.staticInitializationPerformed = true;
        BarcodeScanner.nativeBarcodeScannerInit = addonNative.nativeBarcodeScannerInit.bind(BarcodeScanner);
        BarcodeScanner.nativeDispose = addonNative.nativeBarcodeScannerDispose;
    }
}
exports.BarcodeScanner = BarcodeScanner;
BarcodeScanner.registry = new FinalizationRegistry((nativeHandle) => {
    BarcodeScanner.nativeDispose(nativeHandle);
});
BarcodeScanner.staticInitializationPerformed = false;
