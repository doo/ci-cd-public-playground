"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/BarcodeScannerTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.BarcodeScannerResult = exports.BarcodeScannerConfiguration = exports.BarcodeAccumulationConfiguration = exports.BarcodeAccumulationMethodValues = exports.BarcodeScannerEngineModeValues = exports.BarcodeItem = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const AcceleratorTypes_1 = require("../ml/AcceleratorTypes");
const BarcodeDocumentTypes_1 = require("../barcode/BarcodeDocumentTypes");
const BarcodeConfigurationTypes_1 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_2 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_3 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_4 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_5 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_6 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_7 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_8 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_9 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_10 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_11 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_12 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_13 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_14 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_15 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_16 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_17 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_18 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_19 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_20 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_21 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_22 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_23 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_24 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_25 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_26 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_27 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_28 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_29 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_30 = require("../barcode/BarcodeConfigurationTypes");
const BarcodeConfigurationTypes_31 = require("../barcode/BarcodeConfigurationTypes");
const GenericDocument_1 = require("../generic_document/GenericDocument");
const ImageRef_1 = require("../image/ImageRef");
const CommonTypes_1 = require("../common/CommonTypes");
const BarcodeTypes_1 = require("../barcode/BarcodeTypes");
const BarcodeTypes_2 = require("../barcode/BarcodeTypes");
const AcceleratorTypes_2 = require("../ml/AcceleratorTypes");
/**
A single barcode found in the input image. Barcodes with the same content but different locations in the image are considered separate barcodes.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getBarcodeItemFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class BarcodeItem {
    /** @param source {@displayType `DeepPartial<BarcodeItem>`} */
    constructor(source = {}) {
        /**
        Barcode format.
        
        Default is NONE
        */
        this.format = "NONE";
        /**
        True if this is a 1D barcode that is printed upside-down, that is, the barcode was scanned right-to-left.
        
        Default is false
        */
        this.isUpsideDown = false;
        /**
        A crop from the input image containing the barcode.
        */
        this.sourceImage = null;
        /**
        True if the barcode contains a GS1 message. Requires GS1 handling to be enabled in the decoding option.
        
        Default is false
        */
        this.isGS1Message = false;
        /**
        True if this result is the 2D part of a GS1 Composite barcode.
        Can only happen if GS1_COMPOSITE scanning is disabled and a part of the composite (1D) or (2D) is scanned separately.
        When GS1_COMPOSITE scanning is enabled, parts of the composite barcode are never returned separately, even if their respective
        format is enabled in the decoding options.
        
        Default is false
        */
        this.isGS1CompositePart = false;
        /**
        The number of 1D stacks in the barcode. Applies only to DATABAR and DATABAR_EXPANDED barcodes.
        
        Default is 1
        */
        this.dataBarStackSize = 1;
        /**
        The size score is a floating point value between 0 and 1 that represents the relative size of the barcode in the input image.
        Barcodes taking up a small portion of the input image will have a score close to 0, while barcodes that take a large portion will have a score close to 1.
        
        Default is 0
        */
        this.sizeScore = 0.0;
        /**
        The index of the barcode to uniquely identify it.
        In case of frame accumulation, the index remains the same across frames.
        
        Default is -1
        */
        this.globalIndex = -1;
        this._releasePromise = null;
        if (source.format !== undefined) {
            this.format = source.format;
        }
        if (source.text !== undefined) {
            this.text = source.text;
        }
        else {
            throw new Error("text must be present in constructor argument");
        }
        if (source.rawBytes !== undefined) {
            this.rawBytes = source.rawBytes;
        }
        else {
            throw new Error("rawBytes must be present in constructor argument");
        }
        if (source.rawBytesEncodings !== undefined) {
            this.rawBytesEncodings = source.rawBytesEncodings.map((it) => { return new BarcodeTypes_1.RangeEncoding(it); });
        }
        else {
            throw new Error("rawBytesEncodings must be present in constructor argument");
        }
        if (source.structuredAppendInfo !== undefined) {
            this.structuredAppendInfo = source.structuredAppendInfo != null ? new BarcodeTypes_2.StructuredAppendInfo(source.structuredAppendInfo) : null;
        }
        else {
            throw new Error("structuredAppendInfo must be present in constructor argument");
        }
        if (source.quad !== undefined) {
            this.quad = source.quad.map((it) => { return { "x": it.x, "y": it.y }; });
        }
        else {
            throw new Error("quad must be present in constructor argument");
        }
        if (source.quadNormalized !== undefined) {
            this.quadNormalized = source.quadNormalized.map((it) => { return { "x": it.x, "y": it.y }; });
        }
        else {
            throw new Error("quadNormalized must be present in constructor argument");
        }
        if (source.extendedQuad !== undefined) {
            this.extendedQuad = source.extendedQuad.map((it) => { return { "x": it.x, "y": it.y }; });
        }
        else {
            throw new Error("extendedQuad must be present in constructor argument");
        }
        if (source.extendedQuadNormalized !== undefined) {
            this.extendedQuadNormalized = source.extendedQuadNormalized.map((it) => { return { "x": it.x, "y": it.y }; });
        }
        else {
            throw new Error("extendedQuadNormalized must be present in constructor argument");
        }
        if (source.isUpsideDown !== undefined) {
            this.isUpsideDown = source.isUpsideDown;
        }
        if (source.sourceImage !== undefined) {
            this.sourceImage = source.sourceImage != null ? ImageRef_1.ImageRef.fromPartial(source.sourceImage) : null;
        }
        if (source.upcEanExtension !== undefined) {
            this.upcEanExtension = source.upcEanExtension;
        }
        else {
            throw new Error("upcEanExtension must be present in constructor argument");
        }
        if (source.isGS1Message !== undefined) {
            this.isGS1Message = source.isGS1Message;
        }
        if (source.isGS1CompositePart !== undefined) {
            this.isGS1CompositePart = source.isGS1CompositePart;
        }
        if (source.dataBarStackSize !== undefined) {
            this.dataBarStackSize = source.dataBarStackSize;
        }
        if (source.sizeScore !== undefined) {
            this.sizeScore = source.sizeScore;
        }
        if (source.extractedDocument !== undefined) {
            this.extractedDocument = source.extractedDocument != null ? new GenericDocument_1.GenericDocument(source.extractedDocument) : null;
        }
        else {
            throw new Error("extractedDocument must be present in constructor argument");
        }
        if (source.globalIndex !== undefined) {
            this.globalIndex = source.globalIndex;
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            {
                if (this.sourceImage != null) {
                    promises.push(this.sourceImage.release());
                }
            }
            {
                if (this.extractedDocument != null) {
                    promises.push(this.extractedDocument.release());
                }
            }
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.BarcodeItem = BarcodeItem;
/** @hidden */
exports.BarcodeScannerEngineModeValues = [
    "LEGACY",
    "NEXT_GEN_LOW_POWER",
    "NEXT_GEN",
    "NEXT_GEN_LOW_POWER_FAR_DISTANCE",
    "NEXT_GEN_FAR_DISTANCE",
    "NEXT_GEN_MAX_DISTANCE"
];
/** @hidden */
exports.BarcodeAccumulationMethodValues = [
    "LAST_VISIBLE",
    "INTERPOLATE_BY_CAMERA"
];
/**
Configuration for how to accumulate results.
*/
class BarcodeAccumulationConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeAccumulationConfiguration>`} */
    constructor(source = {}) {
        /**
        The time in milliseconds to accumulate barcodes across multiple frames.
        
        Default is 500
        */
        this.accumulationTime = 500;
        /**
        If true, the results from previous frames that are not connected to results on the current frame will be removed.
        This is should be enabled when large camera movements are expected.
        For small camera movements, the option can be disabled.
        
        Default is true
        */
        this.removeUnconnectedResults = true;
        /**
        The method how to display barcodes from previous frames.
        
        Default is INTERPOLATE_BY_CAMERA
        */
        this.method = "INTERPOLATE_BY_CAMERA";
        if (source.accumulationTime !== undefined) {
            this.accumulationTime = source.accumulationTime;
            if (!TypeVerification.verifyInt(this.accumulationTime)) {
                throw TypeError('accumulationTime must be of type number');
            }
        }
        if (source.removeUnconnectedResults !== undefined) {
            this.removeUnconnectedResults = source.removeUnconnectedResults;
            if (!TypeVerification.verifyBoolean(this.removeUnconnectedResults)) {
                throw TypeError('removeUnconnectedResults must be of type boolean');
            }
        }
        if (source.method !== undefined) {
            this.method = source.method;
            if (!TypeVerification.enumVerifier(exports.BarcodeAccumulationMethodValues)(this.method)) {
                throw TypeError('method must be of type BarcodeAccumulationMethod');
            }
        }
    }
}
exports.BarcodeAccumulationConfiguration = BarcodeAccumulationConfiguration;
/**
Configuration for the barcode scanner.
*/
class BarcodeScannerConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeScannerConfiguration>`} */
    constructor(source = {}) {
        /**
        Options for barcode decoding.
        */
        this.barcodeFormatConfigurations = [new BarcodeConfigurationTypes_9.BarcodeFormatCommonConfiguration({})];
        /**
        List of document formats to be extracted.
        Barcodes that decode to one of the extracted document formats will have extractedDocument field in BarcodeItem populated with the parsed document.
        By default all supported barcode document formats are accepted.
        If empty, no barcodes will be parsed into documents.
        To only accept the document formats listed in extractedDocumentFormats, the property onlyAcceptDocuments must be true.
        */
        this.extractedDocumentFormats = ["AAMVA", "BOARDING_PASS", "DE_MEDICAL_PLAN", "MEDICAL_CERTIFICATE", "ID_CARD_PDF_417", "SEPA", "SWISS_QR", "VCARD", "GS1", "HIBC", "BRITISH_COLUMBIA_DRIVER_LICENSE"];
        /**
        If true and extractedDocumentFormats is not empty, then barcodes that don't decode to one of the extracted document formats will be ignored.
        
        Default is false
        */
        this.onlyAcceptDocuments = false;
        /**
        If true, the barcode image will be returned in the BarcodeItem.
        
        Default is false
        */
        this.returnBarcodeImage = false;
        /**
        Accelerator to use for running machine learning models. Is applied only for live mode scanning.
        */
        this.accelerator = null;
        /**
        The engine mode for barcode scanning.
        
        Default is NEXT_GEN
        */
        this.engineMode = "NEXT_GEN";
        /**
        Configuration to accumulate barcodes across multiple frames in live mode.
        */
        this.accumulationConfig = new BarcodeAccumulationConfiguration({});
        /**
        If true, the quads of linear (1D) barcodes will be further refined to more closely match the barcode's outline in the input image.
        This will also produce stabler barcode outlines across frames for use cases such as AR.
        
        Default is false
        */
        this.optimizedForOverlays = false;
        /**
        Scanners typically can produce better results from a single image if they are allowed to spend a longer time analyzing it.
        
        On the other hand, scanners can produce even better results if they can analyze multiple images of the same subject and cross-check and combine the results.
        The processing mode tells the scanner whether to optimize for single or multiple images of a subject.
        
        Default is AUTO
        */
        this.processingMode = "AUTO";
        if (source.barcodeFormatConfigurations !== undefined) {
            this.barcodeFormatConfigurations = source.barcodeFormatConfigurations.map((it) => { return BarcodeConfigurationTypes_13.BarcodeFormatConfigurationBase.from(it); });
            if (!TypeVerification.listVerifier(TypeVerification.polymorphicTypeVerifier([BarcodeConfigurationTypes_3.BarcodeFormatCodabarConfiguration, BarcodeConfigurationTypes_4.BarcodeFormatCode11Configuration, BarcodeConfigurationTypes_7.BarcodeFormatCode39Configuration, BarcodeConfigurationTypes_8.BarcodeFormatCode93Configuration, BarcodeConfigurationTypes_5.BarcodeFormatCode128Configuration, BarcodeConfigurationTypes_6.BarcodeFormatCode2Of5Configuration, BarcodeConfigurationTypes_14.BarcodeFormatDataBarConfiguration, BarcodeConfigurationTypes_15.BarcodeFormatDataBarExpandedConfiguration, BarcodeConfigurationTypes_16.BarcodeFormatDataBarLimitedConfiguration, BarcodeConfigurationTypes_19.BarcodeFormatItfConfiguration, BarcodeConfigurationTypes_23.BarcodeFormatMsiPlesseyConfiguration, BarcodeConfigurationTypes_30.BarcodeFormatUpcEanConfiguration, BarcodeConfigurationTypes_25.BarcodeFormatPharmaCodeConfiguration, BarcodeConfigurationTypes_2.BarcodeFormatAztecConfiguration, BarcodeConfigurationTypes_27.BarcodeFormatQrCodeConfiguration, BarcodeConfigurationTypes_24.BarcodeFormatPdf417Configuration, BarcodeConfigurationTypes_22.BarcodeFormatMicroPdf417Configuration, BarcodeConfigurationTypes_17.BarcodeFormatDataMatrixConfiguration, BarcodeConfigurationTypes_21.BarcodeFormatMaxiCodeConfiguration, BarcodeConfigurationTypes_1.BarcodeFormatAustraliaPostConfiguration, BarcodeConfigurationTypes_20.BarcodeFormatJapanPostConfiguration, BarcodeConfigurationTypes_28.BarcodeFormatRoyalMailConfiguration, BarcodeConfigurationTypes_29.BarcodeFormatRoyalTntPostConfiguration, BarcodeConfigurationTypes_31.BarcodeFormatUspsIntelligentMailConfiguration, BarcodeConfigurationTypes_26.BarcodeFormatPharmaCodeTwoTrackConfiguration, BarcodeConfigurationTypes_18.BarcodeFormatGs1CompositeConfiguration, BarcodeConfigurationTypes_11.BarcodeFormatCommonOneDConfiguration, BarcodeConfigurationTypes_12.BarcodeFormatCommonTwoDConfiguration, BarcodeConfigurationTypes_10.BarcodeFormatCommonFourStateConfiguration, BarcodeConfigurationTypes_9.BarcodeFormatCommonConfiguration]))(this.barcodeFormatConfigurations)) {
                throw TypeError('barcodeFormatConfigurations must be of type BarcodeFormatConfigurationBase[]');
            }
        }
        if (source.extractedDocumentFormats !== undefined) {
            this.extractedDocumentFormats = source.extractedDocumentFormats.map((it) => { return it; });
            if (!TypeVerification.listVerifier(TypeVerification.enumVerifier(BarcodeDocumentTypes_1.BarcodeDocumentFormatValues))(this.extractedDocumentFormats)) {
                throw TypeError('extractedDocumentFormats must be of type BarcodeDocumentFormat[]');
            }
        }
        if (source.onlyAcceptDocuments !== undefined) {
            this.onlyAcceptDocuments = source.onlyAcceptDocuments;
            if (!TypeVerification.verifyBoolean(this.onlyAcceptDocuments)) {
                throw TypeError('onlyAcceptDocuments must be of type boolean');
            }
        }
        if (source.returnBarcodeImage !== undefined) {
            this.returnBarcodeImage = source.returnBarcodeImage;
            if (!TypeVerification.verifyBoolean(this.returnBarcodeImage)) {
                throw TypeError('returnBarcodeImage must be of type boolean');
            }
        }
        if (source.accelerator !== undefined) {
            this.accelerator = source.accelerator != null ? AcceleratorTypes_1.Accelerator.from(source.accelerator) : null;
            if (!TypeVerification.optionalVerifier(TypeVerification.polymorphicTypeVerifier([AcceleratorTypes_2.TensorRtAccelerator]))(this.accelerator)) {
                throw TypeError('accelerator must be of type Accelerator | null');
            }
        }
        if (source.engineMode !== undefined) {
            this.engineMode = source.engineMode;
            if (!TypeVerification.enumVerifier(exports.BarcodeScannerEngineModeValues)(this.engineMode)) {
                throw TypeError('engineMode must be of type BarcodeScannerEngineMode');
            }
        }
        if (source.accumulationConfig !== undefined) {
            this.accumulationConfig = new BarcodeAccumulationConfiguration(source.accumulationConfig);
            if (!TypeVerification.typeVerifier(BarcodeAccumulationConfiguration)(this.accumulationConfig)) {
                throw TypeError('accumulationConfig must be of type BarcodeAccumulationConfiguration');
            }
        }
        if (source.optimizedForOverlays !== undefined) {
            this.optimizedForOverlays = source.optimizedForOverlays;
            if (!TypeVerification.verifyBoolean(this.optimizedForOverlays)) {
                throw TypeError('optimizedForOverlays must be of type boolean');
            }
        }
        if (source.processingMode !== undefined) {
            this.processingMode = source.processingMode;
            if (!TypeVerification.enumVerifier(CommonTypes_1.ProcessingModeValues)(this.processingMode)) {
                throw TypeError('processingMode must be of type ProcessingMode');
            }
        }
    }
}
exports.BarcodeScannerConfiguration = BarcodeScannerConfiguration;
/**
The result of barcode scanning.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getBarcodeScannerResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class BarcodeScannerResult {
    /** @param source {@displayType `DeepPartial<BarcodeScannerResult>`} */
    constructor(source = {}) {
        this._releasePromise = null;
        if (source.barcodes !== undefined) {
            this.barcodes = source.barcodes.map((it) => { return new BarcodeItem(it); });
        }
        else {
            throw new Error("barcodes must be present in constructor argument");
        }
        if (source.success !== undefined) {
            this.success = source.success;
        }
        else {
            throw new Error("success must be present in constructor argument");
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            this.barcodes.forEach((it) => { promises.push(it.release()); });
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.BarcodeScannerResult = BarcodeScannerResult;
