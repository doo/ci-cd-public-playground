"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/BarcodeConfigurationTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.BarcodeFormatCommonConfiguration = exports.BarcodeFormatCommonFourStateConfiguration = exports.BarcodeFormatCommonTwoDConfiguration = exports.BarcodeFormatCommonOneDConfiguration = exports.BarcodeFormatGs1CompositeConfiguration = exports.BarcodeFormatPharmaCodeTwoTrackConfiguration = exports.BarcodeFormatUspsIntelligentMailConfiguration = exports.BarcodeFormatRoyalTntPostConfiguration = exports.BarcodeFormatRoyalMailConfiguration = exports.BarcodeFormatJapanPostConfiguration = exports.BarcodeFormatAustraliaPostConfiguration = exports.AustraliaPostCustomerFormatValues = exports.BarcodeFormatFourStateConfigurationBase = exports.BarcodeFormatMaxiCodeConfiguration = exports.BarcodeFormatDataMatrixConfiguration = exports.BarcodeFormatMicroPdf417Configuration = exports.BarcodeFormatPdf417Configuration = exports.BarcodeFormatQrCodeConfiguration = exports.BarcodeFormatAztecConfiguration = exports.BarcodeFormatTwoDConfigurationBase = exports.BarcodeFormatPharmaCodeConfiguration = exports.BarcodeFormatUpcEanConfiguration = exports.BarcodeFormatMsiPlesseyConfiguration = exports.MsiPlesseyChecksumAlgorithmValues = exports.BarcodeFormatItfConfiguration = exports.BarcodeFormatDataBarLimitedConfiguration = exports.BarcodeFormatDataBarExpandedConfiguration = exports.BarcodeFormatDataBarConfiguration = exports.BarcodeFormatCode2Of5Configuration = exports.BarcodeFormatCode128Configuration = exports.BarcodeFormatCode93Configuration = exports.BarcodeFormatCode39Configuration = exports.BarcodeFormatCode11Configuration = exports.BarcodeFormatCodabarConfiguration = exports.BarcodeFormatOneDConfigurationBase = exports.BarcodeFormatConfigurationBase = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const BarcodeTypes_1 = require("../barcode/BarcodeTypes");
const BarcodeTypes_2 = require("../barcode/BarcodeTypes");
const BarcodeTypes_3 = require("../barcode/BarcodeTypes");
/** @internal */
var BarcodeFormatConfigurationBase;
(function (BarcodeFormatConfigurationBase) {
    /** @internal */
    function from(source) {
        const _type = source["_type"];
        switch (_type) {
            case "BarcodeFormatCodabarConfiguration": return new BarcodeFormatCodabarConfiguration(source);
            case "BarcodeFormatCode11Configuration": return new BarcodeFormatCode11Configuration(source);
            case "BarcodeFormatCode39Configuration": return new BarcodeFormatCode39Configuration(source);
            case "BarcodeFormatCode93Configuration": return new BarcodeFormatCode93Configuration(source);
            case "BarcodeFormatCode128Configuration": return new BarcodeFormatCode128Configuration(source);
            case "BarcodeFormatCode2Of5Configuration": return new BarcodeFormatCode2Of5Configuration(source);
            case "BarcodeFormatDataBarConfiguration": return new BarcodeFormatDataBarConfiguration(source);
            case "BarcodeFormatDataBarExpandedConfiguration": return new BarcodeFormatDataBarExpandedConfiguration(source);
            case "BarcodeFormatDataBarLimitedConfiguration": return new BarcodeFormatDataBarLimitedConfiguration(source);
            case "BarcodeFormatITFConfiguration": return new BarcodeFormatItfConfiguration(source);
            case "BarcodeFormatMSIPlesseyConfiguration": return new BarcodeFormatMsiPlesseyConfiguration(source);
            case "BarcodeFormatUpcEanConfiguration": return new BarcodeFormatUpcEanConfiguration(source);
            case "BarcodeFormatPharmaCodeConfiguration": return new BarcodeFormatPharmaCodeConfiguration(source);
            case "BarcodeFormatAztecConfiguration": return new BarcodeFormatAztecConfiguration(source);
            case "BarcodeFormatQRCodeConfiguration": return new BarcodeFormatQrCodeConfiguration(source);
            case "BarcodeFormatPDF417Configuration": return new BarcodeFormatPdf417Configuration(source);
            case "BarcodeFormatMicroPDF417Configuration": return new BarcodeFormatMicroPdf417Configuration(source);
            case "BarcodeFormatDataMatrixConfiguration": return new BarcodeFormatDataMatrixConfiguration(source);
            case "BarcodeFormatMaxiCodeConfiguration": return new BarcodeFormatMaxiCodeConfiguration(source);
            case "BarcodeFormatAustraliaPostConfiguration": return new BarcodeFormatAustraliaPostConfiguration(source);
            case "BarcodeFormatJapanPostConfiguration": return new BarcodeFormatJapanPostConfiguration(source);
            case "BarcodeFormatRoyalMailConfiguration": return new BarcodeFormatRoyalMailConfiguration(source);
            case "BarcodeFormatRoyalTNTPostConfiguration": return new BarcodeFormatRoyalTntPostConfiguration(source);
            case "BarcodeFormatUSPSIntelligentMailConfiguration": return new BarcodeFormatUspsIntelligentMailConfiguration(source);
            case "BarcodeFormatPharmaCodeTwoTrackConfiguration": return new BarcodeFormatPharmaCodeTwoTrackConfiguration(source);
            case "BarcodeFormatGS1CompositeConfiguration": return new BarcodeFormatGs1CompositeConfiguration(source);
            case "BarcodeFormatCommonOneDConfiguration": return new BarcodeFormatCommonOneDConfiguration(source);
            case "BarcodeFormatCommonTwoDConfiguration": return new BarcodeFormatCommonTwoDConfiguration(source);
            case "BarcodeFormatCommonFourStateConfiguration": return new BarcodeFormatCommonFourStateConfiguration(source);
            case "BarcodeFormatCommonConfiguration": return new BarcodeFormatCommonConfiguration(source);
            default: throw new Error(`Unknown child class name: ${_type}`);
        }
    }
    BarcodeFormatConfigurationBase.from = from;
})(BarcodeFormatConfigurationBase || (exports.BarcodeFormatConfigurationBase = BarcodeFormatConfigurationBase = {}));
/** @internal */
var BarcodeFormatOneDConfigurationBase;
(function (BarcodeFormatOneDConfigurationBase) {
    /** @internal */
    function from(source) {
        const _type = source["_type"];
        switch (_type) {
            case "BarcodeFormatCodabarConfiguration": return new BarcodeFormatCodabarConfiguration(source);
            case "BarcodeFormatCode11Configuration": return new BarcodeFormatCode11Configuration(source);
            case "BarcodeFormatCode39Configuration": return new BarcodeFormatCode39Configuration(source);
            case "BarcodeFormatCode93Configuration": return new BarcodeFormatCode93Configuration(source);
            case "BarcodeFormatCode128Configuration": return new BarcodeFormatCode128Configuration(source);
            case "BarcodeFormatCode2Of5Configuration": return new BarcodeFormatCode2Of5Configuration(source);
            case "BarcodeFormatDataBarConfiguration": return new BarcodeFormatDataBarConfiguration(source);
            case "BarcodeFormatDataBarExpandedConfiguration": return new BarcodeFormatDataBarExpandedConfiguration(source);
            case "BarcodeFormatDataBarLimitedConfiguration": return new BarcodeFormatDataBarLimitedConfiguration(source);
            case "BarcodeFormatITFConfiguration": return new BarcodeFormatItfConfiguration(source);
            case "BarcodeFormatMSIPlesseyConfiguration": return new BarcodeFormatMsiPlesseyConfiguration(source);
            case "BarcodeFormatUpcEanConfiguration": return new BarcodeFormatUpcEanConfiguration(source);
            case "BarcodeFormatPharmaCodeConfiguration": return new BarcodeFormatPharmaCodeConfiguration(source);
            default: throw new Error(`Unknown child class name: ${_type}`);
        }
    }
    BarcodeFormatOneDConfigurationBase.from = from;
})(BarcodeFormatOneDConfigurationBase || (exports.BarcodeFormatOneDConfigurationBase = BarcodeFormatOneDConfigurationBase = {}));
/**
Codabar barcode configuration. Add to scanner configuration to scan Codabar barcodes.
*/
class BarcodeFormatCodabarConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCodabarConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCodabarConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        If true, barcodes with heavy blur or from far distance are tried to scan.
        
        Default is true
        */
        this.enableOneDBlurScanner = true;
        /**
        If true, return the start and end characters.
        
        Default is false
        */
        this.returnStartEnd = false;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.enableOneDBlurScanner !== undefined) {
            this.enableOneDBlurScanner = source.enableOneDBlurScanner;
            if (!TypeVerification.verifyBoolean(this.enableOneDBlurScanner)) {
                throw TypeError('enableOneDBlurScanner must be of type boolean');
            }
        }
        if (source.returnStartEnd !== undefined) {
            this.returnStartEnd = source.returnStartEnd;
            if (!TypeVerification.verifyBoolean(this.returnStartEnd)) {
                throw TypeError('returnStartEnd must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatCodabarConfiguration = BarcodeFormatCodabarConfiguration;
/**
Code 11 barcode configuration. Add to scanner configuration to scan Code 11 barcodes.
*/
class BarcodeFormatCode11Configuration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCode11Configuration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCode11Configuration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        If true, the check digits are stripped from the result.
        
        Default is false
        */
        this.stripCheckDigits = false;
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        If true, return CODE_11 barcodes only if they have a valid checksum.
        
        Default is true
        */
        this.checksum = true;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.stripCheckDigits !== undefined) {
            this.stripCheckDigits = source.stripCheckDigits;
            if (!TypeVerification.verifyBoolean(this.stripCheckDigits)) {
                throw TypeError('stripCheckDigits must be of type boolean');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.checksum !== undefined) {
            this.checksum = source.checksum;
            if (!TypeVerification.verifyBoolean(this.checksum)) {
                throw TypeError('checksum must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatCode11Configuration = BarcodeFormatCode11Configuration;
/**
Code 39 barcode and derivatives configuration. Add to scanner configuration to scan Code 39, Code 32 (Italian Pharmacode), PZN7 and PZN8 (Pharmazentralnummer) barcodes.
*/
class BarcodeFormatCode39Configuration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCode39Configuration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCode39Configuration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        If true, the check digits are stripped from the result.
        
        Default is false
        */
        this.stripCheckDigits = false;
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        If true, barcodes with heavy blur or from far distance are tried to scan.
        
        Default is true
        */
        this.enableOneDBlurScanner = true;
        /**
        If true, scan and return valid CODE_32 (Italian Pharmacode) barcodes. If false, CODE_32 barcodes are not decoded and are returned as CODE_39 instead.
        
        Default is false
        */
        this.code32 = false;
        /**
        If true, scan CODE_39 barcodes.
        
        Default is true
        */
        this.code39 = true;
        /**
        If true, scan PZN7 (legacy Pharmazentralnummer) barcodes. If false, PZN7 barcodes are not decoded and are returned as CODE_39 instead.
        
        Default is true
        */
        this.pzn7 = true;
        /**
        If true, scan PZN8 (Pharmazentralnummer) barcodes. If false, PZN8 barcodes are not decoded and are returned as CODE_39 instead.
        
        Default is true
        */
        this.pzn8 = true;
        /**
        If true, try to scan CODE_39 in extended mode.
        
        Default is false
        */
        this.tryCode39ExtendedMode = false;
        /**
        If true, return CODE_39 barcodes only if they have a valid check digit.
        
        Default is false
        */
        this.useCode39CheckDigit = false;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.stripCheckDigits !== undefined) {
            this.stripCheckDigits = source.stripCheckDigits;
            if (!TypeVerification.verifyBoolean(this.stripCheckDigits)) {
                throw TypeError('stripCheckDigits must be of type boolean');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.enableOneDBlurScanner !== undefined) {
            this.enableOneDBlurScanner = source.enableOneDBlurScanner;
            if (!TypeVerification.verifyBoolean(this.enableOneDBlurScanner)) {
                throw TypeError('enableOneDBlurScanner must be of type boolean');
            }
        }
        if (source.code32 !== undefined) {
            this.code32 = source.code32;
            if (!TypeVerification.verifyBoolean(this.code32)) {
                throw TypeError('code32 must be of type boolean');
            }
        }
        if (source.code39 !== undefined) {
            this.code39 = source.code39;
            if (!TypeVerification.verifyBoolean(this.code39)) {
                throw TypeError('code39 must be of type boolean');
            }
        }
        if (source.pzn7 !== undefined) {
            this.pzn7 = source.pzn7;
            if (!TypeVerification.verifyBoolean(this.pzn7)) {
                throw TypeError('pzn7 must be of type boolean');
            }
        }
        if (source.pzn8 !== undefined) {
            this.pzn8 = source.pzn8;
            if (!TypeVerification.verifyBoolean(this.pzn8)) {
                throw TypeError('pzn8 must be of type boolean');
            }
        }
        if (source.tryCode39ExtendedMode !== undefined) {
            this.tryCode39ExtendedMode = source.tryCode39ExtendedMode;
            if (!TypeVerification.verifyBoolean(this.tryCode39ExtendedMode)) {
                throw TypeError('tryCode39ExtendedMode must be of type boolean');
            }
        }
        if (source.useCode39CheckDigit !== undefined) {
            this.useCode39CheckDigit = source.useCode39CheckDigit;
            if (!TypeVerification.verifyBoolean(this.useCode39CheckDigit)) {
                throw TypeError('useCode39CheckDigit must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatCode39Configuration = BarcodeFormatCode39Configuration;
/**
Code 93 barcode configuration. Add to scanner configuration to scan Code 93 barcodes.
*/
class BarcodeFormatCode93Configuration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCode93Configuration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCode93Configuration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        If true, the check digits are stripped from the result.
        
        Default is false
        */
        this.stripCheckDigits = false;
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        If true, barcodes with heavy blur or from far distance are tried to scan.
        
        Default is true
        */
        this.enableOneDBlurScanner = true;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.stripCheckDigits !== undefined) {
            this.stripCheckDigits = source.stripCheckDigits;
            if (!TypeVerification.verifyBoolean(this.stripCheckDigits)) {
                throw TypeError('stripCheckDigits must be of type boolean');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.enableOneDBlurScanner !== undefined) {
            this.enableOneDBlurScanner = source.enableOneDBlurScanner;
            if (!TypeVerification.verifyBoolean(this.enableOneDBlurScanner)) {
                throw TypeError('enableOneDBlurScanner must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatCode93Configuration = BarcodeFormatCode93Configuration;
/**
Code 128 barcode configuration. Add to scanner configuration to scan Code 128 barcodes.
*/
class BarcodeFormatCode128Configuration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCode128Configuration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCode128Configuration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        If true, barcodes with heavy blur or from far distance are tried to scan.
        
        Default is true
        */
        this.enableOneDBlurScanner = true;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.enableOneDBlurScanner !== undefined) {
            this.enableOneDBlurScanner = source.enableOneDBlurScanner;
            if (!TypeVerification.verifyBoolean(this.enableOneDBlurScanner)) {
                throw TypeError('enableOneDBlurScanner must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatCode128Configuration = BarcodeFormatCode128Configuration;
/**
Configuration for all 2-of-5 barcode types (except Interleaved 2-of-5, which is handled by ItfConfig).
Add to scanner configuration to scan Code 25, IATA 2-of-5 and Industrial 2-of-5 barcodes.

Industrial 2-of-5 barcodes are a subset of Code 25 barcodes.
Any valid Industrial 2-of-5 barcode is also a valid Code 25 barcode.
*/
class BarcodeFormatCode2Of5Configuration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCode2Of5Configuration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCode2Of5Configuration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        If true, the check digits are stripped from the result.
        
        Default is false
        */
        this.stripCheckDigits = false;
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        If true, scan IATA 2-of-5 barcodes. If useIATA2OF5Checksum is true, only barcodes with a valid checksum are returned.
        
        Default is true
        */
        this.iata2of5 = true;
        /**
        If true, scan and return Code 25 (Code 2-of-5) barcodes. If industrial2of5 is also true, then valid Industrial 2-of-5 barcodes will preferentially be returned as such, instead of Code 25.
        
        Default is false
        */
        this.code25 = false;
        /**
        If true, scan and return valid Industrial 2-of-5 barcodes. If false, but code25 is true, Industrial 2-of-5 barcodes will be returned as Code 25 barcodes.
        
        Default is true
        */
        this.industrial2of5 = true;
        /**
        If true, return IATA_2_OF_5 barcodes only if they have a valid check digit.
        
        Default is true
        */
        this.useIATA2OF5Checksum = true;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.stripCheckDigits !== undefined) {
            this.stripCheckDigits = source.stripCheckDigits;
            if (!TypeVerification.verifyBoolean(this.stripCheckDigits)) {
                throw TypeError('stripCheckDigits must be of type boolean');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.iata2of5 !== undefined) {
            this.iata2of5 = source.iata2of5;
            if (!TypeVerification.verifyBoolean(this.iata2of5)) {
                throw TypeError('iata2of5 must be of type boolean');
            }
        }
        if (source.code25 !== undefined) {
            this.code25 = source.code25;
            if (!TypeVerification.verifyBoolean(this.code25)) {
                throw TypeError('code25 must be of type boolean');
            }
        }
        if (source.industrial2of5 !== undefined) {
            this.industrial2of5 = source.industrial2of5;
            if (!TypeVerification.verifyBoolean(this.industrial2of5)) {
                throw TypeError('industrial2of5 must be of type boolean');
            }
        }
        if (source.useIATA2OF5Checksum !== undefined) {
            this.useIATA2OF5Checksum = source.useIATA2OF5Checksum;
            if (!TypeVerification.verifyBoolean(this.useIATA2OF5Checksum)) {
                throw TypeError('useIATA2OF5Checksum must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatCode2Of5Configuration = BarcodeFormatCode2Of5Configuration;
/**
GS1 DataBar barcode configuration. Add to scanner configuration to scan GS1 DataBar-14, GS1 DataBar-14 Truncated, GS1 DataBar-14 Stacked and GS1 DataBar-14 Stacked Omnidirectional barcodes.
*/
class BarcodeFormatDataBarConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatDataBarConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatDataBarConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
    }
}
exports.BarcodeFormatDataBarConfiguration = BarcodeFormatDataBarConfiguration;
/**
GS1 DataBar Expanded barcode configuration. Add to scanner configuration to scan GS1 DataBar Expanded and GS1 DataBar Expanded Stacked barcodes.
*/
class BarcodeFormatDataBarExpandedConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatDataBarExpandedConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatDataBarExpandedConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
    }
}
exports.BarcodeFormatDataBarExpandedConfiguration = BarcodeFormatDataBarExpandedConfiguration;
/**
GS1 DataBar Limited barcode configuration. Add to scanner configuration to scan GS1 DataBar Limited barcodes.
*/
class BarcodeFormatDataBarLimitedConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatDataBarLimitedConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatDataBarLimitedConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
    }
}
exports.BarcodeFormatDataBarLimitedConfiguration = BarcodeFormatDataBarLimitedConfiguration;
/**
ITF (Interleaved 2-of-5) barcode configuration. Add to scanner configuration to scan Interleaved 2-of-5 (ITF) barcodes.
*/
class BarcodeFormatItfConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatItfConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatITFConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        If true, barcodes with heavy blur or from far distance are tried to scan.
        
        Default is true
        */
        this.enableOneDBlurScanner = true;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.enableOneDBlurScanner !== undefined) {
            this.enableOneDBlurScanner = source.enableOneDBlurScanner;
            if (!TypeVerification.verifyBoolean(this.enableOneDBlurScanner)) {
                throw TypeError('enableOneDBlurScanner must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatItfConfiguration = BarcodeFormatItfConfiguration;
/** @hidden */
exports.MsiPlesseyChecksumAlgorithmValues = [
    "MOD_10",
    "MOD_11_IBM",
    "MOD_11_NCR",
    "MOD_10_10",
    "MOD_11_10_IBM",
    "MOD_11_10_NCR"
];
/**
MSI Plessey barcode configuration. Add to scanner configuration to scan MSI Plessey barcodes.
*/
class BarcodeFormatMsiPlesseyConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatMsiPlesseyConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatMSIPlesseyConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        If true, the check digits are stripped from the result.
        
        Default is false
        */
        this.stripCheckDigits = false;
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        If true, barcodes with heavy blur or from far distance are tried to scan.
        
        Default is true
        */
        this.enableOneDBlurScanner = true;
        /**
        List of MSI Plessey checksum algorithms to apply during scanning.
        A barcode is considered valid if it passes any of the checksum algorithms in the list.
        If the list is empty, no checksum validation is performed.
        */
        this.checksumAlgorithms = ["MOD_10"];
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.stripCheckDigits !== undefined) {
            this.stripCheckDigits = source.stripCheckDigits;
            if (!TypeVerification.verifyBoolean(this.stripCheckDigits)) {
                throw TypeError('stripCheckDigits must be of type boolean');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.enableOneDBlurScanner !== undefined) {
            this.enableOneDBlurScanner = source.enableOneDBlurScanner;
            if (!TypeVerification.verifyBoolean(this.enableOneDBlurScanner)) {
                throw TypeError('enableOneDBlurScanner must be of type boolean');
            }
        }
        if (source.checksumAlgorithms !== undefined) {
            this.checksumAlgorithms = source.checksumAlgorithms.map((it) => { return it; });
            if (!TypeVerification.listVerifier(TypeVerification.enumVerifier(exports.MsiPlesseyChecksumAlgorithmValues))(this.checksumAlgorithms)) {
                throw TypeError('checksumAlgorithms must be of type MsiPlesseyChecksumAlgorithm[]');
            }
        }
    }
}
exports.BarcodeFormatMsiPlesseyConfiguration = BarcodeFormatMsiPlesseyConfiguration;
/**
UPC/EAN barcode configuration. Add to scanner configuration to scan EAN-8, EAN-13, UPC-E and UPC-A barcodes.
*/
class BarcodeFormatUpcEanConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatUpcEanConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatUpcEanConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        If true, the check digits are stripped from the result.
        
        Default is false
        */
        this.stripCheckDigits = false;
        /**
        If true, barcodes with heavy blur or from far distance are tried to scan.
        
        Default is true
        */
        this.enableOneDBlurScanner = true;
        /**
        If true, scan and return valid EAN-8 barcodes.
        
        Default is true
        */
        this.ean8 = true;
        /**
        If true, scan and return valid EAN-13 barcodes.
        
        Default is true
        */
        this.ean13 = true;
        /**
        If true, scan and return valid UPC-A barcodes. If false, but ean13 is true, then UPC-A barcodes will be returned as EAN-13 barcodes.
        
        Default is true
        */
        this.upca = true;
        /**
        If true, scan and return valid UPC-E barcodes.
        
        Default is true
        */
        this.upce = true;
        /**
        Behavior when scanning UPC/EAN barcodes with EAN-2 or EAN-5 extensions.
        
        Default is ALLOW_ANY
        */
        this.extensions = "ALLOW_ANY";
        /**
        In case that ean/upc both with and without extensions are allowed,
        we require confirmations to consider a standalone EAN or UPC (i.e. without extension) as valid.
        
        Default is 1
        */
        this.minimumStandaloneConfirmationCount = 1;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.stripCheckDigits !== undefined) {
            this.stripCheckDigits = source.stripCheckDigits;
            if (!TypeVerification.verifyBoolean(this.stripCheckDigits)) {
                throw TypeError('stripCheckDigits must be of type boolean');
            }
        }
        if (source.enableOneDBlurScanner !== undefined) {
            this.enableOneDBlurScanner = source.enableOneDBlurScanner;
            if (!TypeVerification.verifyBoolean(this.enableOneDBlurScanner)) {
                throw TypeError('enableOneDBlurScanner must be of type boolean');
            }
        }
        if (source.ean8 !== undefined) {
            this.ean8 = source.ean8;
            if (!TypeVerification.verifyBoolean(this.ean8)) {
                throw TypeError('ean8 must be of type boolean');
            }
        }
        if (source.ean13 !== undefined) {
            this.ean13 = source.ean13;
            if (!TypeVerification.verifyBoolean(this.ean13)) {
                throw TypeError('ean13 must be of type boolean');
            }
        }
        if (source.upca !== undefined) {
            this.upca = source.upca;
            if (!TypeVerification.verifyBoolean(this.upca)) {
                throw TypeError('upca must be of type boolean');
            }
        }
        if (source.upce !== undefined) {
            this.upce = source.upce;
            if (!TypeVerification.verifyBoolean(this.upce)) {
                throw TypeError('upce must be of type boolean');
            }
        }
        if (source.extensions !== undefined) {
            this.extensions = source.extensions;
            if (!TypeVerification.enumVerifier(BarcodeTypes_3.UpcEanExtensionBehaviorValues)(this.extensions)) {
                throw TypeError('extensions must be of type UpcEanExtensionBehavior');
            }
        }
        if (source.minimumStandaloneConfirmationCount !== undefined) {
            this.minimumStandaloneConfirmationCount = source.minimumStandaloneConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimumStandaloneConfirmationCount)) {
                throw TypeError('minimumStandaloneConfirmationCount must be of type number');
            }
        }
    }
}
exports.BarcodeFormatUpcEanConfiguration = BarcodeFormatUpcEanConfiguration;
/**
PharmaCode barcode configuration. Add to scanner configuration to scan linear (1D) Laetus Pharmacode barcodes. Two-track PharmaCode scanning is configured separately through the PharmaCodeTwoTrackConfig class.
*/
class BarcodeFormatPharmaCodeConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatPharmaCodeConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatPharmaCodeConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        Minimum value for PharmaCode. Very low values are likely to produce more false positives.
        
        Default is 16
        */
        this.minimumValue = 16;
        /**
        If true, a result consisting of only narrow bars is accepted as valid. The specification does not recommend such barcodes. Default is false.
        
        Default is false
        */
        this.allowNarrowBarsOnly = false;
        /**
        If true, a result consisting of only wide bars is accepted as valid. The specification does not recommend such barcodes. Default is false.
        
        Default is false
        */
        this.allowWideBarsOnly = false;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.minimumValue !== undefined) {
            this.minimumValue = source.minimumValue;
            if (!TypeVerification.verifyInt(this.minimumValue)) {
                throw TypeError('minimumValue must be of type number');
            }
        }
        if (source.allowNarrowBarsOnly !== undefined) {
            this.allowNarrowBarsOnly = source.allowNarrowBarsOnly;
            if (!TypeVerification.verifyBoolean(this.allowNarrowBarsOnly)) {
                throw TypeError('allowNarrowBarsOnly must be of type boolean');
            }
        }
        if (source.allowWideBarsOnly !== undefined) {
            this.allowWideBarsOnly = source.allowWideBarsOnly;
            if (!TypeVerification.verifyBoolean(this.allowWideBarsOnly)) {
                throw TypeError('allowWideBarsOnly must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatPharmaCodeConfiguration = BarcodeFormatPharmaCodeConfiguration;
/** @internal */
var BarcodeFormatTwoDConfigurationBase;
(function (BarcodeFormatTwoDConfigurationBase) {
    /** @internal */
    function from(source) {
        const _type = source["_type"];
        switch (_type) {
            case "BarcodeFormatAztecConfiguration": return new BarcodeFormatAztecConfiguration(source);
            case "BarcodeFormatQRCodeConfiguration": return new BarcodeFormatQrCodeConfiguration(source);
            case "BarcodeFormatPDF417Configuration": return new BarcodeFormatPdf417Configuration(source);
            case "BarcodeFormatMicroPDF417Configuration": return new BarcodeFormatMicroPdf417Configuration(source);
            case "BarcodeFormatDataMatrixConfiguration": return new BarcodeFormatDataMatrixConfiguration(source);
            case "BarcodeFormatMaxiCodeConfiguration": return new BarcodeFormatMaxiCodeConfiguration(source);
            default: throw new Error(`Unknown child class name: ${_type}`);
        }
    }
    BarcodeFormatTwoDConfigurationBase.from = from;
})(BarcodeFormatTwoDConfigurationBase || (exports.BarcodeFormatTwoDConfigurationBase = BarcodeFormatTwoDConfigurationBase = {}));
/**
Aztec configuration. Add to scanner configuration to scan Aztec codes.
*/
class BarcodeFormatAztecConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatAztecConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatAztecConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
    }
}
exports.BarcodeFormatAztecConfiguration = BarcodeFormatAztecConfiguration;
/**
QR Code configuration. Add to scanner configuration to scan QR codes, Micro QR codes and rectangular Micro QR (rMQR) codes.
*/
class BarcodeFormatQrCodeConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatQrCodeConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatQRCodeConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        /**
        If true, the barcode scanner will only return barcodes that pass the strict validation checks according to standards to avoid false positives.
        
        Default is true
        */
        this.strictMode = true;
        /**
        If true, scan and return QR codes.
        
        Default is true
        */
        this.qr = true;
        /**
        If true, scan and return Micro QR codes.
        
        Default is false
        */
        this.microQr = false;
        /**
        If true, scan and return rectangular Micro QR (rMQR) codes.
        
        Default is false
        */
        this.rmqr = false;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
        if (source.strictMode !== undefined) {
            this.strictMode = source.strictMode;
            if (!TypeVerification.verifyBoolean(this.strictMode)) {
                throw TypeError('strictMode must be of type boolean');
            }
        }
        if (source.qr !== undefined) {
            this.qr = source.qr;
            if (!TypeVerification.verifyBoolean(this.qr)) {
                throw TypeError('qr must be of type boolean');
            }
        }
        if (source.microQr !== undefined) {
            this.microQr = source.microQr;
            if (!TypeVerification.verifyBoolean(this.microQr)) {
                throw TypeError('microQr must be of type boolean');
            }
        }
        if (source.rmqr !== undefined) {
            this.rmqr = source.rmqr;
            if (!TypeVerification.verifyBoolean(this.rmqr)) {
                throw TypeError('rmqr must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatQrCodeConfiguration = BarcodeFormatQrCodeConfiguration;
/**
PDF417 configuration. Add to scanner configuration to scan PDF417 codes.
*/
class BarcodeFormatPdf417Configuration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatPdf417Configuration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatPDF417Configuration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        /**
        If true, the barcode scanner will only return barcodes that pass the strict validation checks according to standards to avoid false positives.
        
        Default is true
        */
        this.strictMode = true;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
        if (source.strictMode !== undefined) {
            this.strictMode = source.strictMode;
            if (!TypeVerification.verifyBoolean(this.strictMode)) {
                throw TypeError('strictMode must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatPdf417Configuration = BarcodeFormatPdf417Configuration;
/**
MicroPDF417 configuration. Add to scanner configuration to scan MicroPDF417 codes.
*/
class BarcodeFormatMicroPdf417Configuration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatMicroPdf417Configuration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatMicroPDF417Configuration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        /**
        If true, the barcode scanner will only return barcodes that pass the strict validation checks according to standards to avoid false positives.
        
        Default is true
        */
        this.strictMode = true;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
        if (source.strictMode !== undefined) {
            this.strictMode = source.strictMode;
            if (!TypeVerification.verifyBoolean(this.strictMode)) {
                throw TypeError('strictMode must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatMicroPdf417Configuration = BarcodeFormatMicroPdf417Configuration;
/**
DataMatrix configuration. Add to scanner configuration to scan DataMatrix and DataMatrix rectangular extensions (DMRE) codes.
*/
class BarcodeFormatDataMatrixConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatDataMatrixConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatDataMatrixConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
    }
}
exports.BarcodeFormatDataMatrixConfiguration = BarcodeFormatDataMatrixConfiguration;
/**
MaxiCode configuration. Add to scanner configuration to scan MaxiCode codes.
*/
class BarcodeFormatMaxiCodeConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatMaxiCodeConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatMaxiCodeConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatMaxiCodeConfiguration = BarcodeFormatMaxiCodeConfiguration;
/** @internal */
var BarcodeFormatFourStateConfigurationBase;
(function (BarcodeFormatFourStateConfigurationBase) {
    /** @internal */
    function from(source) {
        const _type = source["_type"];
        switch (_type) {
            case "BarcodeFormatAustraliaPostConfiguration": return new BarcodeFormatAustraliaPostConfiguration(source);
            case "BarcodeFormatJapanPostConfiguration": return new BarcodeFormatJapanPostConfiguration(source);
            case "BarcodeFormatRoyalMailConfiguration": return new BarcodeFormatRoyalMailConfiguration(source);
            case "BarcodeFormatRoyalTNTPostConfiguration": return new BarcodeFormatRoyalTntPostConfiguration(source);
            case "BarcodeFormatUSPSIntelligentMailConfiguration": return new BarcodeFormatUspsIntelligentMailConfiguration(source);
            case "BarcodeFormatPharmaCodeTwoTrackConfiguration": return new BarcodeFormatPharmaCodeTwoTrackConfiguration(source);
            default: throw new Error(`Unknown child class name: ${_type}`);
        }
    }
    BarcodeFormatFourStateConfigurationBase.from = from;
})(BarcodeFormatFourStateConfigurationBase || (exports.BarcodeFormatFourStateConfigurationBase = BarcodeFormatFourStateConfigurationBase = {}));
/** @hidden */
exports.AustraliaPostCustomerFormatValues = [
    "NUMERIC",
    "ALPHA_NUMERIC"
];
/**
Australia Post barcode configuration. Add to scanner configuration to scan Australia Post barcodes.
*/
class BarcodeFormatAustraliaPostConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatAustraliaPostConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatAustraliaPostConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Can be either numeric or alphanumeric and is only relevant for Format Codes 59 and 62.
        
        Default is ALPHA_NUMERIC
        */
        this.australiaPostCustomerFormat = "ALPHA_NUMERIC";
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.australiaPostCustomerFormat !== undefined) {
            this.australiaPostCustomerFormat = source.australiaPostCustomerFormat;
            if (!TypeVerification.enumVerifier(exports.AustraliaPostCustomerFormatValues)(this.australiaPostCustomerFormat)) {
                throw TypeError('australiaPostCustomerFormat must be of type AustraliaPostCustomerFormat');
            }
        }
    }
}
exports.BarcodeFormatAustraliaPostConfiguration = BarcodeFormatAustraliaPostConfiguration;
/**
Japan Post barcode configuration. Add to scanner configuration to scan Japan Post barcodes.
*/
class BarcodeFormatJapanPostConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatJapanPostConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatJapanPostConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatJapanPostConfiguration = BarcodeFormatJapanPostConfiguration;
/**
Royal Mail barcode configuration. Add to scanner configuration to scan Royal Mail (a.k.a. RM4SCC, CBC, BPO 4-State) barcodes.
*/
class BarcodeFormatRoyalMailConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatRoyalMailConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatRoyalMailConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        If true, the check digits are stripped from the result.
        
        Default is false
        */
        this.stripCheckDigits = false;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.stripCheckDigits !== undefined) {
            this.stripCheckDigits = source.stripCheckDigits;
            if (!TypeVerification.verifyBoolean(this.stripCheckDigits)) {
                throw TypeError('stripCheckDigits must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatRoyalMailConfiguration = BarcodeFormatRoyalMailConfiguration;
/**
Royal TNT Post barcode configuration. Add to scanner configuration to scan Royal TNT Post (a.k.a. KIX, Klant IndeX) barcodes.
*/
class BarcodeFormatRoyalTntPostConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatRoyalTntPostConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatRoyalTNTPostConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatRoyalTntPostConfiguration = BarcodeFormatRoyalTntPostConfiguration;
/**
USPS Intelligent Mail barcode configuration. Add to scanner configuration to scan USPS Intelligent Mail (a.k.a. USPS OneCode, USPS-STD-11) barcodes.
*/
class BarcodeFormatUspsIntelligentMailConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatUspsIntelligentMailConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatUSPSIntelligentMailConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatUspsIntelligentMailConfiguration = BarcodeFormatUspsIntelligentMailConfiguration;
/**
PHARMA_CODE_TWO_TRACK barcode configuration. Add to scanner configuration to scan Laetus two-track PharmaCode barcodes. Linear (i.e. one-track) PharmaCode scanning is configured separately through the PharmaCodeConfig class.
*/
class BarcodeFormatPharmaCodeTwoTrackConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatPharmaCodeTwoTrackConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatPharmaCodeTwoTrackConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum value for PHARMA_CODE_TWO_TRACK. Very low values are likely to produce more false positives.
        
        Default is 364
        */
        this.minimumValue = 364;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimumValue !== undefined) {
            this.minimumValue = source.minimumValue;
            if (!TypeVerification.verifyInt(this.minimumValue)) {
                throw TypeError('minimumValue must be of type number');
            }
        }
    }
}
exports.BarcodeFormatPharmaCodeTwoTrackConfiguration = BarcodeFormatPharmaCodeTwoTrackConfiguration;
/**
GS1 Composite configuration. Add to scanner configuration to scan GS1 Composite barcodes.

When not enabled, the individual parts of GS1 Composite barcodes will be returned as separate items,
if the respective formats are enabled (UPC-A, Code 128, MicroPDF417, PDF417).
Those barcode items will have their isGS1CompositePart field set to true.
If GS1 Composite scanning is enabled, the individual parts are never returned, even if their respective formats
are enabled, except when the linear component is a UPC or EAN barcode. In that case the linear component might still
occasionally be returned as a separate result. If this situation needs to be avoided, remove the UPC and EAN formats from
the enabled formats.

If GS1 Composite scanning is disabled, but GS1 message validation is enabled, then the parts of the composite barcode may
fail validation and be rejected.
*/
class BarcodeFormatGs1CompositeConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatGs1CompositeConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatGS1CompositeConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        /**
        If true, the barcode scanner will only return barcodes that pass the strict validation checks according to standards to avoid false positives.
        
        Default is true
        */
        this.strictMode = true;
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
        if (source.strictMode !== undefined) {
            this.strictMode = source.strictMode;
            if (!TypeVerification.verifyBoolean(this.strictMode)) {
                throw TypeError('strictMode must be of type boolean');
            }
        }
    }
}
exports.BarcodeFormatGs1CompositeConfiguration = BarcodeFormatGs1CompositeConfiguration;
/**
Convenience configuration for enabling the scanning of multiple linear (1D) barcode formats with a common configuration.
Add to scanner configuration to enable and configure the scanning of multiple linear (1D) barcode formats.

The given configuration will be applied to all enabled barcode formats, if they support it.
You can override the common configuration for individual barcode formats by additionally adding
their specific configuration to the scanner's configuration.
*/
class BarcodeFormatCommonOneDConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCommonOneDConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCommonOneDConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        If true, the check digits are stripped from the result.
        
        Default is false
        */
        this.stripCheckDigits = false;
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        /**
        If true, barcodes with heavy blur or from far distance are tried to scan.
        
        Default is true
        */
        this.enableOneDBlurScanner = true;
        /**
        List of linear (1D) barcode formats to scan.
        */
        this.formats = ["CODABAR", "CODE_11", "CODE_25", "CODE_32", "CODE_39", "CODE_93", "CODE_128", "DATABAR", "DATABAR_EXPANDED", "DATABAR_LIMITED", "EAN_8", "EAN_13", "IATA_2_OF_5", "INDUSTRIAL_2_OF_5", "ITF", "MSI_PLESSEY", "PHARMA_CODE", "PZN_7", "PZN_8", "UPC_A", "UPC_E"];
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.stripCheckDigits !== undefined) {
            this.stripCheckDigits = source.stripCheckDigits;
            if (!TypeVerification.verifyBoolean(this.stripCheckDigits)) {
                throw TypeError('stripCheckDigits must be of type boolean');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
        if (source.enableOneDBlurScanner !== undefined) {
            this.enableOneDBlurScanner = source.enableOneDBlurScanner;
            if (!TypeVerification.verifyBoolean(this.enableOneDBlurScanner)) {
                throw TypeError('enableOneDBlurScanner must be of type boolean');
            }
        }
        if (source.formats !== undefined) {
            this.formats = source.formats.map((it) => { return it; });
            if (!TypeVerification.listVerifier(TypeVerification.enumVerifier(BarcodeTypes_1.BarcodeFormatValues))(this.formats)) {
                throw TypeError('formats must be of type BarcodeFormat[]');
            }
        }
    }
}
exports.BarcodeFormatCommonOneDConfiguration = BarcodeFormatCommonOneDConfiguration;
/**
Convenience configuration for enabling the scanning of multiple 2D codes with a common configuration.
Add to scanner configuration to enable and configure the scanning of multiple 2D codes.

The given configuration will be applied to all enabled barcode formats, if they support it.
You can override the common configuration for individual barcode formats by additionally adding
their specific configuration to the scanner's configuration.
*/
class BarcodeFormatCommonTwoDConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCommonTwoDConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCommonTwoDConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        /**
        If true, the barcode scanner will only return barcodes that pass the strict validation checks according to standards to avoid false positives.
        
        Default is true
        */
        this.strictMode = true;
        /**
        List of 2D codes to scan.
        */
        this.formats = ["AZTEC", "DATA_MATRIX", "MAXI_CODE", "MICRO_QR_CODE", "MICRO_PDF_417", "PDF_417", "QR_CODE", "RMQR_CODE"];
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
        if (source.strictMode !== undefined) {
            this.strictMode = source.strictMode;
            if (!TypeVerification.verifyBoolean(this.strictMode)) {
                throw TypeError('strictMode must be of type boolean');
            }
        }
        if (source.formats !== undefined) {
            this.formats = source.formats.map((it) => { return it; });
            if (!TypeVerification.listVerifier(TypeVerification.enumVerifier(BarcodeTypes_1.BarcodeFormatValues))(this.formats)) {
                throw TypeError('formats must be of type BarcodeFormat[]');
            }
        }
    }
}
exports.BarcodeFormatCommonTwoDConfiguration = BarcodeFormatCommonTwoDConfiguration;
/**
Convenience configuration for enabling the scanning of multiple four-state barcode formats with a common configuration.
Add to scanner configuration to enable and configure the scanning of multiple four-state codes.

The given configuration will be applied to all enabled barcode formats, if they support it.
You can override the common configuration for individual barcode formats by additionally adding
their specific configuration to the scanner's configuration.
*/
class BarcodeFormatCommonFourStateConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCommonFourStateConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCommonFourStateConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        List of four-state barcode formats to scan.
        */
        this.formats = ["AUSTRALIA_POST", "JAPAN_POST", "ROYAL_MAIL", "ROYAL_TNT_POST", "USPS_INTELLIGENT_MAIL"];
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.formats !== undefined) {
            this.formats = source.formats.map((it) => { return it; });
            if (!TypeVerification.listVerifier(TypeVerification.enumVerifier(BarcodeTypes_1.BarcodeFormatValues))(this.formats)) {
                throw TypeError('formats must be of type BarcodeFormat[]');
            }
        }
    }
}
exports.BarcodeFormatCommonFourStateConfiguration = BarcodeFormatCommonFourStateConfiguration;
/**
Convenience configuration for enabling the scanning of multiple barcode formats with a common configuration.
Add to scanner configuration to enable and configure the scanning of multiple barcodes.

The given configuration will be applied to all enabled barcode formats, if they support it.
You can override the common configuration for individual barcode formats by additionally adding
their specific configuration to the scanner's configuration.
*/
class BarcodeFormatCommonConfiguration {
    /** @param source {@displayType `DeepPartial<BarcodeFormatCommonConfiguration>`} */
    constructor(source = {}) {
        this._type = "BarcodeFormatCommonConfiguration";
        /**
        Regular expression filter for barcode text. If the barcode text does not match the regular expression, it will not be scanned. The default is an empty string (setting is turned off).
        
        Default is ""
        */
        this.regexFilter = "";
        /**
        Minimum acceptable value of a result BarcodeItem's sizeScore (between 0 and 1).
        Barcodes with a sizeScore less than this value will not be scanned.
        When set to 0, barcodes are returned no matter what their size is.
        
        Default is 0
        */
        this.minimumSizeScore = 0.0;
        /**
        If true, we process the barcode scanning with an artificial quiet zone that we add to the input image.
        This flag should be used to scan sharp crops of a barcode in single-shot mode.
        
        Default is false
        */
        this.addAdditionalQuietZone = false;
        /**
        Minimum quiet zone size on the left and right sides of a 1D barcode, measured in number of modules.
        The quiet zone is only for 1D barcodes with weak start/stop patterns, i.e. Iata2Of5, Industrial2Of5, Code25, Itf, Codabar, MsiPlessey and Code11.
        
        Default is 6
        */
        this.minimum1DQuietZoneSize = 6;
        /**
        In case of live scanning, we require confirmations across frames to consider the one-d barcode as valid.
        
        Default is 1
        */
        this.minimum1DConfirmationCount = 1;
        /**
        If true, the check digits are stripped from the result.
        
        Default is false
        */
        this.stripCheckDigits = false;
        /**
        Minimum text length. Applied only to linear barcode formats that allow variable length.
        
        Default is 1
        */
        this.minimumTextLength = 1;
        /**
        Maximum text length. 0 implies no maximum. Applied only to linear barcode formats that allow variable length.
        
        Default is 0
        */
        this.maximumTextLength = 0;
        /**
        GS1 message handling options.
        
        Default is PARSE
        */
        this.gs1Handling = "PARSE";
        /**
        If true, the barcode scanner will only return barcodes that pass the strict validation checks according to standards to avoid false positives.
        
        Default is true
        */
        this.strictMode = true;
        /**
        If true, barcodes with heavy blur or from far distance are tried to scan.
        
        Default is true
        */
        this.enableOneDBlurScanner = true;
        /**
        List of barcode formats to scan. By default, the most commonly used formats are enabled.
        */
        this.formats = ["AZTEC", "CODABAR", "CODE_39", "CODE_93", "CODE_128", "DATA_MATRIX", "DATABAR", "DATABAR_EXPANDED", "DATABAR_LIMITED", "EAN_13", "EAN_8", "ITF", "PDF_417", "QR_CODE", "UPC_A", "UPC_E"];
        if (source.regexFilter !== undefined) {
            this.regexFilter = source.regexFilter;
            if (!TypeVerification.verifyString(this.regexFilter)) {
                throw TypeError('regexFilter must be of type string');
            }
        }
        if (source.minimumSizeScore !== undefined) {
            this.minimumSizeScore = source.minimumSizeScore;
            if (!TypeVerification.verifyDouble(this.minimumSizeScore)) {
                throw TypeError('minimumSizeScore must be of type number');
            }
        }
        if (source.addAdditionalQuietZone !== undefined) {
            this.addAdditionalQuietZone = source.addAdditionalQuietZone;
            if (!TypeVerification.verifyBoolean(this.addAdditionalQuietZone)) {
                throw TypeError('addAdditionalQuietZone must be of type boolean');
            }
        }
        if (source.minimum1DQuietZoneSize !== undefined) {
            this.minimum1DQuietZoneSize = source.minimum1DQuietZoneSize;
            if (!TypeVerification.verifyInt(this.minimum1DQuietZoneSize)) {
                throw TypeError('minimum1DQuietZoneSize must be of type number');
            }
        }
        if (source.minimum1DConfirmationCount !== undefined) {
            this.minimum1DConfirmationCount = source.minimum1DConfirmationCount;
            if (!TypeVerification.verifyInt(this.minimum1DConfirmationCount)) {
                throw TypeError('minimum1DConfirmationCount must be of type number');
            }
        }
        if (source.stripCheckDigits !== undefined) {
            this.stripCheckDigits = source.stripCheckDigits;
            if (!TypeVerification.verifyBoolean(this.stripCheckDigits)) {
                throw TypeError('stripCheckDigits must be of type boolean');
            }
        }
        if (source.minimumTextLength !== undefined) {
            this.minimumTextLength = source.minimumTextLength;
            if (!TypeVerification.verifyInt(this.minimumTextLength)) {
                throw TypeError('minimumTextLength must be of type number');
            }
        }
        if (source.maximumTextLength !== undefined) {
            this.maximumTextLength = source.maximumTextLength;
            if (!TypeVerification.verifyInt(this.maximumTextLength)) {
                throw TypeError('maximumTextLength must be of type number');
            }
        }
        if (source.gs1Handling !== undefined) {
            this.gs1Handling = source.gs1Handling;
            if (!TypeVerification.enumVerifier(BarcodeTypes_2.Gs1HandlingValues)(this.gs1Handling)) {
                throw TypeError('gs1Handling must be of type Gs1Handling');
            }
        }
        if (source.strictMode !== undefined) {
            this.strictMode = source.strictMode;
            if (!TypeVerification.verifyBoolean(this.strictMode)) {
                throw TypeError('strictMode must be of type boolean');
            }
        }
        if (source.enableOneDBlurScanner !== undefined) {
            this.enableOneDBlurScanner = source.enableOneDBlurScanner;
            if (!TypeVerification.verifyBoolean(this.enableOneDBlurScanner)) {
                throw TypeError('enableOneDBlurScanner must be of type boolean');
            }
        }
        if (source.formats !== undefined) {
            this.formats = source.formats.map((it) => { return it; });
            if (!TypeVerification.listVerifier(TypeVerification.enumVerifier(BarcodeTypes_1.BarcodeFormatValues))(this.formats)) {
                throw TypeError('formats must be of type BarcodeFormat[]');
            }
        }
    }
}
exports.BarcodeFormatCommonConfiguration = BarcodeFormatCommonConfiguration;
