"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/CreditCardTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CreditCardScannerConfiguration = exports.CreditCardScanningResult = exports.CreditCardScanningStatusValues = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const GenericDocument_1 = require("../generic_document/GenericDocument");
const CommonTypes_1 = require("../common/CommonTypes");
const FrameAccumulationTypes_1 = require("../frame_accumulation/FrameAccumulationTypes");
/** @hidden */
exports.CreditCardScanningStatusValues = [
    "SUCCESS",
    "ERROR_NOTHING_FOUND",
    "INCOMPLETE"
];
/**
Contains the result of running the credit card scanner.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getCreditCardScanningResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class CreditCardScanningResult {
    /** @param source {@displayType `DeepPartial<CreditCardScanningResult>`} */
    constructor(source = {}) {
        this._releasePromise = null;
        if (source.detectionStatus !== undefined) {
            this.detectionStatus = source.detectionStatus;
        }
        else {
            throw new Error("detectionStatus must be present in constructor argument");
        }
        if (source.scanningStatus !== undefined) {
            this.scanningStatus = source.scanningStatus;
        }
        else {
            throw new Error("scanningStatus must be present in constructor argument");
        }
        if (source.creditCard !== undefined) {
            this.creditCard = source.creditCard != null ? new GenericDocument_1.GenericDocument(source.creditCard) : null;
        }
        else {
            throw new Error("creditCard must be present in constructor argument");
        }
        if (source.quad !== undefined) {
            this.quad = source.quad.map((it) => { return { "x": it.x, "y": it.y }; });
        }
        else {
            throw new Error("quad must be present in constructor argument");
        }
        if (source.quadNormalized !== undefined) {
            this.quadNormalized = source.quadNormalized.map((it) => { return { "x": it.x, "y": it.y }; });
        }
        else {
            throw new Error("quadNormalized must be present in constructor argument");
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            {
                if (this.creditCard != null) {
                    promises.push(this.creditCard.release());
                }
            }
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.CreditCardScanningResult = CreditCardScanningResult;
/**
Configuration for the credit card scanner.
*/
class CreditCardScannerConfiguration {
    /** @param source {@displayType `DeepPartial<CreditCardScannerConfiguration>`} */
    constructor(source = {}) {
        /**
        If true, the document detector will be used to find where the credit card is in the input image.
        If false, the scanner will assume that the credit card has been pre-cropped and takes the entirety of the input image.
        
        Default is true
        */
        this.useDocumentDetector = true;
        /**
        Whether the expiry date is required for a successful scan.
        
        Default is true
        */
        this.requireExpiryDate = true;
        /**
        Whether the cardholder name is required for a successful scan.
        
        Default is true
        */
        this.requireCardholderName = true;
        /**
        Configuration for how to accumulate results.
        */
        this.resultAccumulationConfig = new FrameAccumulationTypes_1.ResultAccumulationConfiguration({});
        /**
        If true, the credit card image will be returned in creditCard field of CreditCardScanningResult.
        
        Default is false
        */
        this.returnCreditCardImage = false;
        /**
        Scanners typically can produce better results from a single image if they are allowed to spend a longer time analyzing it.
        
        On the other hand, scanners can produce even better results if they can analyze multiple images of the same subject and cross-check and combine the results.
        The processing mode tells the scanner whether to optimize for single or multiple images of a subject.
        
        Default is AUTO
        */
        this.processingMode = "AUTO";
        if (source.useDocumentDetector !== undefined) {
            this.useDocumentDetector = source.useDocumentDetector;
            if (!TypeVerification.verifyBoolean(this.useDocumentDetector)) {
                throw TypeError('useDocumentDetector must be of type boolean');
            }
        }
        if (source.requireExpiryDate !== undefined) {
            this.requireExpiryDate = source.requireExpiryDate;
            if (!TypeVerification.verifyBoolean(this.requireExpiryDate)) {
                throw TypeError('requireExpiryDate must be of type boolean');
            }
        }
        if (source.requireCardholderName !== undefined) {
            this.requireCardholderName = source.requireCardholderName;
            if (!TypeVerification.verifyBoolean(this.requireCardholderName)) {
                throw TypeError('requireCardholderName must be of type boolean');
            }
        }
        if (source.resultAccumulationConfig !== undefined) {
            this.resultAccumulationConfig = new FrameAccumulationTypes_1.ResultAccumulationConfiguration(source.resultAccumulationConfig);
            if (!TypeVerification.typeVerifier(FrameAccumulationTypes_1.ResultAccumulationConfiguration)(this.resultAccumulationConfig)) {
                throw TypeError('resultAccumulationConfig must be of type ResultAccumulationConfiguration');
            }
        }
        if (source.returnCreditCardImage !== undefined) {
            this.returnCreditCardImage = source.returnCreditCardImage;
            if (!TypeVerification.verifyBoolean(this.returnCreditCardImage)) {
                throw TypeError('returnCreditCardImage must be of type boolean');
            }
        }
        if (source.processingMode !== undefined) {
            this.processingMode = source.processingMode;
            if (!TypeVerification.enumVerifier(CommonTypes_1.ProcessingModeValues)(this.processingMode)) {
                throw TypeError('processingMode must be of type ProcessingMode');
            }
        }
    }
}
exports.CreditCardScannerConfiguration = CreditCardScannerConfiguration;
