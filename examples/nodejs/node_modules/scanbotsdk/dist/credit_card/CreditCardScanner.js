"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/CreditCardScanner.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CreditCardScanner = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const CreditCardTypes_1 = require("../credit_card/CreditCardTypes");
const CreditCardTypes_2 = require("../credit_card/CreditCardTypes");
const ImageRef_1 = require("../image/ImageRef");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for credit card scanning.
 *
 * Required licence feature(s): CreditCardScanner.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await CreditCardScanner.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class CreditCardScanner {
    /**
    Initialize the credit card scanner.
    @param configuration - The configuration to use for the scanner.
    @returns Instance of CreditCardScanner.
    */
    static async create(configuration = new CreditCardTypes_1.CreditCardScannerConfiguration({})) {
        CreditCardScanner.performStaticInitialization();
        if (!TypeVerification.typeVerifier(CreditCardTypes_1.CreditCardScannerConfiguration)(configuration)) {
            throw TypeError('configuration must be of type CreditCardScannerConfiguration');
        }
        const nativeHandle = await CreditCardScanner.nativeCreditCardScannerInit(configuration).catch(Errors_1.remapError);
        return new CreditCardScanner(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeRun = addonNative.nativeCreditCardScannerRun.bind(this);
        this.nativeClearResult = addonNative.nativeCreditCardScannerClearResult.bind(this);
        CreditCardScanner.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            CreditCardScanner.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = CreditCardScanner.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return CreditCardScanner.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Scan the credit card in the given image.
    @param image - The image to scan.
    @returns The scanning result.
    */
    async run(image) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        const partial = await this.nativeRun(image).catch(Errors_1.remapError);
        return new CreditCardTypes_2.CreditCardScanningResult(partial);
    }
    /**
    Clears the accumulated results. Should be called after a document has been scanned to the customers
    * satisfaction to prevent a poisoned cache.
    *
    */
    async clearResult() {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        const partial = await this.nativeClearResult().catch(Errors_1.remapError);
        return partial;
    }
    static performStaticInitialization() {
        if (CreditCardScanner.staticInitializationPerformed) {
            return;
        }
        CreditCardScanner.staticInitializationPerformed = true;
        CreditCardScanner.nativeCreditCardScannerInit = addonNative.nativeCreditCardScannerInit.bind(CreditCardScanner);
        CreditCardScanner.nativeDispose = addonNative.nativeCreditCardScannerDispose;
    }
}
exports.CreditCardScanner = CreditCardScanner;
CreditCardScanner.registry = new FinalizationRegistry((nativeHandle) => {
    CreditCardScanner.nativeDispose(nativeHandle);
});
CreditCardScanner.staticInitializationPerformed = false;
