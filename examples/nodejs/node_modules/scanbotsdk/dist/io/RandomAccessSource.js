"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.RandomAccessSource = void 0;
const addon = require('../../native/scanbotsdk_nodejs.node');
const TypeVerification = __importStar(require("../support/TypeVerification"));
require("../support/Disposable");
const Errors_1 = require("../support/Errors");
/**
 RandomAccessSource is a wrapper around a native memory-mapped file or byte array.
 It is used to provide a random access interface to the underlying data.
 @example
 Since the instance of the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
 ```
 await using instance = await RandomAccessSource.fromPath('path/to/file.pdf');
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resources. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class RandomAccessSource {
    constructor(nativeHandle) {
        this.releasePromise = null;
        this.nativeHandle = nativeHandle;
        RandomAccessSource.registry.register(this, this.nativeHandle, this);
    }
    /**
     Constructs RandomAccessSource from the given file effectively creating a memory-mapping.
     @param path - Path to the file.
     @returns {@link RandomAccessSource} instance that provides random access to the file data.
     */
    static async fromPath(path) {
        RandomAccessSource.initializeStaticMethods();
        TypeVerification.assertType({ path }, TypeVerification.verifyString, 'string');
        return new RandomAccessSource(await RandomAccessSource.nativeRandomAccessSourceFromPath(path).catch(Errors_1.remapError));
    }
    /**
     Constructs RandomAccessSource from the given byte array.
     @param buffer - The byte array to use as the source.
     @returns {@link RandomAccessSource} instance that provides random access to the byte array data.
     */
    static async fromData(buffer) {
        RandomAccessSource.initializeStaticMethods();
        TypeVerification.assertType({ buffer }, TypeVerification.verifyByteArray, 'Uint8Array');
        return new RandomAccessSource(await RandomAccessSource.nativeRandomAccessSourceFromData(buffer).catch(Errors_1.remapError));
    }
    /**
     Releases the native resources associated with this instance.
     */
    dispose() {
        if (this.releasePromise === null) {
            const nativeHandle = this.nativeHandle;
            this.nativeHandle = null;
            RandomAccessSource.registry.unregister(this);
            this.releasePromise = RandomAccessSource.dispose(nativeHandle).catch(Errors_1.remapError);
        }
        return this.releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    static dispose(nativeHandle) {
        return RandomAccessSource.nativeRandomAccessSourceDispose(nativeHandle).catch(Errors_1.remapError);
    }
    static initializeStaticMethods() {
        if (RandomAccessSource.staticMethodsInitialized) {
            return;
        }
        RandomAccessSource.nativeRandomAccessSourceFromPath = addon.nativeRandomAccessSourceFromPath.bind(RandomAccessSource);
        RandomAccessSource.nativeRandomAccessSourceFromData = addon.nativeRandomAccessSourceFromData.bind(RandomAccessSource);
        RandomAccessSource.nativeRandomAccessSourceDispose = addon.nativeRandomAccessSourceDispose.bind(RandomAccessSource);
    }
    /** @internal */
    _nonPartiallyConstructibleMarker() {
    }
}
exports.RandomAccessSource = RandomAccessSource;
RandomAccessSource.registry = new FinalizationRegistry((nativeHandle) => {
    RandomAccessSource.dispose(nativeHandle);
});
RandomAccessSource.staticMethodsInitialized = false;
