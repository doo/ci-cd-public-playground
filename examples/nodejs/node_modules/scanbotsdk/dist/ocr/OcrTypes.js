"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/OCRTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Page = exports.Block = exports.Line = exports.Word = exports.Glyph = exports.OcrElement = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
/** @internal */
var OcrElement;
(function (OcrElement) {
    /** @internal */
    function from(source) {
        const _type = source["_type"];
        switch (_type) {
            case "Glyph": return new Glyph(source);
            case "Word": return new Word(source);
            case "Line": return new Line(source);
            case "Block": return new Block(source);
            case "Page": return new Page(source);
            default: throw new Error(`Unknown child class name: ${_type}`);
        }
    }
    OcrElement.from = from;
})(OcrElement || (exports.OcrElement = OcrElement = {}));
/**
Represents a single glyph, i.e. a character.
*/
class Glyph {
    /** @param source {@displayType `DeepPartial<Glyph>`} */
    constructor(source = {}) {
        this._type = "Glyph";
        if (source.text !== undefined) {
            this.text = source.text;
            if (!TypeVerification.verifyString(this.text)) {
                throw TypeError('text must be of type string');
            }
        }
        else {
            throw new Error("text must be present in constructor argument");
        }
        if (source.confidence !== undefined) {
            this.confidence = source.confidence;
            if (!TypeVerification.verifyDouble(this.confidence)) {
                throw TypeError('confidence must be of type number');
            }
        }
        else {
            throw new Error("confidence must be present in constructor argument");
        }
        if (source.roi !== undefined) {
            this.roi = source.roi.map((it) => { return { "x": it.x, "y": it.y }; });
            if (!TypeVerification.listVerifier(TypeVerification.verifyPointDouble)(this.roi)) {
                throw TypeError('roi must be of type Point[]');
            }
        }
        else {
            throw new Error("roi must be present in constructor argument");
        }
    }
}
exports.Glyph = Glyph;
/**
Represents a single word. A word is made up of glyphs.
*/
class Word {
    /** @param source {@displayType `DeepPartial<Word>`} */
    constructor(source = {}) {
        this._type = "Word";
        if (source.text !== undefined) {
            this.text = source.text;
            if (!TypeVerification.verifyString(this.text)) {
                throw TypeError('text must be of type string');
            }
        }
        else {
            throw new Error("text must be present in constructor argument");
        }
        if (source.confidence !== undefined) {
            this.confidence = source.confidence;
            if (!TypeVerification.verifyDouble(this.confidence)) {
                throw TypeError('confidence must be of type number');
            }
        }
        else {
            throw new Error("confidence must be present in constructor argument");
        }
        if (source.roi !== undefined) {
            this.roi = source.roi.map((it) => { return { "x": it.x, "y": it.y }; });
            if (!TypeVerification.listVerifier(TypeVerification.verifyPointDouble)(this.roi)) {
                throw TypeError('roi must be of type Point[]');
            }
        }
        else {
            throw new Error("roi must be present in constructor argument");
        }
        if (source.glyphs !== undefined) {
            this.glyphs = source.glyphs.map((it) => { return new Glyph(it); });
            if (!TypeVerification.listVerifier(TypeVerification.typeVerifier(Glyph))(this.glyphs)) {
                throw TypeError('glyphs must be of type Glyph[]');
            }
        }
        else {
            throw new Error("glyphs must be present in constructor argument");
        }
    }
}
exports.Word = Word;
/**
Represents a single line. A line is made up of words.
*/
class Line {
    /** @param source {@displayType `DeepPartial<Line>`} */
    constructor(source = {}) {
        this._type = "Line";
        if (source.text !== undefined) {
            this.text = source.text;
            if (!TypeVerification.verifyString(this.text)) {
                throw TypeError('text must be of type string');
            }
        }
        else {
            throw new Error("text must be present in constructor argument");
        }
        if (source.confidence !== undefined) {
            this.confidence = source.confidence;
            if (!TypeVerification.verifyDouble(this.confidence)) {
                throw TypeError('confidence must be of type number');
            }
        }
        else {
            throw new Error("confidence must be present in constructor argument");
        }
        if (source.roi !== undefined) {
            this.roi = source.roi.map((it) => { return { "x": it.x, "y": it.y }; });
            if (!TypeVerification.listVerifier(TypeVerification.verifyPointDouble)(this.roi)) {
                throw TypeError('roi must be of type Point[]');
            }
        }
        else {
            throw new Error("roi must be present in constructor argument");
        }
        if (source.words !== undefined) {
            this.words = source.words.map((it) => { return new Word(it); });
            if (!TypeVerification.listVerifier(TypeVerification.typeVerifier(Word))(this.words)) {
                throw TypeError('words must be of type Word[]');
            }
        }
        else {
            throw new Error("words must be present in constructor argument");
        }
    }
}
exports.Line = Line;
/**
Represents a single block, e.g. a paragraph. A block is made up of lines.
*/
class Block {
    /** @param source {@displayType `DeepPartial<Block>`} */
    constructor(source = {}) {
        this._type = "Block";
        if (source.text !== undefined) {
            this.text = source.text;
            if (!TypeVerification.verifyString(this.text)) {
                throw TypeError('text must be of type string');
            }
        }
        else {
            throw new Error("text must be present in constructor argument");
        }
        if (source.confidence !== undefined) {
            this.confidence = source.confidence;
            if (!TypeVerification.verifyDouble(this.confidence)) {
                throw TypeError('confidence must be of type number');
            }
        }
        else {
            throw new Error("confidence must be present in constructor argument");
        }
        if (source.roi !== undefined) {
            this.roi = source.roi.map((it) => { return { "x": it.x, "y": it.y }; });
            if (!TypeVerification.listVerifier(TypeVerification.verifyPointDouble)(this.roi)) {
                throw TypeError('roi must be of type Point[]');
            }
        }
        else {
            throw new Error("roi must be present in constructor argument");
        }
        if (source.lines !== undefined) {
            this.lines = source.lines.map((it) => { return new Line(it); });
            if (!TypeVerification.listVerifier(TypeVerification.typeVerifier(Line))(this.lines)) {
                throw TypeError('lines must be of type Line[]');
            }
        }
        else {
            throw new Error("lines must be present in constructor argument");
        }
    }
}
exports.Block = Block;
/**
Represents result of performing OCR on an image. A page is made up of blocks.
*/
class Page {
    /** @param source {@displayType `DeepPartial<Page>`} */
    constructor(source = {}) {
        this._type = "Page";
        if (source.text !== undefined) {
            this.text = source.text;
            if (!TypeVerification.verifyString(this.text)) {
                throw TypeError('text must be of type string');
            }
        }
        else {
            throw new Error("text must be present in constructor argument");
        }
        if (source.confidence !== undefined) {
            this.confidence = source.confidence;
            if (!TypeVerification.verifyDouble(this.confidence)) {
                throw TypeError('confidence must be of type number');
            }
        }
        else {
            throw new Error("confidence must be present in constructor argument");
        }
        if (source.roi !== undefined) {
            this.roi = source.roi.map((it) => { return { "x": it.x, "y": it.y }; });
            if (!TypeVerification.listVerifier(TypeVerification.verifyPointDouble)(this.roi)) {
                throw TypeError('roi must be of type Point[]');
            }
        }
        else {
            throw new Error("roi must be present in constructor argument");
        }
        if (source.blocks !== undefined) {
            this.blocks = source.blocks.map((it) => { return new Block(it); });
            if (!TypeVerification.listVerifier(TypeVerification.typeVerifier(Block))(this.blocks)) {
                throw TypeError('blocks must be of type Block[]');
            }
        }
        else {
            throw new Error("blocks must be present in constructor argument");
        }
    }
}
exports.Page = Page;
