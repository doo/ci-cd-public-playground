"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/GenericDocument.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.GenericDocument = exports.GenericDocumentType = exports.Field = exports.FieldType = exports.FieldParsedData = exports.FieldDataFormatValues = exports.FieldValidationStatusValues = exports.OcrResult = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const CommonFieldType_1 = require("../generic_document/CommonFieldType");
const ImageRef_1 = require("../image/ImageRef");
/**
Result of OCR text recognition.
*/
class OcrResult {
    /** @param source {@displayType `DeepPartial<OcrResult>`} */
    constructor(source = {}) {
        if (source.text !== undefined) {
            this.text = source.text;
            if (!TypeVerification.verifyString(this.text)) {
                throw TypeError('text must be of type string');
            }
        }
        else {
            throw new Error("text must be present in constructor argument");
        }
        if (source.confidence !== undefined) {
            this.confidence = source.confidence;
            if (!TypeVerification.verifyDouble(this.confidence)) {
                throw TypeError('confidence must be of type number');
            }
        }
        else {
            throw new Error("confidence must be present in constructor argument");
        }
    }
}
exports.OcrResult = OcrResult;
/** @hidden */
exports.FieldValidationStatusValues = [
    "INVALID",
    "OBSTRUCTED",
    "NONE",
    "CONFIRMED",
    "INFERRED",
    "VALID",
    "IGNORED"
];
/** @hidden */
exports.FieldDataFormatValues = [
    "ISO_DATE",
    "ISO_COUNTRY_ALPHA_2",
    "ISO_COUNTRY_ALPHA_3",
    "ISO_COUNTRY_NUMERIC",
    "ISO_COUNTRY_NAME",
    "GENDER"
];
/**
Parsed data.
*/
class FieldParsedData {
    /** @param source {@displayType `DeepPartial<FieldParsedData>`} */
    constructor(source = {}) {
        if (source.type !== undefined) {
            this.type = source.type;
            if (!TypeVerification.enumVerifier(exports.FieldDataFormatValues)(this.type)) {
                throw TypeError('type must be of type FieldDataFormat');
            }
        }
        else {
            throw new Error("type must be present in constructor argument");
        }
        if (source.value !== undefined) {
            this.value = source.value;
            if (!TypeVerification.verifyString(this.value)) {
                throw TypeError('value must be of type string');
            }
        }
        else {
            throw new Error("value must be present in constructor argument");
        }
    }
}
exports.FieldParsedData = FieldParsedData;
/**
Generic Document Type.
*/
class FieldType {
    /** @param source {@displayType `DeepPartial<FieldType>`} */
    constructor(source = {}) {
        /**
        A document can contain multiple fields of the same name, the property serves for storing natural order of such fields, null if multiple entries aren't allowed for this field.
        */
        this.listIndex = null;
        if (source.name !== undefined) {
            this.name = source.name;
            if (!TypeVerification.verifyString(this.name)) {
                throw TypeError('name must be of type string');
            }
        }
        else {
            throw new Error("name must be present in constructor argument");
        }
        if (source.fullName !== undefined) {
            this.fullName = source.fullName;
            if (!TypeVerification.verifyString(this.fullName)) {
                throw TypeError('fullName must be of type string');
            }
        }
        else {
            throw new Error("fullName must be present in constructor argument");
        }
        if (source.normalizedName !== undefined) {
            this.normalizedName = source.normalizedName;
            if (!TypeVerification.verifyString(this.normalizedName)) {
                throw TypeError('normalizedName must be of type string');
            }
        }
        else {
            throw new Error("normalizedName must be present in constructor argument");
        }
        if (source.commonType !== undefined) {
            this.commonType = source.commonType != null ? (CommonFieldType_1.CommonFieldTypeValues.includes(source.commonType) ? source.commonType : null) : null;
            if (!TypeVerification.optionalVerifier(TypeVerification.enumVerifier(CommonFieldType_1.CommonFieldTypeValues))(this.commonType)) {
                throw TypeError('commonType must be of type CommonFieldType | null');
            }
        }
        else {
            throw new Error("commonType must be present in constructor argument");
        }
        if (source.listIndex !== undefined) {
            this.listIndex = source.listIndex != null ? source.listIndex : null;
            if (!TypeVerification.optionalVerifier(TypeVerification.verifyInt)(this.listIndex)) {
                throw TypeError('listIndex must be of type number | null');
            }
        }
    }
}
exports.FieldType = FieldType;
/**
Generic document field.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getFieldFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class Field {
    /** @param source {@displayType `DeepPartial<Field>`} */
    constructor(source = {}) {
        /**
        Confidence weight.
        
        Default is 1.0
        */
        this.confidenceWeight = 1.0;
        /**
        Crop of the field.
        */
        this.image = null;
        /**
        Field validation status. Applicable only to fields that support some kind of validation.
        
        Default is NONE
        */
        this.validationStatus = "NONE";
        /**
        Parsed data.
        */
        this.parsedData = [];
        this._releasePromise = null;
        if (source.type !== undefined) {
            this.type = new FieldType(source.type);
            if (!TypeVerification.typeVerifier(FieldType)(this.type)) {
                throw TypeError('type must be of type FieldType');
            }
        }
        else {
            throw new Error("type must be present in constructor argument");
        }
        if (source.value !== undefined) {
            this.value = source.value != null ? new OcrResult(source.value) : null;
            if (!TypeVerification.optionalVerifier(TypeVerification.typeVerifier(OcrResult))(this.value)) {
                throw TypeError('value must be of type OcrResult | null');
            }
        }
        else {
            throw new Error("value must be present in constructor argument");
        }
        if (source.confidenceWeight !== undefined) {
            this.confidenceWeight = source.confidenceWeight;
            if (!TypeVerification.verifyDouble(this.confidenceWeight)) {
                throw TypeError('confidenceWeight must be of type number');
            }
        }
        if (source.image !== undefined) {
            this.image = source.image != null ? ImageRef_1.ImageRef.fromPartial(source.image) : null;
            if (!TypeVerification.optionalVerifier(TypeVerification.typeVerifier(ImageRef_1.ImageRef))(this.image)) {
                throw TypeError('image must be of type ImageRef | null');
            }
        }
        if (source.polygonInRoot !== undefined) {
            this.polygonInRoot = source.polygonInRoot.map((it) => { return { "x": it.x, "y": it.y }; });
            if (!TypeVerification.listVerifier(TypeVerification.verifyPointDouble)(this.polygonInRoot)) {
                throw TypeError('polygonInRoot must be of type Point[]');
            }
        }
        else {
            throw new Error("polygonInRoot must be present in constructor argument");
        }
        if (source.validationStatus !== undefined) {
            this.validationStatus = source.validationStatus;
            if (!TypeVerification.enumVerifier(exports.FieldValidationStatusValues)(this.validationStatus)) {
                throw TypeError('validationStatus must be of type FieldValidationStatus');
            }
        }
        if (source.parsedData !== undefined) {
            this.parsedData = source.parsedData.map((it) => { return new FieldParsedData(it); });
            if (!TypeVerification.listVerifier(TypeVerification.typeVerifier(FieldParsedData))(this.parsedData)) {
                throw TypeError('parsedData must be of type FieldParsedData[]');
            }
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            {
                if (this.image != null) {
                    promises.push(this.image.release());
                }
            }
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.Field = Field;
/**
Generic Document Type.
*/
class GenericDocumentType {
    /** @param source {@displayType `DeepPartial<GenericDocumentType>`} */
    constructor(source = {}) {
        /**
        A document can contain multiple fields of the same name, the property serves for storing natural order of such fields, null if multiple entries aren't allowed for this field.
        */
        this.listIndex = null;
        if (source.name !== undefined) {
            this.name = source.name;
            if (!TypeVerification.verifyString(this.name)) {
                throw TypeError('name must be of type string');
            }
        }
        else {
            throw new Error("name must be present in constructor argument");
        }
        if (source.fullName !== undefined) {
            this.fullName = source.fullName;
            if (!TypeVerification.verifyString(this.fullName)) {
                throw TypeError('fullName must be of type string');
            }
        }
        else {
            throw new Error("fullName must be present in constructor argument");
        }
        if (source.normalizedName !== undefined) {
            this.normalizedName = source.normalizedName;
            if (!TypeVerification.verifyString(this.normalizedName)) {
                throw TypeError('normalizedName must be of type string');
            }
        }
        else {
            throw new Error("normalizedName must be present in constructor argument");
        }
        if (source.listIndex !== undefined) {
            this.listIndex = source.listIndex != null ? source.listIndex : null;
            if (!TypeVerification.optionalVerifier(TypeVerification.verifyInt)(this.listIndex)) {
                throw TypeError('listIndex must be of type number | null');
            }
        }
    }
}
exports.GenericDocumentType = GenericDocumentType;
/**
Generic document.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getGenericDocumentFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class GenericDocument {
    /** @param source {@displayType `DeepPartial<GenericDocument>`} */
    constructor(source = {}) {
        /**
        Image crop of the document.
        */
        this.crop = null;
        /**
        The average confidence in the accuracy of the document recognition result.
        
        Default is 0
        */
        this.confidence = 0.0;
        /**
        The weight of the confidence. Can be used to calculate the weighted average confidence of two documents.
        
        Default is 0
        */
        this.confidenceWeight = 0.0;
        this._releasePromise = null;
        if (source.type !== undefined) {
            this.type = new GenericDocumentType(source.type);
            if (!TypeVerification.typeVerifier(GenericDocumentType)(this.type)) {
                throw TypeError('type must be of type GenericDocumentType');
            }
        }
        else {
            throw new Error("type must be present in constructor argument");
        }
        if (source.fields !== undefined) {
            this.fields = source.fields.map((it) => { return new Field(it); });
            if (!TypeVerification.listVerifier(TypeVerification.typeVerifier(Field))(this.fields)) {
                throw TypeError('fields must be of type Field[]');
            }
        }
        else {
            throw new Error("fields must be present in constructor argument");
        }
        if (source.children !== undefined) {
            this.children = source.children.map((it) => { return new GenericDocument(it); });
            if (!TypeVerification.listVerifier(TypeVerification.typeVerifier(GenericDocument))(this.children)) {
                throw TypeError('children must be of type GenericDocument[]');
            }
        }
        else {
            throw new Error("children must be present in constructor argument");
        }
        if (source.quad !== undefined) {
            this.quad = source.quad.map((it) => { return { "x": it.x, "y": it.y }; });
            if (!TypeVerification.listVerifier(TypeVerification.verifyPointDouble)(this.quad)) {
                throw TypeError('quad must be of type Point[]');
            }
        }
        else {
            throw new Error("quad must be present in constructor argument");
        }
        if (source.quadInRoot !== undefined) {
            this.quadInRoot = source.quadInRoot.map((it) => { return { "x": it.x, "y": it.y }; });
            if (!TypeVerification.listVerifier(TypeVerification.verifyPointDouble)(this.quadInRoot)) {
                throw TypeError('quadInRoot must be of type Point[]');
            }
        }
        else {
            throw new Error("quadInRoot must be present in constructor argument");
        }
        if (source.crop !== undefined) {
            this.crop = source.crop != null ? ImageRef_1.ImageRef.fromPartial(source.crop) : null;
            if (!TypeVerification.optionalVerifier(TypeVerification.typeVerifier(ImageRef_1.ImageRef))(this.crop)) {
                throw TypeError('crop must be of type ImageRef | null');
            }
        }
        if (source.confidence !== undefined) {
            this.confidence = source.confidence;
            if (!TypeVerification.verifyDouble(this.confidence)) {
                throw TypeError('confidence must be of type number');
            }
        }
        if (source.confidenceWeight !== undefined) {
            this.confidenceWeight = source.confidenceWeight;
            if (!TypeVerification.verifyDouble(this.confidenceWeight)) {
                throw TypeError('confidenceWeight must be of type number');
            }
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            this.fields.forEach((it) => { promises.push(it.release()); });
            this.children.forEach((it) => { promises.push(it.release()); });
            {
                if (this.crop != null) {
                    promises.push(this.crop.release());
                }
            }
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.GenericDocument = GenericDocument;
