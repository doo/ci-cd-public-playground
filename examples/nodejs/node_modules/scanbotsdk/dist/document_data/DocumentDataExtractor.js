"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/DocumentDataExtractor.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DocumentDataExtractor = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const DocumentDataExtractorTypes_1 = require("../document_data/DocumentDataExtractorTypes");
const DocumentDataExtractorTypes_2 = require("../document_data/DocumentDataExtractorTypes");
const DocumentDataExtractorTypes_3 = require("../document_data/DocumentDataExtractorTypes");
const GenericDocument_1 = require("../generic_document/GenericDocument");
const ImageRef_1 = require("../image/ImageRef");
const Errors_1 = require("../support/Errors");
const addonNative = require('../../native/scanbotsdk_nodejs.node');
/**
 API for extracting data from documents.
 @example
 Since the class is backed by native resources and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created to ensure the resource is disposed automatically:
 ```
 await using instance = await DocumentDataExtractor.create(...);
 ```
 Alternatively, one can explicitly call {@link dispose} method to release the native resource. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
 */
class DocumentDataExtractor {
    /**
    Initialize the document data extractor.
    @param configuration - The configuration to use for the recognizer.
    @returns Instance of DocumentDataExtractor.
    */
    static async create(configuration = new DocumentDataExtractorTypes_2.DocumentDataExtractorConfiguration({})) {
        DocumentDataExtractor.performStaticInitialization();
        if (!TypeVerification.typeVerifier(DocumentDataExtractorTypes_2.DocumentDataExtractorConfiguration)(configuration)) {
            throw TypeError('configuration must be of type DocumentDataExtractorConfiguration');
        }
        const nativeHandle = await DocumentDataExtractor.nativeDocumentDataExtractorInit(configuration).catch(Errors_1.remapError);
        return new DocumentDataExtractor(nativeHandle);
    }
    constructor(nativeHandle) {
        this.disposePromise = null;
        this.nativeHandle = nativeHandle;
        this.nativeRun = addonNative.nativeDocumentDataExtractorRun.bind(this);
        this.nativeClearResult = addonNative.nativeDocumentDataExtractorClearResult.bind(this);
        this.nativeVerifyDocument = addonNative.nativeDocumentDataExtractorVerifyDocument.bind(this);
        DocumentDataExtractor.registry.register(this, nativeHandle, this);
    }
    [Symbol.asyncDispose]() {
        return this.dispose();
    }
    /**
    As the instance is backed by a native resource, it should be disposed when it is no longer needed.
    This method will release the native resource.
    As a recommended alternative since the class implements `AsyncDisposable` one can use 'await using' syntax to ensure the resource is disposed of automatically.
    If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
    @returns A promise that resolves when the instance is disposed.
    */
    dispose() {
        if (this.disposePromise === null) {
            let nativeHandle = this.nativeHandle;
            DocumentDataExtractor.registry.unregister(this);
            this.nativeHandle = null;
            this.disposePromise = DocumentDataExtractor.dispose(nativeHandle);
        }
        return this.disposePromise;
    }
    static dispose(nativeHandle) {
        return DocumentDataExtractor.nativeDispose(nativeHandle).catch(Errors_1.remapError);
    }
    /**
    Recognize the document in the given image.
    @param image - The image to recognize.
    @returns The recognition result.
    */
    async run(image) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.typeVerifier(ImageRef_1.ImageRef)(image)) {
            throw TypeError('image must be of type ImageRef');
        }
        const partial = await this.nativeRun(image).catch(Errors_1.remapError);
        return new DocumentDataExtractorTypes_1.DocumentDataExtractionResult(partial);
    }
    /**
    Clears the accumulated results. Should be called after a document has been recognized to the customers
    * satisfaction to prevent a poisoned cache.
    *
    */
    async clearResult() {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        const partial = await this.nativeClearResult().catch(Errors_1.remapError);
        return partial;
    }
    /**
    Verify whether the extracted document has signs of forgery or tampering.
    @param documentParts - The document parts to verify. For example, the front and back of an ID card.
    @returns The verification report.
    */
    async verifyDocument(documentParts) {
        if (this.nativeHandle == null) {
            throw (0, Errors_1.createError)('IllegalState', 'This instance has already been disposed');
        }
        if (!TypeVerification.listVerifier(TypeVerification.typeVerifier(GenericDocument_1.GenericDocument))(documentParts)) {
            throw TypeError('documentParts must be of type GenericDocument[]');
        }
        const partial = await this.nativeVerifyDocument(documentParts).catch(Errors_1.remapError);
        return new DocumentDataExtractorTypes_3.DocumentVerificationReport(partial);
    }
    static performStaticInitialization() {
        if (DocumentDataExtractor.staticInitializationPerformed) {
            return;
        }
        DocumentDataExtractor.staticInitializationPerformed = true;
        DocumentDataExtractor.nativeDocumentDataExtractorInit = addonNative.nativeDocumentDataExtractorInit.bind(DocumentDataExtractor);
        DocumentDataExtractor.nativeDispose = addonNative.nativeDocumentDataExtractorDispose;
    }
}
exports.DocumentDataExtractor = DocumentDataExtractor;
DocumentDataExtractor.registry = new FinalizationRegistry((nativeHandle) => {
    DocumentDataExtractor.nativeDispose(nativeHandle);
});
DocumentDataExtractor.staticInitializationPerformed = false;
