"use strict";
/// Auto-generated with ScanbotSDKCodegenV3. Modifications will be overwritten.
/// Generated from SchemasV3/CoreSchemas/schemas/DocumentDataExtractorTypes.yaml
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DocumentVerificationReport = exports.DocumentVerificationStatusValues = exports.InvalidDataVerificationReport = exports.FieldInvalidDataVerificationReport = exports.SuspiciousDataVerificationReport = exports.FieldSuspiciousDataVerificationReport = exports.SemanticsVerificationReport = exports.CompositeFieldValueSemantics = exports.FieldValueSemantics = exports.ValueSemanticsValues = exports.ConsistencyVerificationReport = exports.FieldConsistencyVerificationReport = exports.ValueConsistencyValues = exports.DocumentDataExtractorConfiguration = exports.DocumentTrustModeValues = exports.DocumentDataExtractionResult = exports.DocumentDataExtractionStatusValues = void 0;
require("../support/Disposable");
const TypeVerification = __importStar(require("../support/TypeVerification"));
const DocumentDataExtractorConfigurationTypes_1 = require("../document_data/DocumentDataExtractorConfigurationTypes");
const DocumentDataExtractorConfigurationTypes_2 = require("../document_data/DocumentDataExtractorConfigurationTypes");
const DocumentDataExtractorConfigurationTypes_3 = require("../document_data/DocumentDataExtractorConfigurationTypes");
const DocumentDataExtractorConfigurationTypes_4 = require("../document_data/DocumentDataExtractorConfigurationTypes");
const DocumentDataExtractorConfigurationTypes_5 = require("../document_data/DocumentDataExtractorConfigurationTypes");
const DocumentScannerTypes_1 = require("../document_scanner/DocumentScannerTypes");
const DocumentDataExtractorConfigurationTypes_6 = require("../document_data/DocumentDataExtractorConfigurationTypes");
const GenericDocument_1 = require("../generic_document/GenericDocument");
const DocumentDataExtractorConfigurationTypes_7 = require("../document_data/DocumentDataExtractorConfigurationTypes");
const ImageRef_1 = require("../image/ImageRef");
const DocumentDataExtractorConfigurationTypes_8 = require("../document_data/DocumentDataExtractorConfigurationTypes");
const CommonTypes_1 = require("../common/CommonTypes");
const FrameAccumulationTypes_1 = require("../frame_accumulation/FrameAccumulationTypes");
/** @hidden */
exports.DocumentDataExtractionStatusValues = [
    "OK",
    "OK_BUT_INVALID_DOCUMENT",
    "OK_BUT_NOT_CONFIRMED",
    "SCANNING_IN_PROGRESS_STILL_FOCUSING",
    "ERROR_NOTHING_FOUND",
    "ERROR_BAD_CROP",
    "ERROR_UNKNOWN_DOCUMENT",
    "ERROR_UNACCEPTABLE_DOCUMENT"
];
/**
Contains the result of running the document data extractor.
@example
Since the instance of the class contains objects backed by native resources (e.g. images) and implements `AsyncDisposable` it's highly recommended to use the `await using` syntax when an instance is created or is received from an API:
```
await using instance = await getDocumentDataExtractionResultFromAnApi();
```
Alternatively, one can explicitly call {@link release} method to release the native resources or use {@link autorelease} convenience function. If neither is used, the resource will be released when the instance is garbage collected which is not recommended.
*/
class DocumentDataExtractionResult {
    /** @param source {@displayType `DeepPartial<DocumentDataExtractionResult>`} */
    constructor(source = {}) {
        /**
        Crop of the document if it was detected.
        */
        this.croppedImage = null;
        this._releasePromise = null;
        if (source.status !== undefined) {
            this.status = source.status;
        }
        else {
            throw new Error("status must be present in constructor argument");
        }
        if (source.document !== undefined) {
            this.document = source.document != null ? new GenericDocument_1.GenericDocument(source.document) : null;
        }
        else {
            throw new Error("document must be present in constructor argument");
        }
        if (source.documentDetectionResult !== undefined) {
            this.documentDetectionResult = new DocumentScannerTypes_1.DocumentDetectionResult(source.documentDetectionResult);
        }
        else {
            throw new Error("documentDetectionResult must be present in constructor argument");
        }
        if (source.croppedImage !== undefined) {
            this.croppedImage = source.croppedImage != null ? ImageRef_1.ImageRef.fromPartial(source.croppedImage) : null;
        }
    }
    release() {
        if (this._releasePromise === null) {
            let promises = [];
            {
                if (this.document != null) {
                    promises.push(this.document.release());
                }
            }
            {
                if (this.croppedImage != null) {
                    promises.push(this.croppedImage.release());
                }
            }
            this._releasePromise = Promise.all(promises).then(() => { });
        }
        return this._releasePromise;
    }
    [Symbol.asyncDispose]() {
        return this.release();
    }
}
exports.DocumentDataExtractionResult = DocumentDataExtractionResult;
/** @hidden */
exports.DocumentTrustModeValues = [
    "TRUSTED",
    "UNTRUSTED"
];
/**
Configuration for the document data extractor.
*/
class DocumentDataExtractorConfiguration {
    /** @param source {@displayType `DeepPartial<DocumentDataExtractorConfiguration>`} */
    constructor(source = {}) {
        /**
        Configuration for how to accumulate results.
        */
        this.resultAccumulationConfig = new FrameAccumulationTypes_1.ResultAccumulationConfiguration({});
        /**
        Normalized names of the fields to exclude from the result.
        */
        this.fieldExcludeList = [];
        /**
        List of configuration elements for the document data extractor.
        */
        this.configurations = [new DocumentDataExtractorConfigurationTypes_4.DocumentDataExtractorCommonConfiguration({})];
        /**
        If true, crops of the detected documents and fields will be returned in the result.
        
        Default is false
        */
        this.returnCrops = false;
        /**
        Level of trust in the authenticity of the extracted document.
        
        Default is TRUSTED
        */
        this.documentTrustMode = "TRUSTED";
        /**
        Scanners typically can produce better results from a single image if they are allowed to spend a longer time analyzing it.
        
        On the other hand, scanners can produce even better results if they can analyze multiple images of the same subject and cross-check and combine the results.
        The processing mode tells the scanner whether to optimize for single or multiple images of a subject.
        
        Default is AUTO
        */
        this.processingMode = "AUTO";
        if (source.resultAccumulationConfig !== undefined) {
            this.resultAccumulationConfig = new FrameAccumulationTypes_1.ResultAccumulationConfiguration(source.resultAccumulationConfig);
            if (!TypeVerification.typeVerifier(FrameAccumulationTypes_1.ResultAccumulationConfiguration)(this.resultAccumulationConfig)) {
                throw TypeError('resultAccumulationConfig must be of type ResultAccumulationConfiguration');
            }
        }
        if (source.fieldExcludeList !== undefined) {
            this.fieldExcludeList = source.fieldExcludeList.map((it) => { return it; });
            if (!TypeVerification.listVerifier(TypeVerification.verifyString)(this.fieldExcludeList)) {
                throw TypeError('fieldExcludeList must be of type string[]');
            }
        }
        if (source.configurations !== undefined) {
            this.configurations = source.configurations.map((it) => { return DocumentDataExtractorConfigurationTypes_5.DocumentDataExtractorConfigurationElement.from(it); });
            if (!TypeVerification.listVerifier(TypeVerification.polymorphicTypeVerifier([DocumentDataExtractorConfigurationTypes_2.DateValidationConfiguration, DocumentDataExtractorConfigurationTypes_3.DeAddressParserConfiguration, DocumentDataExtractorConfigurationTypes_6.EuropeanHealthInsuranceCardConfiguration, DocumentDataExtractorConfigurationTypes_8.MrzFallbackConfiguration, DocumentDataExtractorConfigurationTypes_1.AutoFocusConfiguration, DocumentDataExtractorConfigurationTypes_7.GlareDetectionConfiguration, DocumentDataExtractorConfigurationTypes_4.DocumentDataExtractorCommonConfiguration]))(this.configurations)) {
                throw TypeError('configurations must be of type DocumentDataExtractorConfigurationElement[]');
            }
        }
        if (source.returnCrops !== undefined) {
            this.returnCrops = source.returnCrops;
            if (!TypeVerification.verifyBoolean(this.returnCrops)) {
                throw TypeError('returnCrops must be of type boolean');
            }
        }
        if (source.documentTrustMode !== undefined) {
            this.documentTrustMode = source.documentTrustMode;
            if (!TypeVerification.enumVerifier(exports.DocumentTrustModeValues)(this.documentTrustMode)) {
                throw TypeError('documentTrustMode must be of type DocumentTrustMode');
            }
        }
        if (source.processingMode !== undefined) {
            this.processingMode = source.processingMode;
            if (!TypeVerification.enumVerifier(CommonTypes_1.ProcessingModeValues)(this.processingMode)) {
                throw TypeError('processingMode must be of type ProcessingMode');
            }
        }
    }
}
exports.DocumentDataExtractorConfiguration = DocumentDataExtractorConfiguration;
/** @hidden */
exports.ValueConsistencyValues = [
    "OK",
    "OK_PUNCTUATION_UPPER_CASE_CLEANED",
    "OK_DEBURRED",
    "OK_DEBURRED_EXTENDED",
    "OK_TRUNCATED",
    "INCONSISTENT"
];
/**
Report of the field consistency verification.
*/
class FieldConsistencyVerificationReport {
    /** @param source {@displayType `DeepPartial<FieldConsistencyVerificationReport>`} */
    constructor(source = {}) {
        if (source.fieldType !== undefined) {
            this.fieldType = source.fieldType;
        }
        else {
            throw new Error("fieldType must be present in constructor argument");
        }
        if (source.consistency !== undefined) {
            this.consistency = source.consistency;
        }
        else {
            throw new Error("consistency must be present in constructor argument");
        }
        if (source.fieldFullNames !== undefined) {
            this.fieldFullNames = source.fieldFullNames.map((it) => { return it; });
        }
        else {
            throw new Error("fieldFullNames must be present in constructor argument");
        }
    }
}
exports.FieldConsistencyVerificationReport = FieldConsistencyVerificationReport;
/**
Report of the document consistency verification. Shows whether information in the fields that correspond to the same logical field is consistent.
*/
class ConsistencyVerificationReport {
    /** @param source {@displayType `DeepPartial<ConsistencyVerificationReport>`} */
    constructor(source = {}) {
        if (source.overallConsistency !== undefined) {
            this.overallConsistency = source.overallConsistency;
        }
        else {
            throw new Error("overallConsistency must be present in constructor argument");
        }
        if (source.fieldDetails !== undefined) {
            this.fieldDetails = source.fieldDetails.map((it) => { return new FieldConsistencyVerificationReport(it); });
        }
        else {
            throw new Error("fieldDetails must be present in constructor argument");
        }
    }
}
exports.ConsistencyVerificationReport = ConsistencyVerificationReport;
/** @hidden */
exports.ValueSemanticsValues = [
    "SKIPPED",
    "VALID",
    "INVALID"
];
/**
Report of the field value semantics verification.
*/
class FieldValueSemantics {
    /** @param source {@displayType `DeepPartial<FieldValueSemantics>`} */
    constructor(source = {}) {
        if (source.fieldFullName !== undefined) {
            this.fieldFullName = source.fieldFullName;
        }
        else {
            throw new Error("fieldFullName must be present in constructor argument");
        }
        if (source.invalidSemantics !== undefined) {
            this.invalidSemantics = source.invalidSemantics;
        }
        else {
            throw new Error("invalidSemantics must be present in constructor argument");
        }
    }
}
exports.FieldValueSemantics = FieldValueSemantics;
/**
Report of a logical field value semantics verification. If some logical field, e.g. expiry date is present in multiple places in the document, it can occur that some entries are semantically correct and some are semantically incorrect. This report shows the overall status of the logical field value semantics verification and details for each field that was verified.
*/
class CompositeFieldValueSemantics {
    /** @param source {@displayType `DeepPartial<CompositeFieldValueSemantics>`} */
    constructor(source = {}) {
        if (source.overallFieldValueSemantics !== undefined) {
            this.overallFieldValueSemantics = source.overallFieldValueSemantics;
        }
        else {
            throw new Error("overallFieldValueSemantics must be present in constructor argument");
        }
        if (source.fieldDetails !== undefined) {
            this.fieldDetails = source.fieldDetails.map((it) => { return new FieldValueSemantics(it); });
        }
        else {
            throw new Error("fieldDetails must be present in constructor argument");
        }
    }
}
exports.CompositeFieldValueSemantics = CompositeFieldValueSemantics;
/**
Report of the document value semantics verification.
*/
class SemanticsVerificationReport {
    /** @param source {@displayType `DeepPartial<SemanticsVerificationReport>`} */
    constructor(source = {}) {
        if (source.overallInvalidSemantics !== undefined) {
            this.overallInvalidSemantics = source.overallInvalidSemantics;
        }
        else {
            throw new Error("overallInvalidSemantics must be present in constructor argument");
        }
        if (source.expiryDateSemantics !== undefined) {
            this.expiryDateSemantics = new CompositeFieldValueSemantics(source.expiryDateSemantics);
        }
        else {
            throw new Error("expiryDateSemantics must be present in constructor argument");
        }
        if (source.issueDateSemantics !== undefined) {
            this.issueDateSemantics = new CompositeFieldValueSemantics(source.issueDateSemantics);
        }
        else {
            throw new Error("issueDateSemantics must be present in constructor argument");
        }
        if (source.birthDateSemantics !== undefined) {
            this.birthDateSemantics = new CompositeFieldValueSemantics(source.birthDateSemantics);
        }
        else {
            throw new Error("birthDateSemantics must be present in constructor argument");
        }
        if (source.issueDateBeforeExpiryDate !== undefined) {
            this.issueDateBeforeExpiryDate = source.issueDateBeforeExpiryDate;
        }
        else {
            throw new Error("issueDateBeforeExpiryDate must be present in constructor argument");
        }
        if (source.birthDateBeforeIssueDate !== undefined) {
            this.birthDateBeforeIssueDate = source.birthDateBeforeIssueDate;
        }
        else {
            throw new Error("birthDateBeforeIssueDate must be present in constructor argument");
        }
        if (source.birthDateBeforeExpiryDate !== undefined) {
            this.birthDateBeforeExpiryDate = source.birthDateBeforeExpiryDate;
        }
        else {
            throw new Error("birthDateBeforeExpiryDate must be present in constructor argument");
        }
    }
}
exports.SemanticsVerificationReport = SemanticsVerificationReport;
/**
Report of the suspicious data field verification.
*/
class FieldSuspiciousDataVerificationReport {
    /** @param source {@displayType `DeepPartial<FieldSuspiciousDataVerificationReport>`} */
    constructor(source = {}) {
        if (source.fieldFullName !== undefined) {
            this.fieldFullName = source.fieldFullName;
        }
        else {
            throw new Error("fieldFullName must be present in constructor argument");
        }
        if (source.isSuspicious !== undefined) {
            this.isSuspicious = source.isSuspicious;
        }
        else {
            throw new Error("isSuspicious must be present in constructor argument");
        }
    }
}
exports.FieldSuspiciousDataVerificationReport = FieldSuspiciousDataVerificationReport;
/**
Report of the suspicious data document verification.
*/
class SuspiciousDataVerificationReport {
    /** @param source {@displayType `DeepPartial<SuspiciousDataVerificationReport>`} */
    constructor(source = {}) {
        if (source.overallSuspiciousData !== undefined) {
            this.overallSuspiciousData = source.overallSuspiciousData;
        }
        else {
            throw new Error("overallSuspiciousData must be present in constructor argument");
        }
        if (source.fieldDetails !== undefined) {
            this.fieldDetails = source.fieldDetails.map((it) => { return new FieldSuspiciousDataVerificationReport(it); });
        }
        else {
            throw new Error("fieldDetails must be present in constructor argument");
        }
    }
}
exports.SuspiciousDataVerificationReport = SuspiciousDataVerificationReport;
/**
Report which shows whether the data in the field that can be validated is valid.
*/
class FieldInvalidDataVerificationReport {
    /** @param source {@displayType `DeepPartial<FieldInvalidDataVerificationReport>`} */
    constructor(source = {}) {
        if (source.fieldFullName !== undefined) {
            this.fieldFullName = source.fieldFullName;
        }
        else {
            throw new Error("fieldFullName must be present in constructor argument");
        }
        if (source.isInvalid !== undefined) {
            this.isInvalid = source.isInvalid;
        }
        else {
            throw new Error("isInvalid must be present in constructor argument");
        }
    }
}
exports.FieldInvalidDataVerificationReport = FieldInvalidDataVerificationReport;
/**
Report of the field validation verification.
*/
class InvalidDataVerificationReport {
    /** @param source {@displayType `DeepPartial<InvalidDataVerificationReport>`} */
    constructor(source = {}) {
        if (source.overallIsInvalid !== undefined) {
            this.overallIsInvalid = source.overallIsInvalid;
        }
        else {
            throw new Error("overallIsInvalid must be present in constructor argument");
        }
        if (source.fieldDetails !== undefined) {
            this.fieldDetails = source.fieldDetails.map((it) => { return new FieldInvalidDataVerificationReport(it); });
        }
        else {
            throw new Error("fieldDetails must be present in constructor argument");
        }
    }
}
exports.InvalidDataVerificationReport = InvalidDataVerificationReport;
/** @hidden */
exports.DocumentVerificationStatusValues = [
    "OK",
    "OK_WITH_WARNINGS",
    "INVALID"
];
/**
Report of the document verification.
*/
class DocumentVerificationReport {
    /** @param source {@displayType `DeepPartial<DocumentVerificationReport>`} */
    constructor(source = {}) {
        if (source.overallVerificationStatus !== undefined) {
            this.overallVerificationStatus = source.overallVerificationStatus;
        }
        else {
            throw new Error("overallVerificationStatus must be present in constructor argument");
        }
        if (source.consistencyVerificationReport !== undefined) {
            this.consistencyVerificationReport = new ConsistencyVerificationReport(source.consistencyVerificationReport);
        }
        else {
            throw new Error("consistencyVerificationReport must be present in constructor argument");
        }
        if (source.semanticsVerificationReport !== undefined) {
            this.semanticsVerificationReport = new SemanticsVerificationReport(source.semanticsVerificationReport);
        }
        else {
            throw new Error("semanticsVerificationReport must be present in constructor argument");
        }
        if (source.suspiciousDataVerificationReport !== undefined) {
            this.suspiciousDataVerificationReport = new SuspiciousDataVerificationReport(source.suspiciousDataVerificationReport);
        }
        else {
            throw new Error("suspiciousDataVerificationReport must be present in constructor argument");
        }
        if (source.invalidDataVerificationReport !== undefined) {
            this.invalidDataVerificationReport = new InvalidDataVerificationReport(source.invalidDataVerificationReport);
        }
        else {
            throw new Error("invalidDataVerificationReport must be present in constructor argument");
        }
    }
}
exports.DocumentVerificationReport = DocumentVerificationReport;
